<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: rust-core

Cite these in .sofar/repo.md by qualified handle — `rust-core M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-09-15T18:33:25.187Z) — Hot-path perf: `npm run perf` (SOFAR_PERF=1 vitest --project perf, ~6 min, spawns ~1,200 processes) measures every hook, statusline and status at scale; `SOFAR_PERF_RECORD=1` re-records packages/engine/test/conformance/perf/baseline.typescript.json (+ .md), `SOFAR_PERF_CELLS=repo,floor` limits cells (merged into the baseline when recording), `SOFAR_CONFORMANCE_BIN=<binary>` measures a candidate with ratios, `SOFAR_PERF_GATE=1` fails a candidate over the target. Results for non-record runs land in $TMPDIR/sofar-perf.<impl>.{json,md}. Skipped under plain `npm test`. Never run it with other CPU-heavy work in flight — the numbers are wall clock.
- **M2** (2026-09-16T07:19:57.723Z) — Rust envelope cross-check against the TypeScript reference (rust-core 2.2): `npx esbuild packages/engine/test/conformance/canon-pairs.ts --bundle --platform=node --format=esm --outfile=/tmp/canon-pairs.mjs && node /tmp/canon-pairs.mjs packages/engine/test/conformance/fixtures /tmp/canon-pairs.tsv && SOFAR_CANON_PAIRS=/tmp/canon-pairs.tsv cargo test -p sofar-core --test canonical_crosscheck -- --ignored`. Ignored by default (the pairs file is generated, ~8 MB); run it whenever json.rs, envelope.rs or the TypeScript envelope/log.ts change. Passing means every fixture line (13,095 at 179b8fd-equivalent sources) parses, validates and re-serializes byte-identically, with the same warning text for the corrupt ones.
- **M3** (2026-09-16T08:52:36.385Z) — Fold parity against the Rust core: `cargo build --release -p sofar-core && SOFAR_CONFORMANCE_BIN="$PWD/target/release/sofar-core" npx vitest run fold-parity` (the runner splits the value on spaces, so use an absolute path with none; needs `npm run build` first for the reference in record mode only). The same eight cases run in-process with `cargo test -p sofar-core --test fold_parity`, which fails with a byte diff against golden/*.state.json. `npm run schema:emit` now runs BOTH emitters (fingerprint then Rust JSON Schema); a schema change means schema:emit, then `cargo xtask schema`, then a bumped snapshot version by construction (the fingerprint hash is baked at compile time via include_str!, the engine version via build.rs from packages/engine/package.json).
- **M4** (2026-09-16T11:02:02.037Z) — Schema codegen after a merge from r1-fixes/main: `npm run build && npm run schema:emit && cargo xtask schema`, then `cargo build -p sofar-schema`. The generated crate derives PartialEq only (not Eq, since SuggestionTrust has real floats); every INTEGER-valued number field in packages/schema/src/events.ts needs a `/** @asType integer */` JSDoc line or it generates as f64 — these annotations live on the rust-core branch (exit, count, detector_version, judged, and the 1.4 set), so a peer adding a number field on r1-fixes will build-break the crate here until annotated. Hot-path golden re-pin (D11): copy golden/ aside as golden-<version-or-sha>/, `SOFAR_CONFORMANCE_RECORD=1 npx vitest run conformance`, inspect `git diff -U0 golden/`, rewrite golden/MANIFEST.md with the sha and one reason per changed golden, update the README's Golden sets paragraph.
- **M5** (2026-09-16T11:43:19.298Z) — Schema regen order in rust-core: `npm run build` FIRST, then `npm run schema:emit`, then `cargo xtask schema` (or `npm run schema:check`). emit-fingerprint.mjs reads packages/engine/dist/schema.js, so with a stale dist it silently rewrites packages/schema/schema-fingerprint.txt to the OLD fingerprint — and the Rust snapshot version (snapshot.rs include_str of that file) then disagrees with the TypeScript engine's. Diagnose with `git diff packages/schema/schema-fingerprint.txt` after a merge that touched packages/schema/src.
- **M6** (2026-09-16T12:56:11.759Z) — Unfiltered rust-core proof of the mixed install (3.1): `cargo build --release` then `SOFAR_CORE=$PWD/target/release/sofar-core npx vitest run --project unit packages/engine/test/conformance/conformance.test.ts` — the reference stub dispatches every event/statusline/status to the binary and falls back on its exit 64; no SOFAR_CONFORMANCE_SKIP, all 27 cases. Negative control: `SOFAR_CORE=/usr/bin/false …` must fail ~25/27. `SOFAR_CORE=0` forces pure TypeScript; the perf harness takes the same variable (`SOFAR_PERF=1 SOFAR_CORE=… npx vitest run --project perf`) to measure what a mixed install pays through node. Adding a module under packages/engine/src fails architecture-doc.test.ts until docs/ARCHITECTURE.md names it.
- **M7** (2026-09-16T13:43:25.002Z) — Release with the native core (rust-core 3.2): bump packages/engine/package.json, `node packaging/npm/emit.mjs` (regenerates the five platform packages and sofar.sh's optionalDependencies; `--check` fails on drift), download the `sofar-core-<target>` artifacts from the CI `core` job into one dir, `node packaging/npm/emit.mjs --binaries DIR`, `npm publish` each packaging/npm/sofar-core-* package FIRST, then the user runs `! npm publish -w sofar.sh` (M1). sofar.sh pins the platform packages at its exact version, so the order matters. Local check of the whole channel: `cargo build --release -p sofar-core` then `npx vitest run --project unit packages/engine/test/packaging.test.ts` (the native-core E2E case skips without the release binary). npm resolves/installs sofar.sh fine while the platform packages are unpublished (lockfile marks them `optional: true`).
- **M8** (2026-09-17T10:37:57.191Z) — Byte-exact fixtures must stay LF (rust-core 3.2, Windows CI): packages/engine/test/conformance/** (cases, goldens, fixtures), crates/sofar-core/tests/** and crates/sofar-schema/schema/** are pinned `text eol=lf` in .gitattributes because the Windows runner's autocrlf checkout rewrote them to CRLF and fold-parity's golden compare failed on `\r`. Any new byte-compared fixture directory needs the same line, or the Windows job is the first place it breaks.
- **M9** (2026-09-17T10:41:45.733Z) — The conformance goldens and the Rust core's V8 message fixtures (D13) name Node 24: JSON.parse error text differs by runtime ("Expected property name or '}' in JSON at position 1 (line 1 column 2)" on Node 24; no parenthetical on Node 20), so CI runs every golden job on node-version 24 (rust-core 3.2, first Linux mixed-install run). Re-record goldens only on Node 24, and bump this pin together with the fixtures when the reference runtime moves.
- **M10** (2026-09-21T10:12:52.839Z) — A vitest run that hangs in CI with no failure (job cancelled at the 6h limit) is a file that never finished: vitest prints a file's results only on completion, so diff the files the CI log reported against `git ls-files '*.test.ts'`. Never point a test's "unwritable" path under /proc: on Linux, mkdirSync({recursive:true}) there loops forever (ENOENT/EEXIST retry), a sync spin no test timeout can interrupt; use a path under a regular file (ENOTDIR on every platform, even as root). Reproduce Linux-only behaviour locally with Docker (`open -a Docker`, then node:20 / node:24 images).
- **M11** (2026-09-21T16:23:11.699Z) — sofar_update_plan (plan_updated) rebuilds the phase objects, so every phase NOTE set by an earlier phase_status_changed is silently dropped. The fold warns only about task and phase STATUSES, and the plan schema has no field to restate a note. After any plan replacement, re-issue sofar_update_phase with the same status and note for each phase that had one. To write to a record that lives on another worktree's branch (e.g. rust-core in ~/IO/sofar-rust-core) from a session whose MCP server is bound to main, spawn `sofar mcp` with cwd set to that worktree and call the tools over stdio JSON-RPC (initialize, notifications/initialized, tools/call). A plan_updated from main's stale copy would clobber the branch plan on union. Event-only writes (notes, memories, session events) can instead go through main and be carried over with `sofar export <slug>` | `sofar import - <slug> --root <worktree>`, which dedupes by id.
- **M12** (2026-09-22T07:28:34.294Z) — Run long perf harnesses on this laptop under `caffeinate -ims` (e.g. `caffeinate -ims npx vitest run --project perf`). If the Mac idle-sleeps mid-run, especially on battery overnight, the harness fails with `spawnSync ... ETIMEDOUT` at a random measure or inside prepare(). The 120 s spawn timeout counts wall-clock time, and that includes the sleep. The same spawns run directly take 1-4 s. Diagnose with `pmset -g log | grep -E 'Entering Sleep|DarkWake'` over the run's window. Found 2026-09-22 when two team100 A/B runs died this way (sleep at 23:56 and 23:58 for 1,050 s).
- **M13** (2026-09-22T15:17:54.623Z) — File-mention cross-check against the TypeScript extractor (memory-lead 2.1 mirror): bundle a script that imports packages/engine/src/core/file-mentions and writes one JSON line per decision/note text found in every .sofar/initiatives/*/events.jsonl, as {text, mentions, depths} with depths against /repo/src/core/fold.ts. Then run `SOFAR_MENTIONS_PAIRS=<jsonl> cargo test -p sofar-core --test mentions_crosscheck -- --ignored`. Ignored by default. Run it whenever file_mentions.rs or core/file-mentions.ts changes. It passed on 1,238 texts at main a4f270a.
- **M14** (2026-09-22T15:17:54.644Z) — After merging main into rust-core, generated record files (.sofar/**/*.md) often conflict, and events.jsonl never does (merge=union). Take either side with `git checkout --ours -- <paths>`, then regenerate from the union-merged log. `sofar import` re-projects only when it appends something, so it cannot do this. Instead, esbuild-bundle a 4-line script that calls createToolContext(root).foldState(slug) and regenerateProjections(ctx.initiativeDir(slug), state) from packages/engine/src, and run it with the root and slugs. Before committing, check `grep -c '^<<<<<<<' events.jsonl` is 0: several logs hold literal conflict markers inside note payloads, which are not conflicts.
- **M15** (2026-09-22T15:17:54.661Z) — Conformance cases that need files outside the fixture's dot-sofar use the step's `before: (m) => …` hook (m.root, m.home). syn.surfacing writes source files and a check-approval trust file at <home>/.local/state/sofar/checks/<sha256(realpath(root/.git))[:32]>.json with approved[sha256(cmd)]. syn.copies fakes linked worktrees under m.home: <root>/.git/worktrees/<n>/{gitdir,HEAD,commondir} plus <home>/<n>/.git. They go under m.home so the goldens tildify them to ~/<n>. Scratch paths outside root and home are not masked and would make the golden machine-specific.
