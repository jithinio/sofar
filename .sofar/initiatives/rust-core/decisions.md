<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: rust-core

- 2026-09-15T15:56:18.112Z — rule: **Hot-path event and projection behaviour is defined by docs/SPEC.md plus the conformance suite and must pass against both the TypeScript and Rust implementations; Rust schema types are generated from packages/schema/src, never hand-written.** — chose **Adopt Rust for sofar's HOT PATH, incrementally, starting now. Ruled by the run owner 2026-09-15 ("yes do it").
SUPERSEDES, for the hot path only, the CLAUDE.md engineering conventions:
- "TypeScript strict mode; Node ≥18; ES modules";
- "Dependencies: … Nothing else without logging a Decision". Rust crates are limited to serde, serde_json and ulid until another Decision adds one.

Scope of `sofar-core`: hook handlers (session-start, user-prompt-submit, post-tool-use, stop, session-end), atomic event append, fold, digest/status render, and statusline.

TypeScript keeps: the MCP server, `sofar drive`, init/doctor/review, and cloud commands.

The contract comes first: docs/SPEC.md plus a black-box conformance suite over commands, hook stdin/stdout, env and record bytes. It runs against BOTH implementations.

The schema rail holds: event payload types stay authored ONLY in packages/schema/src. Rust types are GENERATED from them, never hand-written. Projection templates keep one source of truth, and Rust output is verified byte-identical by golden tests.

Parity gate: 100% conformance, perf targets met, and a benchmark arm "sofar-rust-core" vs "sofar-ts" that shrinks no D19 lead margin.

Sequencing: r1-fixes waves 1–2 land in TypeScript now, and the Rust core must reach parity INCLUDING them. After parity, new hot-path code is Rust-only, which stops the TypeScript port surface growing. Distribution: prebuilt per-platform binaries through npm optionalDependencies, with a TypeScript fallback.** over 
- A big-bang rewrite now: it freezes waves 1–3, leaves round 2 unable to attribute gains, and means porting 27.6k engine lines and 34k test lines before anything is trustworthy.
- Staying TypeScript-only behind a measure-first gate: the run owner's point is that the port cost grows with every feature, and Codex CLI, Turborepo and Biome show the path is proven. because 
- Sofar runs on every tool call of every agent session. A native core gives near-zero hook start-up, fast folds over very large records (this repo's own is 9.7 MB), a single binary with no Node dependency for Codex/Cursor/OpenCode users, and headroom for many parallel agents.
- Measured 0.31 hooks are only ~0.75% of session time today, so the win is future scale and distribution rather than today's tokens. That is why it goes incrementally, and must pass the same benchmark gate as any other change.
- A conformance-first strangler port means neither implementation can drift from the SPEC.
- 2026-09-15T16:11:33.105Z — rule: **Hot-path Rust code reproduces the JavaScript text semantics pinned in docs/HOTPATH.md §Text-semantics pins, never Rust defaults; a parity difference is fixed in Rust or ruled by a Decision, never absorbed by loosening the conformance suite.** — chose **Byte parity is defined against the TypeScript engine's JavaScript text semantics, inventoried in docs/HOTPATH.md §Text-semantics pins: lengths and clips in UTF-16 code units, JS non-unicode regex classes, JS whitespace class, ECMAScript number formatting in canonical JSON, Node path.relative/basename, Math.round half-up, millisecond ISO timestamps. docs/HOTPATH.md is the hand-written contract inventory the conformance suite (1.2) drives; SPEC gaps it lists (G1–G15) are resolved by SPEC edits or Decisions, never by letting the two implementations differ.** over Rust-native defaults (char/byte lengths, Unicode-aware regex classes, ryu number formatting) with SPEC amended to tolerate the drift between implementations. because The 10k injection cap, every clip budget, the redaction and guard regexes and the canonical event bytes all decide observable output; a Rust core that used its own defaults would pass no golden test against the live 9.4 MB record and would fork events.jsonl bytes on identical events (the spurious-diff hazard SPEC §Event envelope exists to prevent). Reproducing JS semantics is cheap and mechanical; tolerating drift would make "same bytes from both implementations" (D1) unprovable.
- 2026-09-15T16:40:37.019Z — rule: **Before writing Rust, consult the current stable Rust docs and best practices, prefer newer features that improve the core, and record every improvement idea in this record.** — chose **Rust work (Phase 2 onward) checks the CURRENT stable Rust release notes, std/crate documentation and community best practices before and while integrating, and adopts newer language or library features where they make sofar-core measurably better (smaller, faster, safer, simpler). Any improvement, idiom or best practice found that is not applied immediately is captured in this record as a note (sofar_add_note) or repo memory (sofar_remember), not left in a session's context. Ruled by the run owner 2026-09-15 during rust-core 1.2.** over Porting from memory of an older Rust edition and treating the port as a mechanical transcription with no improvement pass. because The run owner wants the Rust core to benefit from the newest stable features and idioms rather than reproduce TypeScript structure verbatim, and wants improvement ideas kept in the initiative record so no session loses them. Byte parity (D1, D2) still bounds every change: an improvement may change how the bytes are produced, never which bytes.
- 2026-09-15T16:46:47.721Z — chose **The conformance suite (1.2) masks volatile bytes BY SHAPE instead of injecting a clock: a run-minted ulid becomes <ULID> only when its time part falls in the run's window, a run-minted ISO timestamp becomes <TS>, the two relative-age labels (Nm/Nh/Nd ago, ~Nh/~Nd since) become <AGO>, scratch paths become <ROOT>/<HOME>. Fixture bytes are never masked. Goldens are recorded from the TypeScript reference only, and only after the fixture horizon (newest fixture event + the 1 h cold-resume gap) so the presence of time-gated lines cannot flip. Everything else is pinned by building the child's environment from nothing but PATH (scratch HOME, git identity, XDG dirs, peer registry, update cache, LANG, TZ). Chosen under autonomy while the run owner's O6 ruling is outstanding; a SOFAR_NOW override can still be added later and would only shrink the mask list.** over Adding a test-only SOFAR_NOW clock override (plus a ulid seed) to the hot path so goldens compare unmasked, or leaving time-dependent surfaces out of the suite. because Masking needs no hot-path code change in either implementation, keeps the shape of every volatile field asserted (a malformed ulid or timestamp still fails), and ulid randomness would defeat an unseeded clock override anyway. Leaving session-start's time-gated lines out would drop the surface the 10k cap and preface composition depend on.
- 2026-09-15T18:33:19.261Z — rule: **A Rust perf claim is made only with `npm run perf` numbers (SOFAR_CONFORMANCE_BIN, same machine as a fresh reference run), never with in-process or hand-timed figures; the 3.3 gate is every cell's p50 and p95 at or under the recorded target.** — chose **The perf target for the Rust core is the checked-in TypeScript baseline (packages/engine/test/conformance/perf/baseline.typescript.json), recorded by the 1.3 runner with the same black-box method the conformance suite uses (one process per hook, spawn to exit, p50/p95 nearest rank over 20 spawns) across 10/100/1,000 initiatives × 1/10 MB bound logs, this repo's record and a no-record floor. The 3.3 gate (SOFAR_PERF_GATE=1) passes only when every candidate p50 AND p95 is ≤ the target in every cell, candidate and reference re-measured on the same machine in the same sitting.** over In-process micro-benchmarks of fold/render (would not count node boot, the part Rust removes, and cannot run against a binary); a single aggregate score (would let a 10 MB regression hide behind a 1 MB win); comparing against numbers recorded on another machine. because The user pays per spawn, so the measurement must include process start and be runnable against any implementation binary; per-cell p50 and p95 is the smallest set that catches both a median regression and a tail regression at each scale; hardware differences dwarf the margins being gated.
- 2026-09-16T06:20:58.635Z — rule: **Hot-path sorts compare UTF-16 code units; never localeCompare, Intl, or a Unicode collation, in either implementation.** — chose **O1 ruled: BOTH implementations sort by UTF-16 code-unit order (plain `<`/`>` compare), replacing ICU `localeCompare` wherever the hot path sorts paths or slugs; the TypeScript change lands with re-recorded conformance goldens and the pin is added to docs/HOTPATH.md §Text-semantics pins.** over Keeping ICU collation and vendoring a root collation table into the Rust core. because ICU or a vendored collation is a multi-MB dependency in a binary whose whole point is a ~1 ms start; code-unit order is already what the fold uses for ids, and the only visible change is mixed-case/punctuated path ordering. Ruled by the run owner 2026-09-16.
- 2026-09-16T06:21:03.713Z — chose **O2 ruled: the Rust statusline reads the update cache and renders the segment only; the refresh claim-and-spawn (`node cli.js update-check --refresh`) stays with the TypeScript surfaces (status, init, doctor) and is removed from the statusline contract the Rust core must reproduce. O3 ruled: Rust keeps `git config user.email` as a subprocess for identity, for parity with TypeScript; a file-read implementation is a later optimisation that needs its own Decision and a conformance case for git's precedence and includes.** over O2: the Rust binary spawning node for the refresh (needs node on PATH and the npm layout inside a native binary). O3: reading git config files directly now. because A native binary should not depend on locating the node runtime it exists to replace; the refresh runs at most daily and has human-frequency homes. Identity parity first keeps the 2.5 conformance surface unchanged; the ~8 ms it costs is measured (1.3) and can be attacked once parity is proven. Ruled by the run owner 2026-09-16.
- 2026-09-16T06:21:09.111Z — chose **O4 ruled: the Rust core owns plain `sofar status` (byte-identical to renderFullStatus); styled output (cli/ui/layout.ts, text.ts) is not ported — the dispatcher sends a TTY `status` to TypeScript. O5 ruled: `commit-trailer` is OUT of the Rust core's scope; it stays TypeScript.** over Porting the styled layout grammar and the commit-trailer into Rust in Phase 2. because Neither is a hook on the tool-use critical path: styled status is human-frequency and its rules are specified by behaviour, not bytes (HOTPATH G11); commit-trailer runs once per commit. Both would widen the parity surface without touching the latency the initiative exists to remove. Ruled by the run owner 2026-09-16. Conformance cases tagged O4/O5 stay skipped for a candidate.
- 2026-09-16T06:21:19.131Z — rule: **Runtime crates in the hook binary are exactly serde, serde_json, ulid, lexopt; adding one needs a Decision citing this one. Deferred and declined items are not re-proposed without new measurements.** — chose **The nine Rust research notes' verdicts are adopted as filed (run owner, 2026-09-16). ADOPT: toolchain 1.98.1 pinned in rust-toolchain.toml (never 1.98.0), edition 2024 (let chains, cfg_select!, assert_matches!, format_into), a synchronous one-shot hook binary with no tokio, release profile lto=fat/codegen-units=1/panic=abort/strip with split debuginfo kept, static musl on Linux, an env switch between cores from the first integration, dev-only crates insta, proptest, cargo-fuzz, cargo-nextest, gungraun, hyperfine. ADOPT WITH CONDITION: a sidecar `events.jsonl.lock` protocol both TypeScript and Rust implement (O_EXCL create + stale-lock recovery) plus an idempotency key in fold; a resolver shim rather than absolute binary paths in committed settings; a fold checkpoint validated by prefix hash + line count + last-line hash, any mismatch → full refold; schema codegen via ts-json-schema-generator pinned to TS 5.9/6 with a typify spike and a narrow emitter as fallback. This amends D1's runtime crate list to: serde, serde_json, ulid, lexopt. DEFER: WASM single core, tantivy, rmcp, rkyv, memmap2, mimalloc, PGO, WASI plugins, win32-arm64. DECLINE: sonic-rs; a hash-chained log (needs a per-session or Merkle redesign and a FORMAT Decision first).** over Re-litigating each item in 2.1, or adopting the deferred items speculatively. because The peer research verified each item against current stable Rust (D3) and named the real pain where it exists; the run owner accepted the filed verdicts, so 2.1 can start with a fixed toolchain and crate list.
- 2026-09-16T06:21:24.670Z — rule: **The Phase 3.3 perf target is the TypeScript baseline recorded from a build that includes r1-fixes 2.7; the "0.32.0 as-shipped" numbers are history, never the gate.** — chose **The double fold on appending hooks found by 1.3 is fixed in TypeScript on the r1-fixes branch (r1-fixes 2.7, D17), NOT in this record, and the rust-core perf target is the FIXED TypeScript: the 1.3 numbers recorded at a79c4a7 are kept labelled "0.32.0 as-shipped", and 1.3 is re-run against the r1-fixes build so the Phase 3.3 gate targets the fixed baseline. Conformance goldens must stay byte-identical across the fix.** over Fixing it in the rust-core worktree; leaving the as-shipped numbers as the Rust target. because Round 1 runs the pinned 0.32.0 install so no benchmark is contaminated; the fix is a real hot-path win for every TypeScript user now and round 2 measures the r1-fixes RC as its own predicted-gain row; TypeScript hot-path edits belong on r1-fixes, which is already editing that code; and comparing Rust against a TypeScript baseline carrying a known bug would let Rust bank a win that is really a TypeScript fix. Ruled by the run owner 2026-09-16, concurring with the r1-fixes session.
- 2026-09-16T06:24:57.897Z — rule: **A conformance or perf target names its TypeScript commit; a re-record states the sha and a reason per changed golden, and keeps the previous set alongside.** — chose **Both parity targets are pinned to a NAMED COMMIT of the TypeScript engine, never a branch tip: the perf baseline records the sha of the build it measured (`commit` in baseline.typescript.json; a45ea21 on r1-fixes for the first fixed target) and the conformance goldens, when re-recorded, carry a manifest naming the sha they were recorded from plus a one-line reason per golden that changed. The target moves only deliberately, by a Decision; the final target is the r1-fixes RC commit round 2 runs (bench-refresh D20). The 0.32.0 as-shipped goldens are kept alongside the re-recorded set so the RC's intentional digest changes stay auditable, exactly as the as-shipped perf baseline is.** over Recording against whatever the r1-fixes tip is when a session happens to run; letting a re-record replace goldens with no per-file reason. because The r1-fixes tip changed digest bytes in 16 of 27 goldens for reasons unrelated to 2.7; a target that follows a moving branch would let an unintended TypeScript behaviour change be absorbed silently as "new expected", which D2 forbids. Proposed by the r1-fixes session (sofar-4c) 2026-09-16 and adopted here; the golden manifest is built at the re-record that follows the rebase onto r1-fixes.
- 2026-09-16T06:38:07.812Z — rule: **No perf claim below ±20% without an interleaved A/B (n ≥ 25, same record and session) and the load average noted; the Phase 3.3 gate re-records the reference in the same sitting as the candidate.** — chose **Standing perf method while round 1 (or any benchmark) occupies this machine: every latency claim is an INTERLEAVED A/B (ABAB, order alternating per iteration), n ≥ 25, same record and same session id for both sides, with the 1-minute load average recorded in the perf JSON. Single-order A-then-B runs are indicative only. The 1.3 runner gains SOFAR_PERF_AB_BIN (a comparator spawned interleaved with every measured spawn, stats in `ab`) and a `load` header; the read paths on the real record use r1-fixes' shared harness `npm run bench:read-paths` (r1-fixes D18) so both records report the same table.** over Trusting the single-order 1.3 numbers at the 10 ms scale; a rust-core-only interleaving harness that r1-fixes cannot run. because r1-fixes could not reproduce rust-core's +11/+12 ms read-path drift when measuring interleaved (session-start +0.5, user-prompt +3.4); round 1 runs under launchd on this machine, so load drifts between an A run and a later B run at exactly that scale. Proposed by the r1-fixes session (sofar-4c) 2026-09-16, adopted here. Large single-order effects (the −33 to −39% write-path gains) stand but are re-confirmed interleaved once.
- 2026-09-16T07:08:58.194Z — rule: **A lone-surrogate JSON escape decodes to U+FFFD in the Rust core; every other JSON.parse/JSON.stringify behaviour (P5) is reproduced exactly and proven by fixture tests generated from Node.** — chose **The Rust core's JSON layer (crates/sofar-core/src/json.rs) reproduces JSON.parse/JSON.stringify semantics (P5: ECMAScript number formatting, last-wins duplicate keys, integer-like keys first in property order, f64 for every number, non-finite → null) with ONE ruled divergence: a lone-surrogate `\uD800`–`\uDFFF` escape decodes to U+FFFD at parse time, because Rust strings are UTF-8 and cannot hold a lone surrogate. UTF-16 lengths and stdout bytes are identical to Node's (which also emits U+FFFD when writing a lone surrogate); the only observable difference is a lone surrogate RE-SERIALIZED to JSON by the Rust core (`�` raw instead of `\ud800`), which can happen only when hook stdin or a log line carries one and the core re-emits it into an index file, a hookSpecificOutput line, or a payload it mints.** over A WTF-8 string newtype threaded through the fold, digest and every text helper so lone surrogates survive a round trip byte-for-byte; or serde_json::Value (rejects lone surrogates outright, u64/i64 number model, BTreeMap key order). because D2 lets a parity difference be ruled rather than absorbed; the WTF-8 type would cost every string operation in the core for an input no real writer produces (JSON.stringify emits `\ud800` only when a JS string already held a lone half). The conformance case that carries one (repo.append step 12) goes through the TypeScript-owned `event append` and renders through `status` with the same U+FFFD bytes. Revisit with a WTF-8 type only if a golden ever shows a re-serialized lone surrogate.
- 2026-09-16T07:40:19.674Z — rule: **The Rust fold exposes incremental fold(state, events) and a snapshot versioned by schema hash + engine version that lives outside the record and refolds fully on any mismatch; the order-independence and snapshot+tail parity properties are one suite shared with r1-fixes and run against both implementations.** — chose **Adopted (run owner via peer sofar-4c, 2026-09-16): the engine gains a PUBLIC incremental `fold(state, events) -> state` plus a versioned, cursor-carrying snapshot. TypeScript ships it first in r1-fixes (the release after 0.33.0-rc.1, not the RC); rust-core mirrors it behind the SAME contract and conformance suite. Coordination: (1) the order-independence property (1.6) and the snapshot+tail === full-fold parity property are one SHARED suite with r1-fixes, run against both implementations, and are public guarantees cited to enterprise buyers; (2) the snapshot is derived state only — versioned by schema hash plus engine version, never in the record, never committed or synced, and a version mismatch forces a full refold, never a silent stale read; (3) the Rust fold API stays shaped for since-cursor reads that never fold the whole stream (cloud's next dependency). Order unchanged: 1.4 re-pin, then 1.5/1.6, then 2.3.** over Cloud reimplementing incremental fold server-side (the drift fold-parity forbids), or two parallel property suites (one per implementation) that could diverge in what they guarantee. because The published engine exposes only total foldLines(lines), which blocks cloud's snapshot+tail ingest; a single contract and one shared property suite is the only form in which "same fold everywhere" can be cited to a buyer. The snapshot rules match D9's checkpoint condition (validated, any mismatch → full refold) so the Rust fold (2.3) inherits them from the start.
- 2026-09-16T08:28:35.122Z — rule: **sofar-core owns exactly the five hooks, `statusline` and the hidden `fold` conformance shape; any further argv shape needs a Decision citing this one.** — chose **The sofar-core binary's owned argv surface (cli.rs, HOTPATH §Entry points and dispatch) gains ONE more shape: the hidden conformance command `fold --events <jsonl> [--take <n>] [--snapshot <file> --since <n>] [--write-snapshot <file>]` (SPEC §Incremental fold — the same shape as the TypeScript `sofar fold`), dispatched only when sofar-core is invoked directly; the TypeScript shim never routes it. Its output is canonical JSON per D22 and exit 2 on a usage error.** over Keeping the binary to the five hooks plus statusline and proving fold parity only in-process (Rust tests reading the goldens); or a separate test-only binary. because D22 names ONE shared black-box suite (fold-parity.test.ts) as the parity proof both implementations cite, and it drives `<SOFAR_CONFORMANCE_BIN> fold …` — so the property names snapshot-plus-tail, order-independence, version-mismatch-refolds and pure-of-clock-and-env can only be claimed for a binary that owns that shape. It is hidden in TypeScript too, adds no operator surface, and a second binary would prove a different build than the one shipped. r1-fixes ea777c8 is the pinned TypeScript; it is merged here as 0572b3f (same parent a088a8c, byte-identical tree outside .sofar/, the amended commit of ea777c8).
- 2026-09-16T11:01:30.397Z — rule: **sofar-core owns exactly the hook shapes cli/fast.ts's SUBCOMMANDS list (six as of r1-fixes 4077c9a), `statusline` and the hidden `fold`; any further argv shape needs a Decision citing D15 and this one.** — chose **D15's owned surface reads as "the hook shapes cli/fast.ts owns" rather than a fixed count: since r1-fixes 2.5 (self-improve 1.2) the shim routes SIX hooks — session-start, post-tool, post-tool-failure, user-prompt, stop, session-end — plus statusline and the hidden fold; cli.rs dispatches `event post-tool-failure` as owned now (handler arrives with 2.5) and HOTPATH §Entry points and dispatch lists it.** over Keeping cli.rs at five hooks until 2.5 and letting `post-tool-failure` fall through to the TypeScript CLI as "not ours". because The dispatcher mirrors runFast's SUBCOMMANDS exactly (HOTPATH §Entry points and dispatch); a shape the shim owns but the binary hands back would make 3.1's boot-stub dispatch route a hook to a binary that refuses it. The count in D15's rule was a description of the surface at the time, not the law — the law is "what the shim owns, and nothing else"; this restates it so a future hook added on r1-fixes is a merge, not a Decision.
- 2026-09-16T11:31:42.754Z — chose **The sofar-core binary's owned argv surface gains plain `status [slug] [--root D] [--no-color] [--color]` (D14 made concrete, cli.rs + status_cli.rs): the binary applies the stdout colour ladder of cli/ui/caps.ts itself (NO_COLOR > --no-color > FORCE_COLOR > --color > TTY-and-TERM≠dumb; the ambient CI clause dropped for a pipe) and exits 64 for a render that would be styled, so the TypeScript layout grammar stays the only styled renderer; `--watch`, a second positional or any other option is commander's. Record resolution (branch → bindings.json → quick lane, containment, existence) is ported now with the TypeScript error text verbatim (resolve.rs), ahead of 2.5. The stderr update notice (`withUpdateNotice`) is NOT rendered yet: it needs the update-cache reader and CURRENT_VERSION comparison the statusline segment (2.6) brings, and no conformance case reaches it (the scratch state dir holds no cache).** over Leaving `status` to the 3.1 shim's decision (the shim cannot know the colour ladder outcome without the binary reporting it) or routing every `status` through TypeScript until 2.6. because D14 rules the core owns the uncapped agent-readable bytes; repo.status (9 steps: bound record, explicit slugs, closed record, --no-color, --root, unknown slug, escaping slug) now passes black-box against target/release/sofar-core, which is the cited proof 2.4 needs and the shape 3.1 dispatches to. Exiting 64 on a styled render keeps a single renderer per surface (D14) without a second implementation of the layout grammar.
- 2026-09-16T11:31:55.342Z — chose **The templates and both status renders are proved by a render-parity golden suite (packages/engine/test/conformance/render-parity, rust-core 2.4): one golden per fixture initiative (83 over the records and synthetic fixtures), sections framed `== <name> (<bytes> bytes) ==` holding renderFullStatus, five renderStatus variants (plain, hook-shaped with session/git/adjacency/repo-memory/notices, lane, activity-off, and a cap variant whose oversized notices push EVERY record through enforceStatusLimit — some cuts landing inside a surrogate pair), plan.md, decisions.md, memory.md and every sessions/<id>.md; the digest options are embedded as the golden's first section so both sides render from identical inputs. Both runs are in-process (vitest imports the templates; cargo test folds the same events.jsonl and compares), goldens name their TypeScript commit in MANIFEST.md (D11), and a lone surrogate is stored as the U+FFFD Node would write (P1).** over Proving the digest only through the black-box session-start cases (which need 2.5 and cover 5 records, never the cap on most), or a hidden `render` command in the binary (a new owned shape needing its own Decision like D15, for no proof the in-process run does not give). because Byte identity is the 2.4 acceptance and the template surface is wide (55 real initiatives, 255 sessions, every budget and cap path); 988 surfaces byte-identical on the first Rust run is the evidence, and the black-box repo.status case plus the 2.5 session-start cases remain the command-line proofs. The suite costs 6.6 MB of committed goldens, accepted for the same reason the three conformance golden sets are.
- 2026-09-16T12:05:42.114Z — chose **The conformance harness gains per-step routing (SOFAR_CONFORMANCE_CANDIDATE_STEPS=<regex>, packages/engine/test/conformance/harness.ts): with a candidate named, only the steps whose argv matches run on it and every other step — including the concurrent-append writers — runs on the TypeScript reference in the SAME scratch root, the golden comparison staying whole. 2.5 is proved hook by hook this way: session-start is green on all 23 cases with '^(event session-start|status)( |$)' while the reference drives post-tool, user-prompt, stop and session-end, and the two implementations share one record, one index and one diagnostics store mid-case. The mode is development-only; the 3.3 gate is the unfiltered run.** over Landing all six hooks before any black-box proof (weeks of port with no golden feedback), or filtering steps OUT of the goldens (which changes the record state later steps see and the bytes they render). because Every case interleaves hooks, so a half-ported binary can pass none of them whole; routing per step keeps every golden byte in force and additionally exercises mixed writers — TypeScript-written cursors and marks read by Rust and back — which is exactly what the 3.1 fallback world will do. It cost 20 lines of harness and no golden moved.
- 2026-09-16T12:19:48.596Z — chose **Two std-only approximations in the user-prompt hook, each bounded and named: (1) the lessons line's word class — TypeScript's Unicode regex `[\p{L}\p{N}][\p{L}\p{N}_.-]*` — is Rust's `char::is_alphabetic() || is_numeric()` (lexicon.rs), which differs from `\p{L}\p{N}` only on Other_Alphabetic combining marks and letter-numbers, i.e. which marks bind to a word in some non-Latin scripts; every conformance prompt and record folds identically. (2) Peer liveness — `process.kill(pid, 0)` — is `/proc/<pid>` on Linux and ONE `ps -p <pids> -o pid=` elsewhere (peers.rs), run only when a sibling must be named (a conflict or a landed push), so a quiet prompt still spawns nothing as commit-attribution D6 pins; the divergence is a pid owned by another user (TypeScript says dead on EPERM, `ps` says alive).** over A vendored Unicode general-category table for exact `\p{L}` (tens of KB in a binary whose point is a millisecond start) and a `libc` crate or an `unsafe extern` block for `kill(pid, 0)` (D9 fixes the crate list; the workspace forbids unsafe). because Both differences are unreachable by any fixture and by every record this repo has produced, both stay inside the budget lines they feed, and each is one function that a later Decision can replace with the exact form if a golden ever shows the gap; D2 asks that a difference be fixed or ruled, and this rules it. Proof of the whole surface: all 23 conformance cases pass with every hook and `status` routed to target/release/sofar-core (D29 mixed mode), the reference driving only `event append` and the statusline.
- 2026-09-16T12:56:06.569Z — rule: **The boot stub interprets exactly one core exit code, 64, as "TypeScript runs it"; the core never reads stdin or writes a byte before returning 64, and a new dispatched shape or a second interpreted exit needs a Decision citing this one.** — chose **The boot stub (cli/boot.ts, rust-core 3.1) dispatches by exit code, not by re-parsing argv: every `event`, `statusline` and `status` argv is spawned on the native core with stdio inherited and `SOFAR_CORE_DISPATCHED=1`; the core's exit 64 (NotOurs, or a styled `status`) means "run the TypeScript CLI on the same argv" — the core reads no stdin before deciding and prints no exit-64 diagnostic when dispatched, so the fallback sees whole stdin and clean streams — and every other exit is mirrored. The core is `SOFAR_CORE=<path>` (`0`/empty forbids one), else the platform package `@sofar/core-<platform>-<arch>` resolved from the stub (the one constant 3.2 may rename); absent means TypeScript silently, a named core that cannot spawn warns once and falls back, a signal-killed core exits 1 (stdin gone, no fallback). The update-refresh claim moves to the stub after a core-rendered `statusline`/`status` (cli/update-cache.ts, node builtins only, shared with update-check.ts), keeping the O2 ruling that the binary only reads the cache; `sofar-core status` renders the stderr notice with the stderr caps ladder. Proof: the reference conformance suite with `SOFAR_CORE=$PWD/target/release/sofar-core` — 27/27 cases, no tag skipped, `event append`, styled `status` and `commit-trailer` reaching TypeScript through the stub's own fallback — plus a fake-core dispatch test under `npm test` and a negative control (a core that always exits 1 fails 25/27). The hook shims keep `exec sofar event <hook>`: a direct exec needs a stable per-machine binary path, which is the 3.2 install layout's to settle.** over The stub re-implementing the argv grammar (a third copy of parseRoot/dispatch) or the colour ladder to decide the styled-status case itself; capturing the core's stderr to hide the exit-64 diagnostics (changes the child's stderr TTY-ness, so a human at a terminal would get a plain notice where TypeScript colours it); the core claiming and spawning the refresh itself (O2, rejected); rewriting the shims to exec the binary now with an install path guessed ahead of 3.2. because Exit 64 is already the contract HOTPATH pins for "not mine" and D14 chose it precisely because the shim cannot know the colour ladder outcome; one env marker keeps both diagnostics for a human running sofar-core directly while the stub's fallback is the answer under dispatch. Sharing update-cache.ts between the stub and update-check.ts keeps one refresh policy, and the stub's cost is a file read only after a core-rendered statusline or status. The unfiltered run is the standalone 3.1 proof the plan asked for and is the shape the 3.3 gate and the 4.1 arm will use.
- 2026-09-16T13:43:20.310Z — rule: **Platform packages are named `sofar-core-<platform>-<arch>`, generated only by packaging/npm/emit.mjs, versioned in lockstep with sofar.sh, and published before sofar.sh; hook shims carry routing to `sofar-core` and nothing else.** — chose **The native core ships as unscoped npm platform packages `sofar-core-<platform>-<arch>` (darwin arm64/x64, linux x64/arm64, win32 x64), each holding the binary and nothing else — no bin, no scripts — generated by packaging/npm/emit.mjs (the single source of the platform list, the names and the version lockstep; `--check` in CI and the packaging test) and declared as optionalDependencies of sofar.sh at sofar.sh's exact version; boot.ts's CORE_PACKAGE (now cli/core.ts, shared with `sofar doctor`'s new "hot path:" finding) resolves that name, replacing D31's provisional `@sofar/core-*`. The binary reaches PATH through sofar.sh itself: `bin/sofar-core` is a JavaScript shim equal to `sofar` that postinstall (install.mjs) replaces with the platform package's binary on non-Windows, silently leaving the shim when there is no package or scripts are off. The hook shims route before they exec — `sofar-core event <hook>` when `command -v` finds one (SOFAR_CORE=0 forces the CLI, SOFAR_CORE=<path> names a core), else `sofar event <hook>` — so a hook on the core is one exec of native code with no node in front. CI gains a five-runner `core` matrix (fmt, clippy -D warnings, test, release build, artifact upload) and a `core-conformance` job running the unfiltered mixed-install suite, fold-parity, render-parity, `cargo xtask schema --check` and `emit.mjs --check`; publishing (platform packages first, then sofar.sh) stays the human step per repo memory M1. Verified: npm 11 resolves and installs sofar.sh with the five packages unpublished (lockfile records them `optional: true`, install succeeds, the shim answers); the packaging test installs the locally staged platform package next to sofar.sh in a temp prefix and proves the postinstall replacement byte-identical, `sofar-core status` by PATH name, the init-installed shim exec'ing it, and `sofar status` through the stub and with SOFAR_CORE=0 rendering the same bytes.** over The `@sofar/…` scope (the npm token in this environment is invalid, E401, so scope ownership could not be checked; the only published package is unscoped sofar.sh); a `bin` entry on each platform package (npm links only the top-level package's bins on a global install, so it would not reach PATH); symlinking instead of copying in postinstall (a pruned or swapped platform package would leave a dead link on PATH — worse than the shim); a postinstall download of the binary (network at install time, and BD1's "the tarball is the product boundary"); baking a machine path into the committed .claude/hooks shims (shared by every clone); running CI publishes with an NPM_TOKEN (M1: the user publishes, OTP). because esbuild's layout (optional platform packages + a postinstall that swaps a shim for the binary) is the proven shape for exactly this problem and degrades to "no core" on every failure path, which is the 3.1 fallback world already proved byte-identical; unscoped names are publishable by the existing sofar.sh owner without an org; routing in the shim is not behaviour (BD4 — the CLI still owns every byte) and is the only way a hook avoids node's boot, which HOTPATH measured at ~32 ms of every hook and which D5 says only `npm run perf` may quantify.
- 2026-09-16T16:25:56.953Z — chose **append.rs carries the one-fold-per-process cache of r1-fixes 2.7 (D17), mirrored exactly: fold_state keys a retained FoldCheckpoint by the log's size and mtime (any write this process did not make misses and refolds; a bounded eight entries), finalizes a clone on every hit, and append_and_project advances the checkpoint by the line it just wrote only when the log now measures exactly cached + line + newline and append_to_checkpoint accepts it, dropping the entry on any doubt. fold_state also stops going through the snapshot machinery (no prefix sha256 over the whole log for a hook that never persists a snapshot). The 3.3 gate then passes: every cell's p50 and p95 at or under the reference re-recorded in the same sitting (D12), p50 ratios 0.06×–0.64×, filed as perf/gate.sofar-core.065e5f7.{md,json} beside baseline.typescript.{md,json} (re-recorded at 065e5f7; the 179b8fd set kept alongside per D11).** over Leaving the appending hooks folding two to four times (registration check, handler state, post-append projections) and looking for the loss in the JSON parser or the projections; or a Rust-native memo keyed by content hash. because The first gated run (perf/gate.sofar-core.065e5f7.before-fold-cache.md) failed nine 10 MB measures — post-tool Edit 1.17× p50 / 1.50× p95, session-end ~1.0×, cold session-start p95 — while every read-only hook was 0.5×–0.7×, which is the signature of extra folds, not a slow fold; TypeScript had removed the same double fold in r1-fixes 2.7 and D1 requires the same behaviour in both implementations. After the cache every 10 MB append measure sits at ~0.55×, well outside D12's ±20% band even at load 8.5; conformance stays byte-identical (27/27 unfiltered through the stub, 24/24 direct on owned shapes).
- 2026-09-16T18:32:41.803Z — chose **1.6 lands inside the shared fold-parity suite rather than as a suite of its own (D14: one suite, both implementations): case FP-11-session-lifecycle-out-of-order joins FP-01…FP-10 (a write-back filed before its registration in file order, a mechanical event with an id below its session_started, a close with an id below its registration; three seeds, a monotonic tail) and the public property `fold-parity/union-merge` (D21 name) proves the merge law on bytes and a command line: a case's head committed to a git repository with `sofar init`'s `.sofar/**/events.jsonl merge=union` attribute, its tail dealt round-robin to three branches merged back in turn, merges without conflict, the merged file is the exact union of every branch's lines, and its fold equals the case golden's state whatever order the union driver chose; an across-initiatives form merges branches that touched different records plus one that touched both. Golden recorded through the reference at this tree; green on the reference (39/39), on target/release/sofar-core black-box (39/39) and in-process in cargo (the union-merge form is black-box only — it needs git, and drives whichever binary SOFAR_CONFORMANCE_BIN names). The merged-log fold time is printed by the suite as a measurement, never asserted and never a claim (D5).** over A separate proptest-driven Rust suite (a new dev-dependency and a second corpus the TypeScript side would not share), or asserting the fold time. because The order-independence property already existed as three seeded shuffles per case on both implementations (r1-fixes 5.1), so 1.6's remaining substance was the lifecycle-order case and the git-merge law; putting both in the shared suite keeps one corpus, one runner and one set of public names for procurement to cite, and the Rust in-process runner picks up the new case with no code beyond its count.
- 2026-09-17T08:57:47.799Z — chose **The conformance harness is made portable and the goldens re-recorded once for it (D11: at rust-core 6af34c4, reason in golden/MANIFEST.md, previous set kept as golden-4077c9a-symlinked-tmp/): `scratch()` returns the PHYSICAL tmpdir (realpathSync) so `<ROOT>` is the path the child's process.cwd() reports on every platform, and every byte count in a record-delta header is computed over the masked text. The first Linux CI run (draft PR #2) exposed that macOS's /var → /private/var symlink had put every `<ROOT>`-substituted hook path outside the child's physical root, so the goldens held the absolute-path branch of relative(root, path) — a branch Linux never takes — and counts that embedded the recording machine's tmpdir length; 16 goldens changed in exactly those bytes and nothing else. Alongside: the concurrent-append case gets a 120 s timeout (a hosted runner needs tens of seconds for 100 node spawns, and a mid-run timeout leaves children whose cwd the cleanup deletes — the uv_cwd ENOENT in the log), and lock.rs treats Windows' delete-pending PermissionDenied on create_new as contention (retry to the deadline) under cfg(windows) only, which is what made the Windows lock test see overlapping sections.** over Masking or dropping the byte counts (a wrong count would no longer fail); recording Linux-specific goldens; passing --root explicitly to every step (hides the cwd path the shims really use); treating PermissionDenied as contention on Unix too (a 2 s stall on an unwritable index dir where today the hook runs unlocked at once). because Every golden must mean the same bytes on every machine the suite runs on (D1, D11) and CI is now one of those machines; the relative-path branch is the one users hit, so the old goldens were proving the wrong branch; counting over masked text keeps the count a real assertion while removing the only machine-specific input. Both fixes are harness- or platform-only: no hot-path byte moved (27/27 on the reference under both tmpdir layouts and 27/27 through the core).
- 2026-09-17T10:18:05.463Z — chose **memory-lead 1.4's mirror lands as one Rust pass over the wave-a merge (17817db): rule_fidelity.rs from SPEC §Rule fidelity; DecisionState.quote with payload validation (non-empty, ≤300 UTF-16 units, requires rule) and the generated schema regenerated; status.rs rewritten to memory-lead D4's block/yield composition (the next task first, memory / repo memory / decision index / last session yielding by precedence under the 6,000-unit cap, constraints last ranked by lexicon relevance with the operator's quote, minutiae heads, the protected end) plus D3's Session line; session_pointer.rs (every hook writes `.sofar/.index/session.json`, session-end clears its own); host.rs (Cursor dialect both ways, the host's tool on registrations and diagnostics rows); repo.md stub stripping; and the unbound `sofar status` orientation (L10) handed back with exit 64 rather than ported — it needs `sofar list`, which the core does not own, and the stub's fallback renders it byte-identically. Goldens re-recorded once at 17817db with the reason per set (D11): 17 conformance, 94/94 render-parity; previous sets kept. Proof: render-parity 94/94 in-process on the first run, conformance 27/27 through the stub, fold-parity 39/39, npm test 2510, cargo 120 + clippy -D warnings.** over Porting `sofar list` to render the unbound orientation in Rust (a full-CLI surface, D14/D15 own only the plain status bytes); mirroring the Cursor dialect only on the tool attribution (D1 wants the same bytes for the same stdin, and a Cursor payload through the core would otherwise print Claude-shaped output Cursor ignores); re-recording the digest goldens per contract (D3 then D4) instead of once after both. because Exit 64 is already the contract for "the TypeScript CLI runs it" (D31) and the unbound case is a human-frequency orientation, not a hook; the dialect port is 160 lines that keep D1 honest for Cursor hosts before rc.2 ships them; one re-record after the merge spends one D11 entry per set instead of three and the first-run 94/94 shows the port is verbatim.
- 2026-09-17T10:43:40.737Z — chose **The conformance goldens are runtime-neutral: the harness masks V8's JSON.parse position suffix (` (line N column M)`, added in Node 22) down to the `in JSON at position N` part, so the same golden asserts the same bytes on Node 20 and Node 24; the CLI's error text contract is untouched (it prints whatever its runtime says, and the Rust core keeps reproducing the Node 24 form its D13 fixtures pin). CI's engine suite becomes a Node 20 + 24 matrix (the declared floor is >=18), core-conformance runs on Node 24 (the runtime the core's message fixtures name), read-paths stays on 20. Replaces the aa0061c blanket move of every job to Node 24, which sofar-4c held as a support regression.** over Pinning every CI job to Node 24 (drops coverage near the supported floor and would carry into main); asserting only the error code rather than the message (loses the message contract the golden was recording); recording per-runtime golden sets. because Only two goldens carried the suffix and only on TypeScript-only `event append` steps; a shape mask is the suite's existing tool for bytes a run cannot help minting differently (D4's ulid/timestamp masks), and it keeps the rest of the message asserted. The matrix keeps the floor honest at the cost of one more runner.
- 2026-09-21T18:25:11.408Z — chose **Fix a cost that exists only in the Rust port (the TypeScript reference has no counterpart) in the core directly, as its own commit with its own ablation arm. First case: record_guard_violations cloned each decision's guard and rule on every file and command event (~31M String allocations on team100, 56% of the replay after FileIndex). The fix borrows instead (8bad310). A cost both implementations share still lands in TypeScript first and is then mirrored (D1), as 1.5 turn 1 did (r1-fixes 4.5, then 21ea3e8).** over (a) waiting for a TypeScript change first, when TypeScript has nothing to change; (b) folding the fix into the FileIndex mirror commit, which would credit two levers' gains to one (r1-fixes D5) because D1 governs behaviour: SPEC plus the conformance suite, passing on both implementations. It does not require a TypeScript change for an allocation pattern that exists only in the port. Fold output stays byte-identical on 120 logs, including 36 real guard crossings. Kept separate, the arms are 2,702 -> 1,625 ms for FileIndex and then -> 1,225 ms for the borrow on the team100 bound log.
- 2026-09-22T14:29:04.212Z — rule: **Merge main into rust-core as trunk moves and mirror each hot-path change against that trunk commit; never batch mirrors for an RC re-pin.** — chose **rust-core tracks trunk continuously. Merge main into rust-core as trunk moves, at least whenever a hot-path commit lands, and mirror each hot-path change with conformance parity against the trunk commit that carried it before the next merge. Goldens are re-recorded against that named trunk commit (D11: sha, one reason per changed golden, previous set kept). Mirrors are no longer batched for an RC re-pin. PR #2 is ready for a merge decision only when conformance, fold parity and render parity are green on the trunk commit last merged.** over Batching the mirrors for the next RC re-pin (the rc.3 plan in notes 01M341T4 / 01M343EX). No rc.3 was scheduled, and by 2026-09-22 the branch was 131 commits behind main with six named hot-path features unmirrored. because Operator direction, 2026-09-22. Main's hooks run the Rust core wherever its binary is installed (the boot stub has no version gate, rust-core 3.1), so an unmirrored trunk feature is silently missing for every mixed install the moment PR #2 merges. The port cost also grows with every batched commit. D11's rule stands: every target still names its TypeScript commit. Only the cadence changes, from RC tags to trunk.
- 2026-09-22T14:29:04.231Z — chose **Renumber this branch's fold-parity case FP-11-session-lifecycle-out-of-order (rust-core 1.6) to FP-12-session-lifecycle-out-of-order. Trunk's FP-11-run-adoption-fencing (drive-visibility 2.2, be4ce88) keeps FP-11. This is the Decision r1-fixes D21 requires: old name FP-11-session-lifecycle-out-of-order, new name FP-12-session-lifecycle-out-of-order. Case, sidecar, fold golden and render golden were renamed with bytes unchanged apart from the id (merge 9614860). The seeds [31, 32, 33] stay.** over (a) Renaming trunk's FP-11-run-adoption-fencing: it is published on main, and drive-visibility D8 and its 2.2 note say never rename it. (b) Two cases sharing the FP-11 prefix: ids must be unique, and the Rust suite counts cases. because Both branches minted FP-11 independently, and the merge of main 72146d9 is the first time they met. This branch's case was never on main, so renaming it breaks no published name. Trunk's case is the one other implementations and SPEC readers already cite.
- 2026-09-23T15:14:39.225Z — rule: **A persisted fold snapshot lives only in the per-clone state dir ($XDG_STATE_HOME/sofar/<cloneKey>/), never under .sofar/; a hook output cache is keyed by the log's size and mtime plus every other input that hook reads, and any unkeyed input is a bug.** — chose **4.4 split, operator ruling relayed by sofar-e8 on 2026-09-23. (B) statusline and user-prompt get per-hook OUTPUT caches. Key: the bound log's (size, mtime) plus every other input the hook reads, enumerated exactly. The TypeScript reference comes first, then the Rust mirror with conformance parity. The cache is derived and lives in .sofar/.index/, the same derived, gitignored home as the told set and the lexicon. It is not a snapshot, so D20 does not govern it. (A) The other hooks (session-start, stop, post-tool) resume from a PERSISTED fold snapshot plus tail: the D14 snapshot through the existing serialize/parse and fold-file-since path, whose refusals mean a full refold. WHERE IT LIVES, reading D14 ("outside the record") and r1-fixes D20 ("never part of the record, never committed, exported or synced"; SPEC: "never written under .sofar/ by the engine"): the per-clone state dir of core/state-dir.ts, $XDG_STATE_HOME/sofar/<cloneKey>/folds/, keyed by the clone's real path. That is the home sync-client D2 and self-improve D3 already use, and it refuses any path that resolves inside the clone. No new rule and no guard-rail touched: no new crate (serde_json is in D9), no server, no sync. C (a warm process) is not chosen.** over (A) placed in .sofar/.index/, which is gitignored but under .sofar/, so SPEC's snapshot law forbids it. (B) keyed on the log alone, which goes stale when git, the clock or another record changes. One mechanism for all five hooks: A alone cannot reach 20 ms, because parse and finalize grow with the log, and B alone cannot serve an appending hook, whose output changes with every write. (C) a warm process, not chosen. because The 4.4 profile (note 01M37CPW, under load, relative only) puts ~80% of every hook in replaying and finalizing the whole log. Only skipping the fold entirely (B) can reach 20 ms, and it is safe only where a hook's inputs are narrow and can be enumerated. Appending hooks must fold anyway, so a persisted snapshot turns their replay into applying the tail. Placing it in the existing per-clone state dir satisfies D14 and D20 literally, using a home and a containment check that already exist, instead of minting a new one.
- 2026-09-23T15:26:50.931Z — chose **4.4 step 1: homeInitiative's per-log registration lookup reads a DERIVED registration cache, .sofar/.index/registrations/<slug>.json, maintained by reading only the log's tail. TypeScript first, then the Rust mirror with parity. For every session, the cache holds exactly the answer registrationIn gives: the first line in file order that parses as an object with type session_started, a string session, id and ts, and whose raw text contains that session id (registrationIn's own pre-filter, kept so the answer is identical). It covers complete lines only, and the unterminated last line is always scanned live. The cache is trusted only when the log is unchanged (size and mtimeMs) or has grown past the consumed offset with its last consumed line hashing the same (the check D22's chain-hash snapshots use). Anything else rebuilds from a full scan. registrationIn itself stays the reference, and the scan remains the fallback.** over Keeping the full-log scan per call. At team100 it reads and decodes 95 MB on every hook and statusline call: 143 ms of a 143 ms cached statusline, relative and under load. Also over a written session-to-slug index, which record-integrity D1 rejected and which this is not. because record-integrity D1 rejected a second store WRITTEN at registration, which could desync, needed pruning and meant committing routing state under .sofar/. It also judged the scan cheap at ~350 KB of total logs. This cache is none of the rejected things. It is derived only from the log, and every read re-checks it against the log (so it cannot desync silently). It is gitignored under .sofar/.index like the index tiers (so no Record-layout change). And it is disposable (lost or corrupt means a rescan). The log is still the only truth, as D1 requires. What changed is D1's cost premise: at team100 the scan is the whole remaining cost of the cached statusline and of every hook's resolution.
- 2026-09-23T15:45:36.904Z — chose **Supersedes the A half of D34 (event 01M37DAH, the 4.4 B+A split). Operator ruling relayed by sofar-e8 on 2026-09-23: drop A from 4.4. B stays for statusline and user-prompt as D34 set it, and extends to session-start's digest inputs: session-start first, then user-prompt. The appending hooks (post-tool, stop) are outside 4.4's read-time target and keep folding in-process. D34's placement rule for any future persisted snapshot stands: only in the per-clone state dir, never under .sofar/. The edge-free hook checkpoint is recorded as an unbuilt D3 idea.** over D34's A half: a persisted D22 snapshot plus tail for the appending hooks and for B's misses. because Measured at team100, 2026-09-23, relative and under load (note 01M37F1K). The team-bound snapshot is 264,764,498 bytes. 250 MB of it is 1,078,393 adjacency edges; the state is 14 MB. Rust resumes from it in 2.6-2.8 s against 2.0 s for a full refold, and TypeScript takes 1.25-1.84 s. A persisted snapshot at this scale costs more to parse than the log costs to replay. The variant that would help (fold edges into finalize's accumulators as they are produced, ~25 MB) is a refactor of the fold core in both implementations, for an estimated ~1 s to ~400 ms on appending hooks. That is not 4.4's target.
- 2026-09-23T15:47:52.402Z — chose **4.4 B for session-start is a FACTS-level cache, not an output cache. Cache a trimmed "digest state" per record in .sofar/.index/digest/<slug>.json. It holds the finalized InitiativeState with every unbounded collection cut to exactly the members renderStatus and its helpers can reach: sessions (last with summary, last unwritten with activity, the unwritten list, open sessions, overlapping write-backs), with files_touched dropped. The key is the log's size + mtimeMs + engine + schema, and a miss folds and rewrites. Every live input (session id, git, repo memory, neighbours, rules, notices, env switches) stays live and is passed to renderStatus unchanged. The cut rule is enforced by a parity suite, not by argument: renderStatus(digestState(s), o) must equal renderStatus(s, o) byte for byte over every real log in this repo, the team100 corpus and the status fixtures, across a matrix of options. It runs in both implementations, TS first then the Rust mirror. The file-states index read (refresh_file_states, ~25% of session-start at team100) is the next cost after that. It belongs to record-index's index format, so it is named here and not changed.** over An output cache keyed by every input: it can never hit, because the output carries `Session: <id>`, session-start fires once per session, and the notices carry clock-derived ages. Also over caching the full edge-free state: 14 MB at team100 is ~140 ms to parse in the core. because Session-start profile at team100 (2026-09-23, under load, relative): fold 316 of 523 samples, refresh_file_states 128 (index JSON parse 80), render_status 40 (open_session_file_conflicts 33), attribution 11. The options (repo memory, neighbours, rules, notices) take part in renderStatus's cap arithmetic, so the state part cannot be rendered once and composed. The only exact route that skips the fold is a smaller state that provably renders the same, and "provably" here means a byte-parity suite over real and synthetic records.
- 2026-09-23T16:45:52.238Z — chose **Merge rust-core into main before the next release cut (operator, 2026-09-23), so the cut carries D33 (fdlibmLog, bit-stable BM25 scores across Node builds). That satisfies P3 of memory-lead's round-3 pre-registration.** over Pinning one Node version for round 3 instead; cutting from main without rust-core because Round 3's pin must carry D33, or lesson rankings differ by Node build. Merging is cheaper and fixes it for every user, not only for the benchmark.
