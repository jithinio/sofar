<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: session-strategy-bench

- 2026-07-19T14:16:42.073Z — chose **Pre-registered falsifiable predictions (P1 cost-climb-vs-flat, P2 quality-parity, P3 arm L wins verbatim-recall-from-early-session — deliberately the opponent's territory, P4 latency-growth) frozen in product doc/session-strategy-bench-evidence.md at initiative creation, before any run** over Post-hoc analysis of whatever the data shows, or predictions written/softened after seeing run 1 because The benchmark must be able to lose — P3 registers a case where the long session is expected to WIN, so a clean sweep for arm S would itself be suspect. Falsified predictions get recorded as falsified; marketing copy derives only from the honest-summary paragraph.
- 2026-07-19T14:16:46.816Z — chose **Measure-only jurisdiction: read-side only, zero changes to engine or injection content; any finding that suggests an engine change is logged as a proposal in this record, never implemented here** over Letting benchmark findings drive engine/injection tweaks in-flight within this initiative because Measuring and optimizing in one initiative contaminates both: the numbers stop being comparable across runs and the changes ship without their own acceptance criteria. Injection byte-stability is a SPEC invariant; this bench depends on the measured surface staying frozen.
- 2026-07-19T14:16:53.221Z — chose **Reuse the handoff-bench machinery: harness/rubric base at ~/IO/handoff-bench, evidence file alongside R04's in product doc/ (sofar-bench format), dual blind graders with >=1 cross-family, "blind LLM-graded, cross-family" label only when both graders ran, and minimum 2 workload types (feature + refactor) before any headline claim** over Building a new bench, single-family (Anthropic-only) grading, or headline claims from one workload because R04's judge-provenance mitigations (cross-family re-grade + founder spot-check, both cleared 2026-07-11) set the credibility bar; a second scenario inherits it by reusing the same machinery instead of re-earning it. Single-workload results are an anecdote, not a claim.
- 2026-07-19T14:44:14.418Z — chose **Arm L runs in a worktree with sofar wiring stripped (sofar uninit --purge; scenario-1 arm-A doctoring precedent); both arms share a frozen TASKS.md committed into the snapshot so task prompts stay byte-identical; arm S's initiative plan is seeded from TASKS.md** over Running arm L with sofar wired (its Stop hook exits 2 on any turn without session_ended, forcing per-task write-backs), or arm-specific prompt wording pointing arm S at the record because The Stop gate makes "session never ends" impossible for a wired arm L and would contaminate the treatment: L must be the pure long-session strategy (context as memory), S the pure sofar pattern (record as memory). TASKS.md as the shared task source is the only way to keep prompts byte-identical while each arm keeps its own memory channel.
- 2026-07-19T15:05:39.039Z — chose **Bench isolation via a dedicated clone (~/IO/brillo-bench @ a6e0c8d2, origin remote removed) with user-named roadmap features as workloads: W1 Meeting Minutes &amp; Summary, W3 AI Chat (optional third); sync-health W1 retired unrun; W2 retargeted at the clone. Feature tasks enforce a no-live-API rule (deterministic reference impls behind provider-agnostic interfaces; acceptance = typecheck + unit tests)** over Running bench worktrees inside the live brillo checkout (prod app), keeping the invented sync-health workload, or letting workload acceptance depend on live model/OAuth calls the bench copy has no credentials for because User direction 2026-07-19: brillo is live prod — duplicate it and bench in the copy. Removing the origin remote makes a push-back to prod impossible by construction; user-named features are real roadmap work (better workload validity than an invented feature); retiring before any run keeps the freeze discipline intact (no prediction touched).
- 2026-07-20T06:10:55.697Z — chose **Publish P1 as SPLIT (arm-S-flat falsified on the feature workload, held on the refactor), P3 as NOT TESTED (probe design failed — both arms imported the constant instead of recalling it), P4 as PARTIALLY SUPPORTED (large erratic latency penalty, not monotonic prefix growth); record the arm-L wall-clock win prominently** over Rewording P1 to "climbs less steeply" as if that had been predicted, quietly dropping the failed probe, reporting only the cost win that favors the product, or treating the workload-type split as noise to be averaged away because The pre-registration exists precisely so a losing prediction gets published as losing; the benchmark had to be able to lose (D1). Arm S won cost on both workloads but lost wall-clock on both — reporting only the former would be marketing, not evidence. Workload-type variance is a finding (scenario-1 precedent), and a probe that fails to discriminate is a methodology result worth more than a fabricated P3 verdict.
- 2026-07-20T07:58:07.723Z — chose **Report P2 as SUPPORTED on the grounds that the arm gap (2 of 108) is smaller than the inter-grader gap, and publish the direction flip explicitly (Claude: S better by 1; GLM: L better by 2)** over Reporting the worse-of-two total (L 20 vs S 22) as "the long session produces better work", or reporting the same-family grader alone (S 19 vs L 20) as "the record pattern produces better work" because Either single-grader reading would be a real number used to tell a false story — the gap is not robust to grader choice, which is exactly what the cross-family requirement exists to expose. Quality parity is the defensible claim, and it is what makes the 17.7% cost saving meaningful rather than a discount for worse output.
- 2026-07-20T09:39:07.540Z — chose **S2-R02 P3' probe ships as a NEW task 7 in both workloads; tasks 1-6 stay byte-identical to R01 (specs, prompts, snapshots unchanged). P1' is evaluated on tasks 1-6 totals — directly comparable to R01 — and the arm-S R01-vs-R02 delta table covers tasks 1-6 only; task 7 is priced and reported separately and excluded from every curve and total.** over Replacing task 6 with the redesigned probe as the brief literally specified because Replacing task 6 would change that task's spec in both workloads, so neither task 6 nor the workload totals would be comparable to R01 — and the workload total is exactly what P1' (wall-clock gap at most half of R01's +26%/+48%) is measured on. The probe's job is to test P3', not to cost the run its comparability spine. Appending it keeps every R01 number a like-for-like referent at the price of 4 extra task runs (~$18). User-sanctioned 2026-07-20.
- 2026-07-20T09:39:20.878Z — chose **R02 arm S runs the PUBLISHED @alignlabs/sofar 0.10.0 in DEFAULT config — stdio MCP as `sofar init` registers it, `sofar serve` NOT running, nudge and drift-gated Stop active as shipped. The T3 HTTP daemon's benefit may not be claimed from this run. Version note frozen: R01 arm S = 0.9.2 (published 2026-07-13, latest at R01 time), R02 arm S = 0.10.0 (published 2026-07-20T09:24Z).** over Running arm S against the daemon to show the T3 speedup, or leaving the R01 arm-S engine version unrecorded because The daemon is opt-in: a default-config user does not get it, so a number produced with it running would not describe the shipped product. Version-delta audit over 183e4db..f067915 (0.9.2 to 0.10.0) found exactly four changed source files — cli/event.ts (T1 Stop gate), cli/serve.ts (T3 HTTP MCP), core/fold.ts (T4 task_files), projections/templates/status.ts (T4 files: line) — with src/mcp/**, packages/schema/** and src/hooks/*.sh untouched, so the 7-tool schema surface is unchanged and NOTHING rode along beyond the speed initiative. A rendered-block diff on the real R01-era W1 arm-S record (truncated to task 4 active) differs by exactly one line: the T4 files: hint. R02's delta is therefore attributable to the speed batch alone.
- 2026-07-20T09:39:28.580Z — chose **Three graders, two roles. PRIMARY = Grader A claude-opus-4-8 + Grader B GLM 5.2 via OpenCode, worse-of-two, unchanged from R01 — this pair alone adjudicates P2' and carries the R01-to-R02 quality delta. ROBUSTNESS = Grader C gpt-5.5 via `codex exec` under an ISOLATED CODEX_HOME (auth.json symlinked; no user hooks, skills, memories or rules), empty working dir, read-only sandbox — reported in its own table, never folded into the primary rule. Label: "blind LLM-graded, cross-family x2".** over Folding all three graders into one worse-of-three or majority rule, or dropping Grader C because two graders already earned the R01 label because R01's primary pair IS the comparability instrument — re-weighting the rule while also changing the engine would confound the quality delta with a grading-protocol change, and there would be no way to tell which moved. A third outside-family grader is worth having (R01's headline finding was that the arm gap is smaller than the inter-grader gap, which one extra family can corroborate or break) but only as a disclosed second opinion. If C's ordering disagrees with the primary pair that is published as a finding, not as grounds to re-weight after data. Access verified before the arms run: codex exec + gpt-5.5 returns clean output from an isolated home; it bills the ChatGPT subscription, so no marginal API spend.
- 2026-07-20T09:39:38.045Z — chose **P5' fabricated-path sidecar runs POST-HOC by replay, never inside the measured run: for each task N and arm, a scratch worktree is checked out at that arm's task-(N-1) product commit with a copy of the arm's .sofar truncated to the matching event boundary, and one orientation-only session ("which files would you open for this task and why") runs there and ends. Main task prompts are untouched; sidecar cost is extracted separately and EXCLUDED from every cost/time curve and total.** over Running the orientation probe live in the arm worktree before each task, or appending the question to the task prompt because A live probe in the arm-S worktree would append session_started events to the very record under measurement and shift its "Last session" surface; a prompt change would break the byte-identical-prompt integrity rule that the whole R01 comparison rests on. Replay reconstructs the exact orientation state each arm faced with zero contamination. Disclosed limitation: for arm L the sidecar is a COLD-session proxy, not L's operating condition (a warm long session already knows its files), so the arm-L fabricated-path rate is a floor-comparison number, not L's in-run behavior.
- 2026-07-20T09:42:15.445Z — chose **R02 replicates R01's arm-setup procedure EXACTLY — arm S gets the live ~/IO/brillo/.claude copied in (hooks, settings.local.json, and its two skills: shadcn, migrate-radix-to-base), arm L gets none of it after `sofar uninit --purge` — and the skills asymmetry is named as a disclosed confound in R02's caveats AND flagged as retroactively applying to R01, whose caveat 7 named only the record-keeping obligation.** over Equalizing the arms in R02 by giving arm L a .claude/ carrying the same skills minus sofar wiring, or leaving the asymmetry unmentioned as R01 did because Equalizing would be the better experiment but it would make R02 non-comparable to R01, and the R01-vs-R02 arm-S delta is this run's headline artifact — changing two variables at once destroys it. Leaving it unmentioned is not an option: arm S could reach a shadcn skill on W1's UI task that arm L could not, so "arm S = the sofar pattern" overstates what the treatment actually was in both runs. Naming it costs nothing and is owed to R01's published numbers too. Equalized arms are the right design for a future R03.
