<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: speed-2

- 2026-08-03T10:22:33.462Z — chose **Split the CLI into a 545-byte dist/cli.js stub + 76KB dist/fast.js (hooks, statusline) + 986KB dist/full.js, with the stub calling module.enableCompileCache() before either import. fast.ts owns only the exact argv shapes the shims emit and returns false for anything else, falling through to the full CLI.** over Lazy dynamic imports inside the existing single bundle (esbuild inlines them back, so nothing is saved), or a second hand-written hook implementation (two sources of truth for hook behavior). because Measured: hooks were compiling 986KB (MCP SDK, chokidar, commander, cloud, doctor) to append one line. Hot path 56-64ms to 26-28ms; the slow path also improved 60ms to 47ms because the stub makes the compile cache actually apply. Falling through on unrecognized shapes keeps commander the single owner of the command surface, so --help, --version and every error message are byte-identical.
- 2026-08-03T10:32:57.763Z — chose **Skip projection writes whose bytes already match disk (read + compare before writeFileAtomic), leaving the render unconditional.** over Regenerating only the session file the append touched (true O(1)), or caching rendered output in memory across appends. because Measured on a 25-session initiative: rendering every session costs 0.03ms, writing every session costs 3.2ms — the render is 100x cheaper than the write, so caching renders optimizes the wrong half. Targeted regeneration would need to infer which session files an event can change, but renderSession takes the whole InitiativeState, so that inference is unsound. Content-compare is provably output-identical — the file holds the same bytes either way — so no reader can observe it, and it needs no knowledge of which event touched what.
- 2026-08-03T10:47:24.457Z — chose **T5a only: the MCP protocol block now tells agents NOT to call sofar_get_state at session start (the SessionStart hook already injected renderStatus with strictly more options), while keeping sofar_start_session mandatory. AGENTS.md dialect untouched.** over T5b — also dropping sofar_start_session to save a second round trip. because get_state digest is renderStatus(state) and the hook is renderStatus(state, {repoMemory, sessionId, git}) — same projection, superset options, verified empirically as zero digest lines absent from the injected block, so the call can only return less. But start_session's load-bearing effect is ctx.session.set(), not its event: without an active session resolveWriteInitiative falls back to the branch binding (which moves mid-session — it moved in this repo today) and appendAndProject stamps envelope.session "cli", detaching decisions and tasks from the session and breaking the Stop write-back linkage. That is the record-integrity misroute class, and its side-index workaround is already rejected. MCP-less AGENTS.md tools get no hook injection, so their orient-first step stays.
- 2026-08-03T13:56:54.623Z — chose **Refresh an installed protocol block ONLY when it byte-matches a block sofar itself previously shipped (SHIPPED_PROTOCOL_BLOCKS ledger, append-only). Anything else is customized and is reported but never rewritten. doctor grades the same three states via a shared classifyProtocolBlock; upgrade's success message points at sofar init.** over Always refreshing between the markers (clobbers user edits), never refreshing and only warning in doctor (leaves every repo stranded on an old protocol), or versioning the marker itself (cannot identify blocks already installed under the old marker). because The block is sofar's only lever on agent behaviour, so an un-updatable block means the product cannot evolve its core mechanism in the field — T5a was the first casualty. A byte-match proves a previous sofar wrote those bytes and nobody edited them, which is exactly the condition where replacing loses nothing, so the safe and useful cases are separated without guessing. Cheapest possible moment: first publish was 2026-07-30, so almost no customized blocks exist yet. Verified against this repo, whose CLAUDE.md carries a local ORDER MATTERS clause and is correctly refused. The ledger must stay append-only and byte-exact or repos still on an old block silently fall back to "customized".
