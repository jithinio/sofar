<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: initiative-lifecycle

Cite these in .sofar/repo.md by qualified handle — `initiative-lifecycle M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-08-05T15:07:49.280Z) — Back-compat probes run against a PUBLISHED build, not the working tree: `npm init -y && npm install sofar.sh@<version>` in a scratch dir, then `sofar init && sofar new` a throwaway git repo and drive `./node_modules/.bin/sofar` against it. The MCP surface is drivable the same way — pipe initialize + notifications/initialized + tools/call JSON-RPC lines into `sofar mcp` on stdin. Hooks are drivable by piping their JSON payload into `.claude/hooks/<name>.sh`. This is how task-drop-state 1.1 and initiative-lifecycle 1.1 were verified; a working-tree test cannot answer what an OLDER engine does with a NEWER log.
