<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: plan-carry-forward

Goal: Close the hole SPEC left in its own rule: §Forward compatibility of plan_updated says an unrecognised status is coerced to pending AND WARNED ABOUT, while an OMITTED status reaches the same destination in silence. RULED 2026-08-13 as (e), D1: extend the fold's existing coerce-and-warn to omission, scoped to entries PRESENT in the payload whose previous status was resolved. Diagnostic, never preventive — state is untouched, so every historical log folds byte-identically, and because 1.1 measured the omission shape at ZERO across 85 plan_updated events the change is provably silent on all existing history. Renames are ruled out of scope with reasons (D2); the 34 recent phase closes need nothing (D3). Measured, not assumed: the original premise that this bug had fired did NOT survive Phase 1, and the record carries both retractions by name.

Progress: 10/10 tasks done (100%)

## Phase 1 — Establish the exposure (blocks the ruling) [done] — 3/3 done

- [x] 1.1 MEASURE it retroactively before designing anything, the repo's refute-before-building discipline. Replay every initiative's log and find every plan_updated that took a phase or task from a resolved status (done/dropped/blocked) back to pending or omitted. Publish the count, name the records and the events. If it is ZERO, the hazard is theoretical and the ruling changes shape — say so rather than building for it. MEASURE ON THE PAYLOAD, never on folded state: omitted-versus-explicit is the whole discriminator and folded state cannot see it.
- [x] 1.2 Establish whether the hole is MCP-only or dialect-wide. `sofar event append --type plan_updated` bypasses any tool-layer merge entirely, and the MCP-less convention dialect uses exactly that path (phase-lifecycle D1). Read cli/event.ts's append against mcp/update-plan.ts and state which callers a tool-boundary fix would and would not cover.
- [x] 1.3 Separate the two candidate defects. Does SPEC promise full-replace to CALLERS, and do the tool description and docs tell an agent it must restate statuses? A contract nobody is told about is a documentation defect, not a missing merge, and it has a much cheaper fix.

## Phase 2 — The ruling [done] — 3/3 done

> All three ruled: D1 the mechanism (e), D2 renames out of scope with reasons, D3 the existing closes need nothing. The scoping in D1 is what makes it shippable — 1.1's zero means the warning is provably silent on every byte of existing history.

- [x] 2.1 DECIDE the mechanism, against a measured incident count of ZERO — so do-nothing is a serious answer and the burden is on any option that adds code. Weigh a tool-boundary merge, a fold-level merge, a boundary warning, documentation only, and extending the fold's existing coerce-and-warn. Log the decision citing task-drop-state D2 and phase-lifecycle M2.
- [x] 2.2 DECIDE what protects the closes already written, if 2.1 leaves any window open. 34 phase closes across 16 STILL-ACTIVE records are the concrete thing this initiative was opened over. Answer it explicitly rather than by assumption.
- [x] 2.3 DECIDE what happens across a RENAME, at its real size: the mechanism is real and its occurrence count is ZERO. Weigh the narrow residual against a mechanism that would have to guess identity, which the payload cannot support. Deciding to do NOTHING is legitimate; it must be a decision rather than an oversight.

## Phase 3 — Build the warning (D1) [done] — 4/4 done

> Built, tested, and proven silent on every existing record: patched-build doctor output is byte-identical to unmodified 0.28.0 across all 41. Suite 1678 passing, typecheck clean, no schema change needed.

- [x] 3.1 core/fold.ts, plan_updated case: when a phase or task is PRESENT in the payload with no `status` key AND its previous status was resolved (done/dropped/blocked), push a fold warning naming the line, the subject and what the status was. State is NOT touched — the entry still becomes pending, exactly as today, so the log folds byte-identically (D1's rule). Entries ABSENT from the payload get nothing (D2). Mirror the wording of the existing unrecognised-status warning, since this is the same rule finishing its sentence.
- [x] 3.2 Tests: an omitted status on a previously-DONE task warns; on a previously-PENDING task does not (a fresh plan legitimately omits statuses and must stay silent); an explicit `pending` on a previously-done entry does NOT warn, because that is authorship and the discriminator is key presence, not value; an entry absent from the payload does not warn (D2); folded STATE is byte-identical before and after the change over a fixture with all four shapes.
- [x] 3.3 Prove the retroactive-silence claim rather than asserting it: fold all 41 existing records on the built engine and confirm the new warning fires ZERO times, which is what 1.1 predicts and what makes this change safe to ship. If it fires anywhere, 1.1 was wrong and the ruling reopens.
- [x] 3.4 docs/SPEC.md is authoritative: extend §Forward compatibility of plan_updated so the omitted case sits beside the unrecognised one and the asymmetry is gone, plus §Acceptance criteria. Also fix what 1.3 found on the caller side — the sofar_update_plan entry and the tool description say nothing about status at all, and an omitted status meaning `pending` rather than `unchanged` is exactly what a caller needs told.

Next action: Decide whether doctor should render more than the first fold warning per record — the Phase 3 review found this signal is a bare count in any record that already has warnings, which is a doctor question predating D1 and affecting every fold warning equally.
