<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: plan-carry-forward

- 2026-08-13T15:44:47.477Z — rule: **Never let this initiative change folded STATE. The deliverable is a warning; state stays exactly what the payload says, so every historical log folds byte-identically before and after. And never widen the warning to entries ABSENT from the payload — that is the rename/delete case, it cannot be told from a deliberate deletion, and 1.1 measured ~52 historical instances that would become permanent doctor noise.** — chose **(e) Extend the fold's EXISTING coerce-and-warn rule to cover an omitted status — warn when a plan_updated omits the status key on a phase or task that IS present in the payload and whose previous status was resolved. Scoped deliberately to that case ALONE. Ruled by the user 2026-08-13.** over (a) carry-forward at the tool boundary, which prevents rather than reports but covers only MCP callers and misses `sofar event append --type plan_updated` — the MCP-less dialect's only path and the one adopt.ts prints for migrations (1.2). (b) merging in the fold, which is the one option that changes how existing logs fold. (c) a warning at the write boundary, strictly worse than (e) on coverage for the same reason as (a). (d) documentation only, which leaves the failure silent if it ever fires. (e)+(a) together, two mechanisms to keep in step forever for a bug with zero occurrences. because SPEC already ruled this hazard and (e) finishes the job rather than opening a new one: §Forward compatibility of plan_updated says an unrecognised status is coerced to pending AND WARNED ABOUT, on the stated rationale that a reader should over-report remaining work rather than quietly claim something was resolved. An OMITTED status reaches the same destination in silence. That asymmetry is an oversight in task-drop-state D2, which was reasoning about a stale reader meeting a newer engine's status and never considered the payload simply not carrying one. The fold is downstream of both writers and of import and sync, so it covers every dialect by construction, including the Tier 3 path 1.2 found exposed — and fold warnings surface in doctor, which those sessions do run. THE SCOPING IS THE LOAD-BEARING PART, and it comes straight from 1.1: fold warnings ACCUMULATE ACROSS REPLAY and are rendered by doctor forever, so a warning that also fired on entries ABSENT from the payload would emit roughly 52 permanent warnings across harness-build, drift-certification, commit-attribution and token-optimization for restructures that were deliberate — wallpaper that would teach everyone to ignore the axis. 1.1 measured the omission shape at ZERO across 85 plan_updated events, so scoped this way the change is provably silent on every byte of existing history and can only ever fire on the bug itself. Accepted cost, stated: this is diagnostic, not preventive. It reports a dropped status; it does not keep it.
- 2026-08-13T15:44:58.906Z — chose **2.3 — do NOTHING about renames, as a decision rather than an oversight. A resolved phase or task whose name or id simply leaves the plan gets no warning, no carried status, and no identity affordance.** over Adding stable phase ids to the payload so status can survive a rename (a schema change, and schema lives only in packages/schema/src), an explicit rename affordance on the tool, or widening D1's warning to cover any resolved entry leaving the plan. because The payload genuinely cannot support it: a rename and a delete-plus-add are byte-identical, so any mechanism would have to GUESS identity, and guessing wrong silently re-attaches a done status to work that was never done — strictly worse than the silence it replaces. The occurrence count is zero in the shape that implicates the default: 1.1 found all 54 displacements inside fully-explicit payloads, 52 of them entries vanishing by name, and re-verification confirmed the motivating example (commit-attribution Phase 3) was a deliberate reopen to an explicit `active`, which the default cannot produce. Widening the warning is separately ruled out by D1's scoping: it would emit roughly 52 permanent doctor warnings for deliberate restructures. The honest residual is narrow and is recorded rather than fixed — a fully-explicit payload proves the author wrote statuses deliberately for the entries they INCLUDED, not that they noticed one leaving. If that ever bites, it will bite visibly as a phase reading pending that someone remembers finishing, and this decision is where they should start.
- 2026-08-13T15:45:06.408Z — chose **2.2 — the 34 phase closes phase-lifecycle 4.3 wrote across 16 still-active records need NOTHING beyond D1. No migration, no protective re-statement, no audit pass.** over A protective sweep that re-asserts every recent phase close, or a doctor axis that watches those specific records for regression. because The question was posed when the exposure was assumed and has to be answered against what 1.1 actually measured: zero occurrences of the omission shape in 85 plan_updated events across 41 records and roughly fourteen months. Nothing has ever been lost this way, so there is nothing to repair, and a protective sweep would append 34 events to defend against an event that has never happened once. D1 makes any future occurrence visible in doctor at the moment it folds, which is the proportionate answer. Recorded explicitly rather than dropped, because 2.2 was written as a separate question precisely so it would not be assumed answered by the mechanism ruling.
