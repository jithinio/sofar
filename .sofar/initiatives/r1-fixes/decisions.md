<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: r1-fixes

- 2026-09-15T15:39:56.392Z — chose **1.1: gate the SessionStart unbound notice on .sofar/ existing (not on ≥1 initiative); add a zero-initiative variant (new --goal → start_session with id → update_plan) and the status block's Session line to both variants, via a shared sessionIdLine()** over (a) auto-creating a default initiative at first SessionStart; (b) keeping the slug gate and relying on the unknown_initiative error's `sofar new` hint; (c) putting the hint in the CLAUDE.md protocol block because Round-1 Claude S1 injected 0 chars and spent turns probing, then minted a 2nd session id beside the hook's. (a) is lazy binding, which initiative-lifecycle D4 rejects and would name the record before the agent knows the project; (b) costs the probe turns the fix exists to remove and still gives no id; (c) is paid every session in every repo, while the notice renders only when nothing resolves. Output stays <700 chars and appends nothing (record-hygiene D2).
- 2026-09-15T15:47:17.930Z — rule: **Register sessions only through ToolContext.registerSession — never call appendAndProject with session_started directly.** — chose **1.2: one idempotent ToolContext.registerSession used by all three registration paths (PostToolUse hook, start_session unknown-id, CLI append session_started), double-checked: unlocked fold fast path, then fresh fold + append under an O_EXCL lock in .sofar/.index/locks/ keyed per (slug, session), degrading to unlocked after 2s** over (a) fold-side silent dedupe only (lines still land in the committed log); (b) lock-free wx claim marker per session (a crash between claim and append strands the session unregistered, and racing events can land before the start); (c) locking every append (appends are already O_APPEND-atomic; would tax the hot path); (d) flock via a native dep (CLAUDE.md dependency rail); (e) fixing only the hook race (the CLI dialect re-running start had no check at all) because Repro on 0.32.0: 12 parallel post-tool processes wrote 12 session_started; fixed: 1 at N=12 (5 runs) and N=40 (226ms wall, 0 locks left). Registered sessions never touch the lock, so the per-event hot path is unchanged. Holding the lock across the append also orders racing events after their registration. Degrade-not-block keeps BD22: the worst case is the duplicate the fold already skips. Per-log dedupe cannot move a home: registeredAt reads a log's FIRST start, so record-integrity D9 is unaffected.
- 2026-09-15T16:01:41.418Z — rule: **Never write an envelope source outside SOURCES — map unknown agent names to cli; widening the enum needs its own Decision and a reader-first release.** — chose **1.3 --source: accept any agent name on `event append`, recording names outside the envelope enum as `cli` (toSource, the start_session mapping) with the real name in session_started.tool; the enum stays closed (mixed-version reader policy, SPEC §Event envelope)** over (a) adding cursor/others to SOURCES; (b) validating source as any slug-shaped string; (c) keeping the refusal and listing valid values in the AGENTS block because envelope.ts validates source against SOURCES on every read, and 0.32.0's fold skips an unknown-source line as corrupt, so (a)/(b) would make new writers' events vanish for pinned or older readers of a shared record (round 1 runs 0.32.0). (c) keeps the failed append + retry. The mapping loses no identity readers use: sessions render tool from session_started. Answers the audit's 'arbitrary --source needs a mixed-version reader policy' blocker.
- 2026-09-15T16:01:47.888Z — chose **1.3 payload reference: EVENT_TYPE_REFERENCE in packages/schema/src/events.ts (writer, via, summary, field grammar interpolating the enum constants, a validating example), printed by `sofar event types [type] [--json]`; the AGENTS block shows plan_updated + phase_status_changed inline, adds --goal and 'one initiative per project or roadmap', and points to `sofar event types` for the rest** over (a) the whole reference inline in AGENTS.md (paid every session, ~3k more bytes); (b) a hand-written docs page; (c) generating fields from validator code (validators are imperative, no introspectable shape); (d) the reference in the engine package because The block is paid per session, the reference only when needed: the block grows 4,660→4,800 bytes in this repo (+1 plan example, phase line, pointer). Schema guard-rail puts payload shapes only in packages/schema; tests pin every example to validatePayload, every validator-required key to `fields`, every block payload to the schema, and every printed example to a real `event append`, so the copy cannot silently drift. Record<KnownEventType,…> makes a new event type fail to compile without an entry.
- 2026-09-15T16:03:23.839Z — rule: **Never sum overlapping predicted gains; attribute each lever's gain with an ablation arm.** — chose **Adopt bench-refresh D20 for r1-fixes (relayed by peer session sofar-05 from the main worktree's record): Phase 4 releases a candidate only (local pinned build or npm `next`), stable publish becomes 4.4 gated on round-2 D19 evidence; 2.5, 3.1 and 3.2 blocked on their audit contracts; 1.3's --source ships with the closed-enum mixed-version policy; overlapping predicted gains are attributed by ablation** over keeping the original Phase 4 (stable publish before round 2) and building 2.5/3.1/3.2 as planned because Run-owner ruling via the Codex audit codex-r1-audit-20260915-01. A stable publish before round 2 would ship unproven changes to every user. 2.5 collides with SPEC commit attribution and self-improve 1.2, 3.1 has no restart-safe contract, and 3.2's wall-clock expiry breaks deterministic replay. Summed gains from overlapping levers (fewer calls, smaller schemas, auto-capture) would double-count one saving. Not yet visible in this worktree's bench-refresh copy until merge.
- 2026-09-15T16:04:23.743Z — rule: **Never define outcome-capture payloads or storage in r1-fixes — consume self-improve 1.2's schema and private store.** — chose **r1-fixes 2.5 consumes self-improve 1.2's outcome capture (its payload schema in packages/schema/src plus its private local store) and stays blocked on self-improve 1.1/1.2** over 2.5 defining its own commit/test/error capture events in the committed log because Ruling self-improve D1 (from its audit, relayed by peer sofar-05): two owners for outcome capture would produce two schemas for one fact. Outcomes in events.jsonl would also collide with SPEC commit attribution (commits derived from git) and the record-hygiene D1 hook exemption for git/sofar commands, and would reach export and sync, which the private store never does.
- 2026-09-15T16:09:22.982Z — chose **1.4 formatter defence in two levers: (1) init writes .mcp.json and .claude/settings.json in the shape the host's own formatter would produce — indent resolved biome.json(c) json.formatter > biome formatter > prettier config (JSON forms + package.json "prettier") > .editorconfig > 2 spaces, with primitive-only arrays collapsed onto one line when they fit (Prettier and Biome both do this; verified on biome 2.5.13 and prettier 3); (2) a new doctor axis "Formatter hazards" that names Biome, Prettier and markdownlint when their config is present and .sofar is not excluded, and --fix adds the exclusion the tool itself documents: Biome v2 `!**/.sofar` in files.includes (v1 `.sofar` in files.ignore, version read from $schema then node_modules), `.sofar/` in .prettierignore, `.sofar/` in .markdownlintignore or `**/.sofar/**` in a markdownlint-cli2 `ignores`. A config carrying comments or an unknown Biome major is reported with the exact line to add and never rewritten.** over (a) ignoring .mcp.json / .claude/settings.json in the host's formatter — hides the user's own files from their formatter to fix a shape sofar controls; (b) writing 2-space JSON regardless and only fixing ignores — the round-1 diff churn on .mcp.json stays; (c) rewriting JSONC configs textually — guessing at structure can eat user config, the same reason init refuses unparseable JSON; (d) excluding .sofar by editing sofar's own output (D-P10 precedent: configure the host tool away from .sofar, never mangle the record). because Round 1 (bench-refresh preview loss row): Biome linted .sofar so the agent hand-added "!**/.sofar" to biome.json, and Biome's tab default rewrites init's 2-space .mcp.json — 3/7 runs spent a detour on it. Formatter-friendly output removes the churn without any config edit; the doctor axis removes the lint detour with one deterministic, idempotent fix per tool, mirroring the Tailwind scanner defence (D-P10, scanner-version-gate D1) including its version gate.
- 2026-09-15T16:25:53.038Z — chose **1.5 input forms: `sofar event append --payload` and `sofar remember [text]` each accept the JSON/text three ways — inline as today, `-` for stdin (the quoted-heredoc form the AGENTS block now shows: `--payload - <<'EOF' … EOF`), and `@<path>` for a file; with the value omitted and stdin piped, stdin is read. `remember --supersedes <M-handle>` records `supersedes` on memory_promoted as the QUALIFIED handle `<slug> M<n>` (a bare `M<n>` resolves against the target initiative; either form is validated to name an existing memory before the append). Fold marks the superseded memory (`superseded_by`) within its own record; memory.md renders it struck with the successor named; doctor's repo-memory axis stops nagging for a superseded memory across every record and nags for its successor instead. sofar_remember gains the same `supersedes` arg.** over (a) a separate `--payload-file`/`--text-file` flag only — one more flag to learn, and the `-`/`@file` idiom already exists on `sofar import`; (b) tolerant JSON parsing (JSON5, smart-quote repair) — hides the mangling instead of removing it and makes the log's validation weaker; (c) storing `supersedes` unqualified — repo.md and doctor cite qualified handles only (repo-memory-capture D2), an unqualified one would be ambiguous repo-wide; (d) deleting or rewriting the superseded memory — history is append-only. because Round 1's CLI agents lost turns to shell quoting: apostrophes and inner quotes inside `--payload '<json>'` and `remember "<fact>"` broke the command and produced correction events. stdin under a quoted heredoc is the one shell form that carries any byte unchanged, and the payload reference already pins every example to validate, so the fix is the transport, not the parser. Supersession lets an outdated fact be replaced without a correction event that leaves doctor nagging for both.
- 2026-09-15T16:35:23.037Z — chose **1.6 drive diagnostics: the adapter exit record (SessionExit) carries `stderr_tail` (what the agent wrote to stderr, last chunk) and `spawn_error` (ENOENT and friends), both adapters fill them; the driver renders one `describeExit` line — exit code or signal, the spawn error, the last non-empty stderr line clipped — and puts it (a) on the progress line of every unresolved launch and every stall handoff, (b) in the run_stopped note for a stall stop (`last: …`), and (c) as an optional `detail` field on the stall/threshold handoff event so the cause is durable per session, not only in the driver's stderr. The handoff REASON is still read from the fold alone (D5) — detail annotates, never decides.** over (a) progress lines only — a detached run's stderr is not the record, and the next driver resumes from the record; (b) trusting the exit code or stderr to classify the handoff — D5 forbids it, and an agent that crashed after writing back still handed off on what the record shows; (c) appending a note_added per failed launch — record hygiene: a launch that registered no session files no handoff (D3), and a note would be a second event type saying the same thing. because Round 1's stalls read `no session registered … (exit 1)` and nothing else, so a missing binary, a logged-out agent and a crashed hook all looked identical and cost the operator a transcript hunt. The adapters already buffer the stderr tail and the spawn error for their own use; carrying them onto the exit record is the cheapest path to `every launch failure names its cause`.
- 2026-09-15T16:40:48.341Z — chose **2.1 four levers, each a separate ablation arm (D5): (A) the digest ends its decisions block with one line naming the NEXT handles — `Next ids: D<n+1>, M<m+1>` — so a session can cite the decision or memory it is about to log without a fold or a get_state; (B) sofar_update_task returns bare {ok, event_id} on every status — the standing-constraint echo on `active` (drift-hardening 4.1) is dropped, superseding that lever; the constraints stay injected at SessionStart and enforced mechanically by the point-of-use guard (§Hooks); (C) sofar_end_session takes `tasks?: [{task_id, status, note?}]`, validated as a whole before any append and applied in order under the session before session_ended, returning `tasks_applied`; (D) the MCP server declares `instructions` (SDK initialize) telling a client with deferred tools to load the core five in ONE ToolSearch and to skip get_state after the hook injection; the CLAUDE.md block's DURING/BEFORE FINISHING lines say task changes may ride the write-back.** over (a) keeping the constraint echo but capping it — every echo repeats text the session already holds from SessionStart, and the guard hook is the enforcing half, so a capped echo is still pure repetition; (b) a separate sofar_batch tool — one more schema on every turn (2.4 is trimming the surface); (c) putting the next ids in tool responses only — the digest is read before the first write, which is when a session plans its citations; (d) instructions long enough to restate the protocol — the block already does, and instructions ride every initialize. because Round 1 measured sofar's share of tool calls at 32–38% per session: one update_task per status change (2–4 per task), a get_state or find to learn the next D id before citing it, a ToolSearch per deferred tool, and constraint echoes of ~600 chars per activation. Each lever removes one of those call classes without removing any information a session needs, so the prediction (share ≤20%, −3 to −5 turns per session) is attributable arm by arm.
- 2026-09-15T16:56:28.305Z — chose **2.2 digest dedupe, index-first: the two decision sections (Recent decisions ×5 at 280 chars, then a Rejected-approaches ledger of EVERY decision's `over`) become one index. (A) Recent window: `- [D<n>] <date> <chose ≤120> — over <over ≤90>` — handle first (the citation key), fields clipped separately so `over` always survives, `because` left to decisions.md (named in the header) instead of being the part the 280 clip cut anyway; a placeholder `(no alternative recorded)` renders no over clause. (B) A ruled decision whose rule rendered in Standing constraints above shows `(rule above)` and a 60-char chose — the constraint is its operative content, so the index stops restating it. (C) The ledger lists ONLY decisions older than the recent window (`Earlier rejected approaches — do NOT re-propose (N older)`), each `- [D<n>] <over ≤90>`, so no `over` text appears twice; ≤5 decisions → no ledger at all. Budgets unchanged elsewhere: rules verbatim, repo memory 1500, summary 1200, hard cap 10,000. PREDICT (bench-refresh D10): S2+ digest −25% chars with no C2/C3 loss; local proxy = this repo's 41 records with ≥2 sessions, mean 5,026 chars before. Each of A, B, C is measured on that corpus as its own arm (D5).** over (a) dropping the rejected ledger and keeping 5 × 280-char prose lines — the ledger is what carries C3 breadth once decisions age out of the window; (b) shrinking the whole line to 200 chars with the same `chose … over … — because` concatenation — the clip lands inside `chose` on every real record, so `over` and `because` vanish together; (c) a new get_state view or find output for decision detail — decisions.md is one read away on every host, and 2.4 is trimming the tool surface, not growing it; (d) cutting the write-back summary or repo memory budgets — neither duplicates anything, and the summary is the one text the previous session wrote for this one. because Measured on this repo's records: decisions + rejected ledger are 34% of the r1-fixes digest (2,410 of 7,102 chars) and up to 4,300 chars on older records; every recent line's 280-char clip lands inside `chose` (5/5 here), so the rationale the line promises is already absent while the same `over` text is paid twice. drift-certification renders at exactly 10,000 chars, so its tail — Next ids and the read-back line — is what enforceStatusLimit cuts today; a count-capped index ends that. Handles on every line make citation possible without a fold, the same reason D10 added Next ids.
- 2026-09-15T17:06:38.874Z — chose **2.3 cache-stable digest layout, ordered by volatility: (1) static head — title, Goal, Standing constraints, Repo memory, Phases; (2) record-state sections — Progress/active/current/next task, Next action, parallel/stale/notes/blocked/concurrent lines, Last session, Driven, unwritten sessions, the decision index, Next ids; (3) a volatile tail — Adjacent records, the `Session:` line, the `Git:` line, and the hook notices (recent work elsewhere, closed banner, cold-resume advisory, shipping) which move INSIDE renderStatus as a `notices` option instead of a preface; (4) Read-back and footer stay last. The rejected ledger's cap reserve now counts the tail's real length, so the protocol tail still renders on heavy records. Nothing moves to UserPromptSubmit: the per-prompt path already carries the drift nudge and the landed line, and the shipping check is two git spawns that D6 keeps off that path. Supersedes placement only — every line keeps its wording and budget: BD43 (Session line near the top), record-integrity 4.1 (Git above Goal), session-orientation 2.2 (recent-work line leads), felt-cost 2.2 (advisory precedes the block), record-index 3.3 (adjacency last in the current block), commit-attribution 3.2 (shipping notice composed around the block). Byte-stability (felt-cost 1.2) is restated precisely: for identical state and options-minus-notices, the block is byte-identical up to the notices. PREDICT (bench-refresh D10): cache-creation tokens per session −15%; local proxy = the share of the block that is byte-identical between consecutive sessions of the same record, 0.8% before (182 pairs, every one diverging at line 3).** over (a) moving the Session id out of the block into a separate hook line after it — the same bytes at the same position, one more surface to teach; (b) leaving the notices as a preface and only reordering the block — the shipping notice renders on every session of an unpushed branch and is the first byte, so the reorder would buy nothing here; (c) sending staleness, notes and the read-back through UserPromptSubmit instead — they are state-derived and cost nothing, and the per-prompt path is budgeted at 100ms (D6); (d) dropping the Session line from the digest and relying on hook-registered adoption — start_session needs the id in the agent's hands, and 1.1 just put it on the unbound notice for the same reason. because Measured on this repo's records: of 182 consecutive-session pairs, the digest shares 0.8% of its bytes — the title only — because the per-session `Session:` line is line 3 and the `Git:` sha is line 5, ahead of a goal, four verbatim rules, 1,500 chars of repo memory and the phase list that do not change between sessions. Prompt caching matches prefixes, so bytes that never change belong before bytes that change every session; the gain is only realized when the host's own prefix (system prompt, CLAUDE.md, its git status) is also identical — same-sha restarts, parallel sessions, compaction re-fires — which is the honest bound on the prediction and why the local proxy measures the block alone.
- 2026-09-15T17:15:01.325Z — chose **2.4 tool surface in two arms (D5): (A) sofar_review, sofar_close_initiative and sofar_find leave the MCP tool list — CLI-first: `sofar review` prints the packet and now ends with the exact `sofar event append --type review_recorded --payload -` heredoc that records the verdict, `sofar close` closes (it already returns the overrides), `sofar find` traverses; the server instructions name the three commands; TOOL_NAMES/TOOL_DEFS shrink to nine and the MCP modules for the three are removed (applyClose stays, the CLI uses it). (B) Every remaining description is cut to its load-bearing sentence(s): the `initiative` property (repeated on 7 tools) drops to one clause, tool and property descriptions stop restating each other, and long prose (end_session's peer protocol, update_phase, remember, log_decision's guard grammar) keeps the rule and loses the essay. PREDICT (bench-refresh D10): MCP tool definitions 15,272 chars measured today (12 tools, name + description + inputSchema serialized; the plan's 14.8k was 0.32.0 before 2.1 added `tasks`) → ≤8,000; A and B measured separately.** over (a) trimming descriptions only and keeping 12 tools — the three schemas alone are 4,876 chars, so ≤8k is unreachable without dropping them; (b) a `sofar review --record` CLI flag — the convention dialect already records any event with a validated payload, and a second write path for one event type is what 1.3's payload reference exists to avoid; (c) keeping the three as MCP tools with one-line descriptions — the inputSchema is most of their size, and hosts without deferred tools pay it every turn; (d) dropping sofar_get_state instead — it is the one read an MCP-only host has when the injected block is missing. because Codex and Cursor carry every MCP tool schema in every turn (round 1: ~3.5k tokens per turn, bench-refresh 1.4), and the three least-called tools — a review is per phase, a close is once, a find is occasional — are a third of the bytes. Each already has a CLI surface that every host can run, and Claude Code's deferred loading (D10's one-call core set) does not include them anyway. Descriptions were written as documentation; the SPEC and the payload reference hold the documentation, and the schema is paid per turn.
- 2026-09-15T17:28:26.415Z — rule: **The quick lane (`quick`) is a catch basin, never a home or a binding: hooks fall back to it only when nothing resolves, never write bindings.json for it, and a registration there never beats a bound branch.** — chose **2.6 quick-work lane = a reserved slug `quick` that resolution FALLS BACK to — never a binding, never a home. (A) resolveInitiative answers `quick` for an unbound branch when .sofar/initiatives/quick/ exists and is open; the first captured PostToolUse edit creates it (mkdir + initiative_created, session cli, source hook, under a lock) — bindings.json is never written. (B) Catch-basin rule: homeInitiative ignores a registration in `quick` whenever a real slug is preferred, so a session that started in the lane follows the branch the moment `sofar new`/`switch` binds it. (C) No ceremony in the lane: Stop never blocks, the prompt-hook write-back nudge is silent, SessionStart renders a lean lane block (fixed goal, three how-to lines, recent quick work, decision index, next ids, no read-back); the only ask is sofar_start_session + sofar_log_decision when a decision was made. (D) `sofar new quick` refuses (the lane creates itself); `sofar close quick` turns the lane off — hooks discard as before and the unbound notice says so. (E) Promotion is `sofar new <slug>`, named on the lane block with the lane's session and decision counts — no carry-over mechanism.** over (a) a flag on `sofar new` (--quick) — still a command and a name before the work, the ceremony the task removes; (b) an init-written default binding "*": "quick" — per-repo setup existing repos never get, and a binding is what D1 (1.1) and initiative-lifecycle D4 rejected; (c) lazily binding the branch to `quick` in bindings.json — a hook writing a committed file, misrouting the branch for every later session; (d) creating the lane at SessionStart — an append on the read path (record-hygiene D2); (e) per-branch or per-day lane records — fragments the one place quick decisions are recalled from. because The lane differs from the rejected default initiative (D1, initiative-lifecycle D4) on the two properties that made that wrong: it never names the project (fixed slug and goal; the notice still says a project needs `sofar new`) and it never binds (bindings.json untouched, so nothing misroutes and nothing undoes a close). PREDICT, one ablation arm each (D5): (A+C) a 1–3 minute fix with no decision pays zero sofar tool calls (was new + start_session + update_plan + end_session ≈ 4 calls, ~2.5k tokens) — overhead ratio ≤15% of task tokens, lane block under ~2,500 chars on a typical lane vs the 10k cap; (B) zero split sessions when quick work grows into `sofar new`; recall: `sofar find` and the graph answer 'why did X change' from quick's file_touched, decisions and commit trailers, measured in round 2's quick-work addendum.
- 2026-09-15T17:30:48.829Z — chose **Carve-out for the quick lane (D14 arm B), naming the rail it bends: record-integrity D9 ("a session's home is the LATEST session_started carrying its id; never let the branch binding outrank a later registration") holds for every real slug, and the lane is the ONE exception — homeInitiative skips a registration in `quick` whenever a real slug is preferred. session-orientation D2 (never redirect a fresh session to the most recently active initiative) is not crossed: the lane is a fixed fallback for a branch bound to NOTHING, never a redirect from a bound record.** over (a) obeying D9 verbatim — a session whose first edit landed in the lane stays homed there after `sofar new` binds its branch, the split-session tear D9 exists to prevent, reintroduced by the fallback; (b) re-registering the session in the new slug from `sofar new` — the CLI does not know the session id; (c) no fallback until a session calls sofar_start_session — the ceremony the task removes. because D9's law is about REAL homes: a later registration means the session moved on purpose. A lane registration is mechanical, made by a hook before anyone chose anything, so it carries no intent to outrank. The carve-out is scoped to one slug and one direction (lane never beats a real preferred slug); with no preference the lane is a home like any other, so the commit trailer and the hooks still find a lane session.
- 2026-09-15T18:57:41.028Z — chose **3.3 relevant-lessons line at UserPromptSubmit, one ablation arm (D5): the hook tokenizes the prompt (the payload's `prompt` field, first 2,000 chars) and BM25-ranks it (core/lexicon.ts rankLexical — the `sofar find` ranker, no model) against THIS initiative's lessons: every decision's full prose (chose+over+because, rendered as its `over`), plus past failures — driver handoffs carrying a `detail` for a stall or error. It prints at most 2 lines, `sofar: ruled out before — [D<n>] <over ≤200> (matched: w1, w2)`, only when a lesson shares ≥2 of the prompt's terms and scores ≥1.5, so a single common word is never a match. Placed directly after guard crossings and before the conflict hazard: guards say done work crossed a rule, this says the intent just typed was ruled out. Best-effort, silence on any failure, stateless (re-fires while the prompt keeps matching).** over (a) reading the repo-wide Tier-1 reach index per prompt — a 245KB JSON read on the hot path for a repo-wide answer when the lesson that matters is this record's; (b) matching only `over` text — the prompt names the subject the decision is ABOUT, which lives in `chose`, so `over`-only misses the re-proposal it exists to catch; (c) a model or embedding — SPEC zero-model invariant; (d) injecting on every prompt unconditionally — noise on the one surface read every turn. because C3 (bench-refresh) is the per-decision re-violation rate: the digest shows rejected approaches once at SessionStart, and by the prompt that re-proposes one they are 40k tokens back. Point-of-use is the mechanism that worked for guards (record-index 3.2). PREDICT: C3 +5 pts (fewer re-proposals of ruled-out approaches); cost per prompt ≤5ms on this record (≈15 decisions tokenized in-process, no file read beyond the fold the hook already does), measured before ship; zero lines on prompts of ≤1 content word ('continue', 'yes').
- 2026-09-16T06:15:14.331Z — chose **2.7 single fold per append, exact: fold.ts splits foldLines into replayDecoded (the per-event loop, retained as a FoldCheckpoint: un-finalized state plus every side table — blockNotes, edges, seenSessions, orphan candidates, guard cache, voided set, last id, line count) and finalizeFold (structuredClone the state, then task_files, activity, deriveCurrent, orphans, unregistered — the post-loop as today, on the clone only). appendToCheckpoint(cp, line) applies ONE appended event through the same loop body when it is envelope-valid, not a correction, and its id ≥ the last replayed id; anything else returns null and the caller refolds. ToolContext.foldState caches {size, mtimeMs, checkpoint} per slug (LRU 8): a stat that matches serves finalizeFold; appendAndProject advances the checkpoint with the line it just wrote when the post-append stat equals cached size + line bytes, else drops it. Projection bytes are untouched: the state a hit returns is deep-equal to a fresh fold by construction, pinned by a property test over prefixes, corrections and out-of-order ids.** over (a) caching only the decoded lines — parse is 33 of 79 ms on an 11 MB log, replay 41, so the second fold would still cost half; (b) replacing the pre-append fold with a raw-line scan (registrationIn-style) for registration and ended checks — diverges from the fold on voided or invalid events, and session-end still needs the fold; (c) deferring projection regeneration — changes when projections are written, which the SPEC's per-append contract and the record's settle-on-commit behaviour rely on; (d) a persisted state cache on disk — a second store that can desync from the log (record-integrity D1). because Measured on a synthetic 11 MB, 40,901-event log: fold 79 ms (read 6, decode 33, replay+derive 41), structuredClone of the folded state 0.6 ms, edges.slice 0 ms. A hook that folds twice pays ~158 ms; with the checkpoint the second fold is one applyEvent plus a finalize, a few ms. PREDICT (one arm, D5): post-tool and session-end hook fold time p50 −40% or better on a ~10 MB record (expected ≈ −45%); MCP tool calls in a long-lived server that append to an unchanged log fold zero times; projection bytes byte-identical to 0.32.0 on every conformance golden. Falls back to a full fold on every path the fast path cannot prove — a correction, an out-of-order id, a size or mtime mismatch — so a stale state is never served.
- 2026-09-16T06:33:19.050Z — rule: **Never ship a release whose session-start, user-prompt, stop or statusline p50 on the real-record fixture exceeds the previous release's by more than 10% (bench/read-paths.mjs, interleaved); a lever over budget gets cheaper or goes behind a flag defaulted off.** — chose **Read-path latency budget for the RC (peer sofar-4c's ruling, 2026-09-16), with the branch's levers attributed by ablation on this repo's real record (55 initiatives, 0.6 MB bound log, 694 events, 17 decisions), interleaved ABAB, N=25, end to end: session-start 62.3 → 62.8 ms (+0.5), user-prompt 45.2 → 48.6 (+3.4, of which the lessons line is +1.5 — the no-prompt run isolates it at +1.9), stop 35.1 → 35.6 (+0.5), statusline 35.5 → 36.5 (+1.0); node floor 47.9 → 48.2. In-process warm p50: session-start 24.2 ms, user-prompt 6.4 with the prompt vs 4.2 without (3.3 = 2.2 ms; relevantLessons alone 1.5), stop 4.0, statusline 4.0; 2.7's finalizeFold 0.09 ms, ctx.foldState hit 0.10. The peer's +11/+12 ms did not reproduce. Shipped: (1) packages/engine/bench/read-paths.mjs, `npm run bench:read-paths -- --baseline <cli.js> --candidate <cli.js>`, interleaved, exits 1 when any candidate p50 exceeds baseline × 1.10 — the acceptance check; (2) 3.3 bounded so it cannot grow into a tax: the last 60 decisions only, each doc's prose clipped to 1,200 chars before tokenizing, and `SOFAR_LESSONS=off` disables the line — the ablation switch round 2 uses to price its tokens separately.** over (a) a vitest timing assertion — flakes under load (round 1 runs on this machine) and would gate every commit on a number; (b) putting 3.3 behind a flag defaulted OFF — it is inside budget, and off-by-default would exclude it from round 2's C3 measurement, the reason it exists; (c) precomputing lesson terms in the Tier-1 index — a 245 KB JSON read per prompt costs more than tokenizing 60 decisions; (d) accepting the peer's numbers unmeasured — the baseline is 0.32.0, so the delta is the whole branch, and a single-order run on a machine hosting round 1 is noise at the 10 ms scale. because Hooks run on every prompt and tool call, so a read-path regression is user-visible in a way a write-path win is not; the budget makes that a gate with a reproducible script rather than a judgement. Attribution by ablation (D5, D20) shows one lever with a real cost — the lessons line at ~2 ms on 17 decisions, which would have reached ~20 ms at the old 200-decision cap — and that cost now sits beside its predicted C3 gain.
- 2026-09-16T06:50:45.701Z — rule: **Never accept a driven task as done without a recorded verification pass on the tree it ran against when a verify command applies, and never run a plan-level verify command outside the run's recorded permission surface.** — chose **3.1 verification-gate contract (run-owner sequencing via peer sofar-4c: contract first, then build, then the RC). (1) SOURCE: the acceptance command is the task's `verify` {cmd, cwd?, timeout_ms?} on plan_updated/task_added tasks (packages/schema/src) or the run's `--verify <cmd>` default, recorded in run_started.verify and taken from the record on --resume, like the surface; no built-in table (D8). (2) PERMISSION: the command runs in the driver's own process, so it runs only when operator-approved — `--verify` is the operator's word; a plan-level `verify`, which an agent can write, runs only if it matches an allow rule of the run's recorded surface (`Bash(<prefix>:*)` or `Bash(<cmd>)`), else the result is `refused` and nothing executes: never wider than the launched agent's surface. (3) PERSISTENCE: `verification_recorded` in events.jsonl, writer driver: {run, task, attempt, command, cwd (relative to the launch dir), checked: {head, tree}, validator (engine version), result: pass|fail|timeout|error|refused, exit_code?, signal?, duration_ms, timeout_ms, diagnostics? ≤1,024 chars — ANSI-stripped, redacted tail of stdout+stderr, D9's precedent for driver diagnostics on the record}. `checked.tree` is sha256 over `git diff HEAD` plus the untracked files' blob hashes: the exact tree the command ran on. (4) INVALIDATION: a pass counts only while the current fingerprint equals `checked` and the command is unchanged; the fold keeps each task's latest verification and the driver re-fingerprints before trusting one — stale means verify again. (5) ELIGIBILITY: where a verify command applies, a task the agent marked done is accepted only on a pass. Otherwise the driver reopens it (task_status_changed → active, note = attempt and first diagnostic line), files the handoff as the new reason `verify_failed` (HANDOFF_REASONS widened; a pre-RC reader skips that handoff with a fold warning — it cannot drive anyway), and the next session's prompt carries the failure verbatim; attempts count per task per run, and --max-verify-attempts (default 3) stops the run as `stall` naming the task. A `dropped` task is never verified and never counted as verified: its handoff stays task_done, the record shows no pass. (6) RESUME/CRASH: no driver state — on every turn the driver checks the task it is about to hand off or resume: done + verify applies + no pass covering the current tree → verify first; a crash between the agent's done and the verification leaves no record, so the resumed driver verifies before moving on. (7) SURFACES: plan.md marks a done task with its latest verification; records without one render byte-identically; `sofar status` lists verifications with the run.** over (a) trusting the agent's task_done, as today — the C4 drive claim stays untested another round; (b) an acceptance step inside the agent's prompt — the agent grades itself; (c) results in driver memory or a side file — dies with the driver, the audit's restart blocker; (d) diagnostics in self-improve's private store — the queue must be readable from the committed record alone, and D9 already carries driver diagnostics on handoffs; the event is the driver's acceptance decision, not outcome capture, so D6 is not crossed; (e) reason `stall` for a failed verification — a stall says nothing moved, here the agent claimed done; (f) a built-in table of test commands (D8's reasoning); (g) running a plan-level verify unconditionally — an agent could write a command the operator never approved into the plan and have the driver run it with the operator's full permissions. because The audit's blockers were exactly restart safety and invalidation on changed inputs; both fall out of putting the result in the append-only record keyed to a tree fingerprint, which is also what lets a resumed driver and a human reader see why a done task came back. PREDICT (one ablation arm, D5; round 2 C4): drive final pass rate +5 pts over round-1 drive on the hidden tests; false task_done rate (tasks marked done that fail the hidden suite) → 0 on verified runs. Cost: one command run per accepted task plus 3–4 git spawns for the fingerprint, on the driver path, never a hook.
- 2026-09-16T07:41:55.367Z — rule: **A fold snapshot is derived state: never part of the record, never committed, never exported or synced, and a version mismatch forces a full refold — never a stale read.** — chose **5.1 contract for the public incremental fold API (sofar.sh/engine). SURFACE: `foldAll(events | lines): Snapshot` — the full fold, retained; `fold(snapshot, events): FoldStep` where FoldStep is {ok: true, snapshot} or {ok: false, reason: 'version' | 'order' | 'correction' | 'invalid'} — the tail is applied through the same loop body the CLI fold uses (2.7's appendToCheckpoint), and any tail the fast path cannot prove exact (an event id below the snapshot's cursor, a correction, an envelope the decoder rejects) is refused so the caller refolds from all events; `stateOf(snapshot): FoldResult` — finalize on a clone, pure and repeatable; `serializeSnapshot(snapshot): string` and `parseSnapshot(text): Snapshot | null` — null on a version mismatch or corrupt text, never a stale read; `readEventsSince(logPath, cursor): {events, cursor, warnings}` — envelope-valid events with id > cursor in id order, a scan that folds nothing. SNAPSHOT = {version: {engine: the package version, schema: sha256 over the schema package version, EVENT_TYPES and every type's reference `fields`}, cursor: max event id folded, lines: lines consumed, and the checkpoint's data with Maps and Sets as entries}. DERIVED ONLY: not an event, fails validateEnvelope, never written under .sofar/ by the engine, never exported, imported or synced (sentinel tests). PURITY: no Date.now, no environment read inside the fold or the snapshot — a test freezes both and compares. PARITY: snapshot+tail === full fold and order independence (any arrival order folds to the same state, with the incremental path refusing out-of-order tails) live in ONE conformance suite shared with rust-core — fixture cases (NDJSON events, a tail index) with TypeScript-reference goldens under packages/engine/test/conformance/fold-parity/, a TS runner here and rust-core's binary runner on the same files; location and shape proposed to sofar-rust-core-5f before the copy is written. SEMVER: additive exports; a snapshot layout change bumps the engine version, which invalidates every snapshot by construction.** over (a) keeping the snapshot format private and offering only foldLines — cloud ingest and the Mac app would keep paying a full fold or a process spawn per read; (b) storing snapshots under .sofar/.index — a second store the record-integrity D1 argument forbids as truth, and one that sync could pick up by accident; (c) applying out-of-order tails incrementally by re-sorting — the snapshot holds no earlier events, so exactness cannot be proven; refusing and refolding is the honest path; (d) a separate TypeScript parity suite beside rust-core's — two suites drift, the run owner asked for one; (e) versioning by engine version alone — a schema change with no engine bump would serve a stale snapshot silently. because The 2.7 checkpoint already proved the mechanism exact (prefix-by-prefix equivalence, 0.6 ms clone, 2 ms apply on an 11 MB log); the public API is that mechanism with a versioned serialisation and a refusal path. PREDICT: `fold(snapshot, tail)` with a 1-event tail on a 10 MB log ≤5 ms against 79 ms for a full fold; parse+apply+finalize of a serialized snapshot ≤25 ms; readEventsSince costs O(tail bytes) — measured before ship. Next release, not the RC: round 2 measures 179b8fd exactly.
- 2026-09-16T07:42:11.221Z — rule: **Never rename or renumber a fold-parity case id or property name (fold-parity/snapshot-plus-tail, fold-parity/order-independence, FP-NN-*) without a Decision that names the old and new names.** — chose **5.1 addendum to D20, sofar-cloud's three requirements (their enterprise D4, relayed by peer sofar-4c): (1) `snapshot.version` {engine, schema} is a READABLE public field of the Snapshot type and of the serialized form, and `parseSnapshot` returns the mismatch as data — {ok: false, reason: 'version', found: {engine, schema}, expected: {engine, schema}} — so ingest can log and alert on refold storms after an upgrade instead of seeing an unexplained latency spike; (2) the parity suite's case ids and property names are public API: cases are `FP-NN-<slug>` files under packages/engine/test/conformance/fold-parity/cases/, the two properties are named `fold-parity/snapshot-plus-tail` and `fold-parity/order-independence`, and the sentinel tests `fold-parity/version-mismatch-refolds` and `fold-parity/pure-of-clock-and-env`; (3) the release that ships the API tells sofar-cloud its version in one line (next release after 0.33.0-rc.1 — the RC round 2 measures does not carry it).** over (a) an opaque version check with a boolean — procurement and ingest telemetry cannot cite or count what they cannot read; (b) test names as incidental strings — a rename would silently break a written customer claim; (c) telling cloud "it's in the next release" without a version — ingest cannot pin to a phrase. because Each is cheap now and expensive later: the version field is one exposed object, the names are chosen once, and the shipping message is one line — while a refold storm mis-diagnosed as latency, a broken procurement citation, or an ingest pinned to the wrong engine each cost a customer conversation.
- 2026-09-16T07:45:08.547Z — chose **5.1 contract amendments agreed with sofar-rust-core-5f (their reply to the shared-suite proposal), binding on both implementations: (1) fold-parity cases are RAW lines (NDJSON, corrupt and unknown lines included — the fold's input is bytes), sidecar {tail_at, seeds, note}; the golden is {state, warnings} for the arrival-order run (warnings are file-order line-numbered and part of parity), order-independence compares state only. (2) FP-08's duplicate ids are byte-identical lines — the idempotency case — so a shuffle folds the same. (3) ONE black-box runner: the suite drives an implementation command — `sofar fold --events <jsonl> [--take <n>] [--snapshot <file> --since <n>] [--write-snapshot <file>] --json`, a hidden commander command over the library API — with SOFAR_CONFORMANCE_BIN selecting the candidate and the built TypeScript CLI as the reference; the library-API test is additional, the cited guarantee is the black-box one. (4) Refusal reasons are a closed enum with exact strings: fold → `version` | `out_of_order_id` | `correction` | `invalid_line` | `cursor_mismatch`; parse → `version` | `corrupt`. (5) Hashes are over BYTES: the snapshot carries prefix {bytes, sha256 of the UTF-8 file prefix through the last consumed line, lines, last_line_sha256}, and `--since` on a file verifies that prefix before applying (mismatch → `cursor_mismatch`); the schema hash is sha256 over a COMMITTED artefact, packages/schema/schema-fingerprint.txt, emitted by `npm run schema:emit` (the exact string: schema version, then one `type: fields` line per EVENT_TYPES entry) and pinned by a test, so Rust embeds the same constant; engine version = the sofar.sh package version. (6) Canonical state JSON: object keys sorted by code point recursively, arrays in order, JSON.stringify(v, null, 2) verbatim. (7) pure-of-clock-and-env: the black-box runner empties and varies TZ/LANG/HOME and runs twice at different wall-clock times; Date.now freezing is the library run's. (8) D21's two sentences — the version is a readable field, the test ids are public API — stand as both records' citation.** over (a) my proposal's envelope-valid cases and a TS-only runner — FP-07 needs torn lines, and a Rust binary proving a different suite proves nothing shared; (b) a schema hash computed from TypeScript reference strings only — Rust could not reproduce it; (c) free-text refusal reasons — two implementations would drift on the one field a consumer branches on. because The run owner asked for one suite run against both implementations; every change makes a property provable from bytes and a command line, which is what a second implementation can actually match.
- 2026-09-16T08:32:07.312Z — chose **Round-2 attribution for the RC (4.3, applying D5 and bench-refresh's claim-hygiene rule of 2026-09-15): the RC at 179b8fd carries exactly ONE ablation switch, `SOFAR_LESSONS=off` (3.3) — verified by grep on the sha, no other `process.env.SOFAR_*` read exists there. Every other lever is attributed by its own pre-registered observable from the plan's PREDICT line (1.1 turns and split sessions, 1.2 duplicate session_started, 1.3 Codex S1 tool calls and plan-bearing initiatives, 1.4 formatter edits, 1.5 shell corrections, 1.6 stall causes, 2.1 tool-call share, 2.2 digest chars, 2.3 cache-creation tokens, 2.4 schema bytes, 3.1 drive pass rate), read from round-2 outputs and published as hit or miss per lever. The C1 headline is measured once on the full RC against 0.32.0 and never reconstructed as a sum of per-lever predictions. Off-switches for 2.1–2.4 and 2.6 ship in the NEXT release beside 3.2's, so round 3 can ablate them.** over (a) adding env switches for 2.1–2.4 and 2.6 to the RC now — changes the sha round 2 measures, which bench-refresh froze at 179b8fd; (b) a second pinned build carrying the switches — two artefacts to keep identical, and public numbers come only from the frozen one; (c) attributing the overlapping wave-2 gains by summing their predictions. because The freeze is worth more than per-lever ablation this round: each wave-2 lever already names a distinct observable in its own unit, so its prediction is checkable as a hit or a miss without a switch; only the C1 total would ever be summed, and that one is measured directly.
- 2026-09-16T09:23:39.675Z — chose **2.5 contract — automatic capture, "the model logs only why" — settled before code, consuming self-improve 1.2 (D6) and adding NO event type, NO payload field and NO storage. (1) INPUTS: file_touched and command_run with their optional `ok`/`exit` (self-improve D2), verification_recorded (D19), task_status_changed; commits are read from git at render time and never recorded (SPEC §Commit attribution; record-hygiene D1 keeps git and sofar commands hook-exempt). (2) FOLD, additive and pure: a `ran` edge carries {ok?, exit?, test?} where `test` is the command text (clipped) when a fixed pure recognizer calls it test-shaped (npm/pnpm/yarn/bun test, vitest/jest/mocha/pytest/rspec/…, cargo/go/dotnet/swift/make test, per shell segment); a new `tested` edge (task → command) is written for every task active at a test-shaped command whose `ok` is known, the same "which tasks were active" rule task_files uses. Derived on finalize: SessionActivity gains OPTIONAL `failed` (commands with ok:false, absent when 0) and `last_test` {cmd, ok, exit?}; InitiativeState gains OPTIONAL `task_tests` (task id → latest {cmd, ok, exit?, ts, event_id}), present only when non-empty — records without `ok` fields fold to byte-identical state, so every fold-parity golden and every 0.32.0 projection is unchanged (D21). A command whose `ok` is absent is UNKNOWN: never counted as failed, never a test outcome. (3) SURFACES: the status block's Current task gains one budgeted line `tests: pass|fail — <cmd>` (a newer D19 verification wins the line); sessions/<id>.md and describeActivity say `N commands (M failed)` and `tests pass|fail`; SessionStart adds one volatile-tail line, from the SAME bounded attribution walk the shipping notice already pays (no second spawn), `Commits (this record, last N walked): <task> ×n, … — newest <sha7> <subject>` using the CLAUDE.md task-id prefix of each trailered commit's subject; CommitAttribution gains `subject`. (4) GUIDANCE: sofar_update_task's `note`, sofar_end_session's `summary` and the AGENTS block's DURING line say once: write why, never what hooks capture — files, commands, test outcomes and commits are derived. (5) SWITCH: `SOFAR_ACTIVITY=off` (also 0/false) removes the tests line, the commits line and the guidance sentences from the injected surfaces and tool descriptions — round 3's ablation arm (D5, D23); projections stay pure and never read the env (5.1 purity). (6) PREDICT: sofar MCP/CLI calls per session −40% vs the RC on the same chain, arm on vs off; notes and task notes that restate files, commands, tests or commits → 0; SessionStart bytes +≤200 on a record with activity. (7) DONE = SPEC §Acceptance criteria bullet "Derived activity (r1-fixes 2.5)" passing.** over (a) commit or test events in events.jsonl — forbidden by D6 and SPEC §Commit attribution, and they would reach export and sync; (b) reading the private diagnostics store at render time — the store is diagnostics, never a projection input (self-improve D3), and the durable `ok`/`exit` already carry the only outcome facts the record may hold; (c) a Stop-hook summary generated from activity in place of the agent's write-back — the why cannot be derived, and the derived resume point already exists for sessions that never wrote back; (d) always-present `task_tests: {}` and `failed: 0` — churns every fold-parity golden and forces rust-core to mirror before it has data to mirror; optional-when-present is additive; (e) a second git walk for the commits line — the shipping notice's bounded walk already has every subject in its %B. because Round 1 shows sofar at 32–38% of tool calls, part of it agents narrating what hooks already recorded; after 1.2 the record holds the outcome facts, so the remaining lever is to derive and show them and to tell the model once that only the why is its job. Everything here is a pure function of events already in the log plus a bounded git read the session already pays, so it costs no event, no schema, no store and no wall-clock in replay.
- 2026-09-16T11:14:24.623Z — chose **3.2 contract — decision retirement without a model, settled before code, for the NEXT release (run-owner ruling 2026-09-16: out of the RC, priced in round 3 by ablation). TWO additive optional fields on decision_logged, in packages/schema/src/events.ts only: (1) `supersedes: "D<n>"` — the bare ordinal handle of an EARLIER decision in the SAME record this one replaces (per-record, like ordinals themselves; no cross-record supersession); (2) `until: "<task id>"` — the decision is in force until that task of this record RESOLVES (task_status_changed to done or dropped, as replayed); the fold resolves both from recorded events only — NO wall-clock, NO env: a fold at any time yields the same state (5.1 D20 invariant). STANDING RULES NEVER AGE OUT: `until` is REJECTED by payload validation on a decision carrying `rule`; a rule-carrying decision is retired ONLY by a superseder that itself carries `rule` (the fold leaves a rule-less superseder's `supersedes` inert — recorded, never applied), so the count of standing constraints only ever falls by an explicit new constraint naming the old one. Forward/self references are inert; an unknown task id never retires. FOLD STATE (mirrored by rust-core): DecisionState gains `supersedes?`, `until?` (as recorded) and `superseded_by?: <ordinal>` (set when resolved and permitted); `until`-retirement is a SELECTOR over final state (retiredDecisionOrdinals), not a stored field. COUNTERS: ordinals D<n> and `Next ids` count EVERY decision, retired or not — ids never renumber (D21 spirit). RENDER (renderStatus digest + the 3.3 lessons line only): retired decisions leave Standing constraints, the Recent-decisions window (which becomes the last 5 IN-FORCE decisions), the rejected ledger and the lessons corpus; the header reads `Recent decisions (last 5 of <in force> in force, <k> retired; …)` and is byte-identical to today when k=0; a superseder's index line carries `(supersedes D<n>)`. decisions.md marks each retired line (`superseded by D<m>` / `until <task>`) and renderFullStatus, the review packet, `sofar find` keep everything — details on demand. OFF-SWITCH: `SOFAR_RETIRE=off` (also 0/false), read at RENDER time only (retireEnabled(), core/retire.ts), renders every surface as if nothing were retired — round 3's ablation arm; the fold never reads it. PARITY: one new case FP-10-decision-supersession (supersedes resolved, rule-mismatch inert, forward ref inert, until-task done) recorded through the reference; existing ids untouched (D21). PREDICT (bench-refresh D10, stated before build): on the real r1-fixes record, retiring the decisions a later one replaces cuts the SessionStart digest ≥10% chars with C3 no worse (arm SOFAR_RETIRE=off vs default in round 3); read-path p50 within D18.** over (a) wall-clock `valid_until` timestamps — replay would depend on when the fold runs, and a standing constraint could vanish between two folds with no event explaining it (the ruling's stated hazard); (b) letting any decision supersede a rule-carrying one — a rule-less superseder silently drops law; (c) storing `until` retirement in fold state — it depends on the task's FINAL status, so it is derived, and rust-core would mirror a redundant field; (d) renumbering or hiding ordinals of retired decisions — citations in prose and commits would dangle; (e) qualified `<slug> D<n>` handles like memories — the digest folds one record, so cross-record supersession has no surface to act on; (f) reading the env switch in the fold — the fold API is env-free by 5.1 D20 because the run-owner ruling made the contract conditions explicit (no wall-clock in replay, standing rules never age out, byte-stable injection, own off-switch), and each field here is derivable from replayed events alone; the record already pays for decisions twice as they accumulate (24 rules render verbatim, 28 older `over` lines), and 2.2's dedupe hit the floor of what rendering-only can trim — retirement is the only lever left that removes bytes rather than clipping them, and it removes exactly the bytes a later decision already declared stale
- 2026-09-16T11:33:45.253Z — rule: **Never sort a shared-surface string (path, slug, session or event id, term) with localeCompare — use byCodeUnit from core/order.ts, so both implementations order the same bytes the same way.** — chose **5.2 adopts rust-core D6 (O1, 2026-09-16) for the TypeScript engine: every sort of a shared-surface string — file paths, slugs, session ids, event ids, lexicon terms — orders by UTF-16 code units through ONE comparator, `byCodeUnit` in core/order.ts, and never by `String.prototype.localeCompare`. Sites converted in this task: fold.ts openSessionFileConflicts, cross-conflicts.ts, index-tier0.ts, index-tier1.ts, listing.ts, lexicon.ts, graph.ts, index-reach.ts, doctor.ts, cli/event.ts. PREDICT (stated before build): no byte moves on any existing projection golden, fold-parity golden or the real record's digest (every fixture is lowercase ASCII, where the two orders agree); a mixed-case set folds to the same order in both implementations — proven by a unit test on README.md / Zed.ts / a.ts / readme.md and slugs with hyphens ('a-b' vs 'ab', where ICU collation ignores punctuation at the primary level and code units do not).** over (a) leaving localeCompare and asking rust-core to match ICU collation — locale-dependent, version-dependent, and unavailable without a collation library in Rust; (b) converting only the five sites rust-core named — the id/term/slug sites carry the same divergence for hyphenated slugs and would be found one at a time; (c) a code-unit sort with case folding first — that is a third order neither side has because two implementations of one record must order the same bytes the same way or the first mixed-case conflict set diverges silently (no current golden exercises one); code-unit order is what Rust's str Ord already is, is locale- and env-free (the 5.1 D20 purity the fold-parity suite proves), and costs nothing on the hot path
- 2026-09-16T16:12:06.296Z — chose **4.1.1 (L07) contract: teach `rule` on the three surfaces a CLI agent without MCP actually reads, and change nothing else. (1) AGENTS_PROTOCOL_BLOCK: the DURING decisions example becomes {chose, over, because, rule} followed by one condition — add `rule` (one short imperative) when the operator states it for the whole project, because `sofar status` shows it to every later session as a standing constraint; omit it for a one-off choice. Edited in place: the post-2.5 block never reached a candidate or an install (no AGENTS.md under ~/IO carries it), so the ledger stays at V7, rc.1's block. (2) EVENT_TYPE_REFERENCE.decision_logged (`sofar event types`): the rule field text carries the same condition and the validating example gains a rule on its existing SQLite decision. (3) codexPinLine, the driven codex preamble: the decision example gains rule plus the condition. Placeholders stay placeholders — no example names a domain choice a bench chain could plant.** over (a) a V8 ledger entry for the unreleased post-2.5 block; (b) making `rule` required on CLI decisions (a one-off choice would carry an invented constraint that never ages out, D25); (c) promoting decisions to rules after the fact by keyword or model; (d) touching the MCP dialect — sofar_log_decision's schema already describes `rule` and Claude MCP cells hold rules for 6 of 6 planted decisions; (e) adopt.ts's replay brief, a migration surface rather than a session protocol because L07: CLI sofar cells recorded 0 rules across 43 decisions while every MCP cell covered all 6 planted decisions, and the one difference between the dialects is that only the MCP tool schema mentions `rule` — a CLI agent copies the example it is shown, and every CLI example showed three fields.
- 2026-09-16T16:15:47.644Z — chose **4.1.4 (L10) contract: `sofar status` with NO slug, in a repo that carries .sofar/, whose branch is unbound (or there is no branch — detached HEAD) orients instead of failing, and exits 0. STDOUT, in order: (1) one plain line naming why and what to pass — no initiative is bound to branch "<b>" (or: no current git branch), so this shows the most recently active open initiative, `<slug>`; pass it explicitly (`sofar status <slug>`, `sofar event append <slug> …`); (2) that initiative's status, byte-identical to `sofar status <slug>` under the same caps; (3) a blank line, then the `sofar list` render. "Most recently active" = the first listInitiatives entry (last event id descending) whose status is not closed. No open initiative: the line says so with the `sofar new <slug> --goal` move, then the list, exit 0. UNCHANGED: an explicit slug that does not exist exits 1; a repo with no .sofar/ exits 1; a branch bound to a missing directory exits 1; the open quick lane still answers an unbound branch (D14 resolves first, nothing here runs); `--watch` on a TTY still refuses unbound; MCP get_state untouched; nothing written — status stays read-only.** over (a) keeping exit 1 with a better message — Codex's first call still fails and costs a turn; (b) printing only the list — the agent needs the digest to orient, and the list alone is what led Codex to open one initiative per roadmap item; (c) choosing by branch-name similarity or a heuristic — recency is already the listing's order and is in the record; (d) auto-binding the branch to the recent initiative — a read command writing bindings.json is a side effect nobody asked for, and binding is `sofar switch`'s job; (e) counting closed records as candidates because L10: an unbound `sofar status` exited 1 on 9 of 10 Codex sessions' first call, and 2 of 3 CLI cells fragmented into several initiatives after it (S2's delete decision sat in `bucket-list`, invisible to the `trips` digest). Showing the record that most recently moved, with its slug named as the thing to pass, is the orientation a failing call withheld.
- 2026-09-16T16:20:39.145Z — chose **4.1.3 (L09) contract — one session id per launch through a per-clone pointer. (1) STORE: `<stateBase>/sessions/<cloneKey>.json` = {session, writer: hook|cli, ts} — the state-dir home the diagnostics store already uses, so it is per-worktree by construction and never under the repo (stronger than a gitignore: nothing to commit, pack or sync). Written only in a repo that carries .sofar/, atomically, and only when session or writer changed. (2) HOOKS: SessionStart, UserPromptSubmit, PostToolUse and PostToolUseFailure write {session: hook session_id, writer: hook}; SessionEnd removes the pointer when it names that session. Best-effort inside each hook's existing try — a pointer failure never changes a hook's output or exit. No record event: the pointer is derived state, never truth. (3) CLI: `sofar event append` loses the `--session` default. An explicit --session always wins, unchanged. Omitted: session_started adopts the pointer's id when that session is not ended (session_ended or session_closed) in the target record, else mints `cli-<ulid>` and writes it as the pointer {writer: cli}; every other type adopts the pointer's id, and with no pointer falls back to `cli` as before. When --session was omitted the JSON result adds `session`, so the id is visible. (4) BLOCK: the AGENTS block's START says to register WITHOUT --session (sofar adopts the id its hooks registered, or mints one and prints it) and every example drops `--session <session-id>`, with one caveat line: two sessions in one worktree at once each pass their own --session. `sofar event types`' usage line drops --session the same way. The codex driver preamble keeps its explicit id (the driver assigns ids and matches on them, session-driver D3).** over (a) the ruling's literal 'gitignored' file inside the repo (.sofar/.session plus a .gitignore entry) — a file under a committed directory is one mistaken `git add -A` from the record, and the state dir already gives per-worktree isolation for free; (b) a pointer in the worktree's own gitdir — needs git, and commit-attribution D20 keeps per-worktree gitdir use to HEAD; (c) keying the pointer by the agent's process tree (ppid walks) — platform-specific and opaque inside sandboxes; (d) telling only hooked agents to omit --session — an agent cannot see whether hooks run, and omitting it without minting would collapse every Codex session into `cli`; (e) extending adoption to MCP sofar_start_session — L09 is a CLI-dialect loss, and 1.1's injected Session line already carries the id to MCP clients; (f) time-based pointer expiry — record state (ended or not) decides, no clock because L09: cursor-sofar/r2 has 20 session_started for 10 launches — the agent minted its own id (as the block told it to) about a minute before the hook registered Cursor's, so all 10 hook sessions end without a write-back and every Stop-gate and drift signal reads the wrong half. 1.2's lock made registration idempotent per id; it cannot merge two ids. Adopting the hook's id removes the second id at its source, and minting on a hookless start keeps Codex's per-session write-backs intact under the same instruction.
- 2026-09-16T16:23:02.918Z — chose **4.1.3 (L09) contract, D29 restated with its STORE clause replaced — one session id per launch through a per-worktree pointer. (1) STORE: `.sofar/.index/session.json` = {session, writer: hook|cli, ts} — the record-index's local dir, which ignores itself (`*`), lives in each worktree's own tree, is derived and disposable, and is writable wherever the agent can write its record. Written only in a repo that carries .sofar/, atomically, and only when session or writer changed. (2) HOOKS: SessionStart, UserPromptSubmit, PostToolUse and PostToolUseFailure write {session: hook session_id, writer: hook}; SessionEnd removes the pointer when it names that session. Best-effort inside each hook's existing try — a pointer failure never changes a hook's output or exit. No record event: the pointer is derived state, never truth. (3) CLI: `sofar event append` loses the `--session` default. An explicit --session always wins, unchanged. Omitted: session_started adopts the pointer's id when that session is not ended (session_ended or session_closed) in the target record, else mints `cli-<ulid>` and writes it as the pointer {writer: cli}; every other type adopts the pointer's id, and with no pointer falls back to `cli` as before. When --session was omitted the JSON result adds `session`, so the id is visible. (4) BLOCK: the AGENTS block's START says to register WITHOUT --session (sofar adopts the id its hooks registered, or mints one and prints it) and every example drops `--session <session-id>`, with one caveat line: two sessions in one worktree at once each pass their own --session. `sofar event types`' usage line drops --session the same way. The codex driver preamble keeps its explicit id (the driver assigns ids and matches on them, session-driver D3).** over (a) D29's store under the XDG state dir — Codex's workspace-write sandbox confines writes to the workspace and temp dirs, so a hookless agent's CLI could not save the id it mints, and every later append would fall back to `cli`: the split this task exists to remove; (b) a new gitignored file elsewhere in .sofar/ plus an edit to the user's .gitignore — .sofar/.index already ignores itself; (c) a pointer in the worktree's own gitdir — needs git, and commit-attribution D20 keeps per-worktree gitdir use to HEAD; (d) keying the pointer by the agent's process tree — platform-specific and opaque inside sandboxes; (e) telling only hooked agents to omit --session — an agent cannot see whether hooks run, and omitting it without minting would collapse every Codex session into `cli`; (f) extending adoption to MCP sofar_start_session — L09 is a CLI-dialect loss, and 1.1's injected Session line already carries the id to MCP clients; (g) time-based pointer expiry — record state decides, no clock because L09: cursor-sofar/r2 has 20 session_started for 10 launches — the agent minted its own id (as the block told it to) about a minute before the hook registered Cursor's, so all 10 hook sessions end without a write-back. 1.2's lock made registration idempotent per id; it cannot merge two ids. Adopting the hook's id removes the second id at its source, minting on a hookless start keeps Codex's per-session write-backs under the same instruction, and the store has to be one the agent's own sandbox lets the CLI write — the record's local index dir is the one place that always holds.
- 2026-09-16T16:28:49.025Z — chose **4.1.2 (L08) contract — a decision that silently reverses a standing one is refused at append time, no model. (1) DETECT, core/reversal.ts, pure over the folded state: terms are core/lexicon's (stopwords dropped, light stems, compounds plus parts). For each clause take its DISTINGUISHING terms — chose minus over, over minus chose — so the word both sides share (`delete` in soft vs hard delete) never counts. A new decision N reverses a prior decision P when share(N.chose′, P.over′) ≥ 0.5 AND share(N.over′, P.chose′) ≥ 0.5, share = |A∩B| ÷ min(|A|,|B|). Only P standing in the SAME record (not in retire.ts's retiredOrdinals); only label-sized clauses — a chose or over over 24 terms is prose and never compared on either side, so long-form contract records cannot trip it. (2) ENFORCE in the two agent-facing writers, before any append: sofar_log_decision and `sofar event append --type decision_logged`. appendAndProject stays the pure validate-append-project path, so hooks, drive and fixtures are untouched. A reversal passes when its `supersedes` names that D<n>, or when `because` cites D<n> as a word — the narrower-exception path while P still stands. Otherwise: typed error invalid_input (no new code, no schema field), message naming each reversed D<n> with its chose/over and date and the three ways forward — follow it; if the operator changed it, pass supersedes D<n> (a rule is replaced only by a rule, D25); for a narrower exception, cite D<n> in because. Nothing is appended. (3) TEACH: `sofar event types decision_logged`'s note gains one clause about the refusal; the MCP schema is not touched (M5's 8k budget) — the refusal text is the lesson, delivered at the moment it applies. No ablation switch: L08 is attributed by its own observable (reversals without supersedes), never summed with L07 (D5).** over (a) warn-only, append anyway — the reversal is already in the record when the agent reads the warning, which is round 1's failure with a footnote; (b) a new payload field or CLI flag to acknowledge an exception — changes the schema fingerprint rust-core embeds (D22) and spends MCP schema bytes M5 does not have, when citing the handle in `because` says the same thing in prose the record already carries; (c) enforcing inside appendAndProject — every internal writer and fixture would inherit a semantic check meant for agents; (d) raw token overlap without the distinguishing-term step — `soft delete` and `hard delete` share half their words and would match everything about deletion; (e) Jaccard — a one-word `over` against a four-word `chose` scores 0.25 on the exact reversal L08 cites; (f) comparing across records — the digest a session obeys is per record because L08: cursor-sofar/r1 S3 logged hard delete over soft delete 3 minutes after S2 logged soft delete, and r2 S4 did it again after S2's app-wide soft delete; A2 at E fell to 17% and 33% against cursor-rules' 67%. A refusal that names the standing decision at the moment of drift turns a silent reversal into a choice the agent must make explicitly — follow, supersede, or cite.
- 2026-09-16T16:36:36.549Z — chose **4.1.5 (L11), the cheap half only: a phase update names its phase by exact name, by number, or by case-insensitive name, and the append always records the plan's canonical name. resolvePhase(phases, ref), exported from mcp/update-phase.ts: (1) exact name; (2) case-insensitive match with whitespace collapsed, when unique; (3) a bare number or `Phase <n>` (any case) → the one phase whose name is labelled `Phase <n>` followed by a non-digit or the end; when NO phase name carries a `Phase <digits>` label, the n-th phase by position. Anything else, or an ambiguous match, stays a miss. Applied on sofar_update_phase (the miss keeps its invalid_input, now listing the accepted forms) and on `sofar event append --type phase_status_changed`, which now resolves the same way and refuses a miss with the same invalid_input instead of letting the fold mint a phantom phase — the hazard update-phase.ts already documents for the MCP path. Fold, schema and every render untouched.** over (a) listing phase ids in the digest — changes the status render goldens rust-core's render parity mirrors (93 goldens), which is not cheap and not needed once the name forms resolve; (b) substring or prefix matching (`wave 3`) — ambiguous on real plans; (c) recording the ref as typed and resolving in the fold — the fold must stay byte-parity with rust-core and never guess; (d) leaving the CLI path permissive — a typo there mints a phase that renders in the plan forever, the failure the MCP path already refuses because L11: claude-sofar/r3 hit invalid_input twice (S7, S8) addressing phases by a name that was not byte-exact, each a wasted call and a retry. Resolving the forms an agent naturally types — the number, or the name in another case — removes those calls without letting a typo through, and the CLI gets the same guard the MCP tool has had.
