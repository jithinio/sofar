<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: initiative-supersession

Goal: Let one initiative supersede others as a recorded edge, not prose: a `superseded` close status carrying a `successor` slug, set by `sofar close --superseded-by` and `sofar new --supersedes`, folded on the old record and derived everywhere else (status, list, banner, doctor, graph, find).

Progress: 11/11 tasks done (100%)

## Phase 1 — Schema and fold [done] — 2/2 done

- [x] 1.1 Schema: `superseded` joins INITIATIVE_STATUSES and the closed set; initiative_status_changed gains `successor` (slug), required for `superseded` and rejected on any other status; tool-inputs for sofar_close_initiative follow
- [x] 1.2 Fold: InitiativeState.successor set by a superseded status event and cleared by any other status event (status_note's rule)

## Phase 2 — Commands [done] — 4/4 done

- [x] 2.1 applyClose takes a successor: must exist under .sofar/initiatives/, must not be the slug itself; idempotent on (status, successor); MCP sofar_close_initiative exposes it
- [x] 2.2 `sofar close --superseded-by <slug>` (exclusive with --drop; note optional — the successor is the reason) with confirmation naming where to resume
- [x] 2.3 `sofar new <slug> --supersedes <a>,<b>`: validate every predecessor before creating anything, create and bind, then close each predecessor as superseded by the new slug
- [x] 2.4 Close-time audit for superseded asks the drop question (tasks left ACTIVE), worded for a successor

## Phase 3 — Surfaces [done] — 3/3 done

- [x] 3.1 Status line, list entry and SessionStart CLOSED banner render `superseded by <successor>` and point resumption at the successor; listing derives `supersedes` on the successor entry
- [x] 3.2 Doctor: a superseded record whose successor does not exist under .sofar/initiatives/ is a warn finding
- [x] 3.3 Graph: structural `superseded_by` edge initiative→initiative in buildGraph; reach index carries the successor per slug and exposes it as hop-1 contents both ways, without ever traversing THROUGH an initiative; `sofar find` phrases it

## Phase 4 — Contract [done] — 2/2 done

- [x] 4.1 SPEC: §Initiative statuses gains Superseding; event types, InitiativeState, CLI new/close, MCP tool, graph edges, acceptance criteria updated; README table
- [x] 4.2 Tests: schema, fold, close/new CLI, MCP tool, closeout, banner/status/list/doctor, graph and reach edges; existing INITIATIVE_STATUSES pins updated

Next action: Nothing open. If a review is wanted after the fact, `sofar review initiative-supersession --final` still works on a closed record.
