<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: self-improve

- 2026-09-15T16:03:42.550Z — rule: **Never keep a self-improvement change unless it beats the equal-budget direct-fix baseline on unseen work net of its full improvement cost, and never write private diagnostics into events.jsonl, git, export or sync.** — chose **Adopt the Codex audit (codex-self-improve-audit-20260915-01) plus these additions, ruled by the run owner 2026-09-15.
AUDIT: (1) signals that aren't captured are reported as UNKNOWN, never guessed; capture is defined first. (2) Private diagnostics are separated from shareable history AT the storage/export boundary. (3) A frozen evaluator runs outside the candidate's control and protects scoring, fixtures and result capture, not just test files. (4) Offline replay proves only context size and information preservation; quality, cost and behaviour need fresh agent runs. (5) Approval binds to an exact tested candidate hash; stale applications are rejected; rejection and reversal history is kept. (6) A durable spend ledger counts every attempt, retry and evaluation across restarts. (7) Development feedback is separated from release evaluation; a held-out workload whose failures guide a fix is retired. (8) The loop must beat a BASELINE: the same agent directly fixing the same loss with equal feedback and budget (Meta-Harness / Better Harness exist, and arXiv 2607.12227 found harness evolution doesn't consistently beat simpler search at equal budget). (9) Downloads and stars stay out of technical gates.
ADDITIONS: (A) The first loop is DEVELOPER-SIDE: bench, loss study, bounded fix, held-out proof, run on our benchmark infrastructure. In-product L1 adaptation inside users' repos is deferred until that loop has repeated wins, which removes privacy and sync risk from the early phases. (B) Outcome capture has ONE owner: self-improve 1.2 defines the schema and private store, and r1-fixes 2.5 consumes it. (C) Improvement claims are NET of the whole improvement cost (fix attempts plus evaluations). (D) Autonomy expands only after 3 consecutive cycles beat the direct-fix baseline on unseen work.
DIFFERENTIATOR: every improvement's benefit, cost, evidence and reversal path is inspectable in the record.** over The original 11-task plan: miner signals assumed available, "never synced" stats with no enforced boundary, checksum-only evaluator protection, replay treated as proof, loose approval semantics, no durable spend limit, repeated holdout reuse, no equal-budget baseline, and public attention metrics near the gate. because Verified against the repo:
- record-hygiene D1 exempts git and sofar commands from the record, so bookkeeping share cannot be mined from events.
- The exporter includes every valid event, and events.jsonl is committed to git, so "local-only" needs a separate store.
The research shows automatic harness loops are not unique and often don't beat simpler search. The risk is better-looking scores without a better product, so the credible edge is a small, auditable loop proven against a direct-fix baseline on unseen work, with its full cost counted.
- 2026-09-15T16:10:28.066Z — rule: **Outcome capture adds only OPTIONAL exit/ok fields to command_run and file_touched in packages/schema/src; every richer observation is a diagnostics row whose shape also lives in packages/schema/src, never validates as an event envelope, and is referenced from the record only by id, hash or count.** — chose **1.1(a) — the outcome-capture SCHEMA contract, gating 1.2 and r1-fixes 2.5.
(1) Two shapes, one home. Both the durable outcome fields (event payloads) and the private diagnostics ROW shape live in packages/schema/src — event payloads in events.ts, the row shape in its own file (e.g. diagnostics.ts), exported from the package entry. Nothing in packages/engine defines or widens either shape.
(2) The split rule. DURABLE (an event in events.jsonl) is a fact the next session on ANY machine needs to resume, at the granularity a human write-back already states: that a command ran and whether it exited ok, that a file was touched. Concretely 1.2 may add OPTIONAL fields to the existing mechanical payloads — command_run gains exit status and an ok flag; file_touched gains an ok flag — and nothing more. Commit facts stay derived from git (commit-attribution: recorded in git, derived from git, never in the record), so r1-fixes 2.5's "commits with task-id prefix" is a read, not a capture. PRIVATE (a diagnostics row, never an event) is everything at tool-call granularity or richer: error text and stderr tails, is_error from the host, PostToolUseFailure payloads, MCP typed-error rejections, hook latency, bytes injected, digest size, memory citation signals, tool names and counts including read-only tools and self-recording git/sofar commands.
(3) Rows are structurally not events. A row carries no ulid `id` and its `kind` is never a member of EVENT_TYPES, so validateEnvelope rejects it by construction; a test pins this. The record may reference diagnostics only by opaque row id, content hash or aggregate count — never by content.
(4) Additive only. New durable fields are optional; an engine that predates them folds the event unchanged (the fold counts command_run and never reads cmd — that stays true). A consumer that finds a field absent reports UNKNOWN for it, never a default (1.3 owns the availability map).
(5) Redaction precedes storage on both sides: command text only through redactCommand; error text clipped to a fixed byte bound and redacted; no tool arguments, no transcripts, no absolute paths outside the clone root by default.** over (a) A single rich `tool_outcome` event type in events.jsonl carrying exit code, is_error and error text — the audit's original 1.2 shape. (b) Keeping the row shape inside packages/engine next to the store code, since only the engine reads it. (c) A new `test_result` event that names pass/fail semantically. because Verified this session against main @ b29d013: core/cursor.ts exportEvents returns every envelope-valid event and client/push.ts pushStream ships that stream with no type filtering, so ANY event in events.jsonl reaches git and sync — a rich outcome event would put error text, which routinely carries paths and secrets, into every clone forever (security-hardening 3.1's argument for redactCommand applies with more force). CommandRunPayload today is {cmd} only, so exit status is genuinely missing from the record, and an optional exit/ok pair is the smallest addition that gives r1-fixes 2.5 its "test pass/fail" without inventing semantics — an exit code is a fact, "tests passed" is an interpretation the audit (S2) says exit codes cannot carry. CLAUDE.md's guard-rail puts schema ONLY in packages/schema/src, and the row shape is schema whatever its storage. r1-fixes ruled 2026-09-15 (its D-rule: never define outcome-capture payloads or storage in r1-fixes) that it consumes this contract, so there is exactly one owner.
- 2026-09-15T16:25:06.707Z — rule: **Private diagnostics live only under the XDG state dir keyed by clone hash, never under the repo or .sofar/.index; every exporter, push and pull path must be pinned by a sentinel test proving it moves none of them.** — chose **1.1(b) — the diagnostics STORAGE BOUNDARY, gating 1.2.
(1) Location: OUTSIDE the repo. Private diagnostics live under `~/.local/state/sofar/diagnostics/<sha256(clone-path)>/` (XDG_STATE_HOME-aware), keyed exactly like the sync cursors in sync-client D2's storage triad. Never under `.sofar/`, never under the clone root; the path resolver refuses a target inside the repo and a test pins that refusal.
(2) Form: an append-only JSONL journal of rows in the packages/schema/src row shape, one file per initiative slug, each row stamped with schema version, engine version, clone hash, initiative, session id, timestamp, kind, host tool and version when known. Rotated at a per-clone byte cap; rows older than 90 days deleted on the next write; a purge subcommand wipes the clone's store. Exact cap and names are settled in 1.2 against measured row sizes.
(3) Not truth, not derived. The store is a THIRD class next to events.jsonl (truth) and .sofar/.index (derived, disposable): raw observations that exist nowhere else and are NOT rebuildable, so it never goes in .index (record-index D1 forbids irrebuildable data there) and never claims disposability. Its loss costs evidence, never record correctness — nothing the fold, the projections or the digest's byte-stable block depends on may read it.
(4) The exemption does not reach it. record-hygiene D1 exempts git and sofar commands from the RECORD because the append would keep the tree from settling; the store is outside the tree, so exempt commands and read-only MCP calls ARE counted there (command token only, redacted). This recovers the bookkeeping-share denominator the audit (S1) called unmeasurable — with coverage limits 1.3 must state: hooks fire only on Edit|Write|MultiEdit|Bash, MCP calls are counted by the server itself, and Read/Grep/other tools are never observed.
(5) Best-effort per BD22, and never recursive: a failed row write never fails the user's tool call, never appends an event, and is never itself recorded as a diagnostic. MCP typed-error rejections write a row and still append nothing to the record (no-write-on-invalid-input is pinned by test).
(6) Enforced at the boundary by tests: with a seeded store carrying a sentinel under a fake XDG_STATE_HOME, exportEvents, exportNDJSON, `sofar export`, pushStream (mock fetch) and pullStream/importNDJSON must move zero bytes of it; a row must fail validateEnvelope; a row write in a temp repo must leave `git status` clean.
(7) Sanitized evidence: a proposal (2.3) may carry row ids, hashes and counts into the record as its evidence; when the rows have expired the consumer reports evidence unavailable, never reconstructs it.** over (a) `.sofar/.diagnostics/` with a self-ignoring `.gitignore` of `*`, the .index pattern, which is what the task text literally says ("gitignored"). (b) Appending diagnostics as events to events.jsonl with an export-time type filter. (c) A new tier inside `.sofar/.index/`. because Verified against main @ b29d013: exportEvents/pushStream read only `.sofar/initiatives/<slug>/events.jsonl` and filter by envelope validity and cursor, never by type — so (b) would need a filter in every present and future exporter, and one missed path leaks. (c) is ruled out by record-index D1: every .index file must be deletable and rebuildable, and raw observations are neither. (a) satisfies the wording but rests on one file staying present: `git add -f`, a deleted .gitignore, `npm pack` of a repo without the private flag, or a worktree copy each defeat it, and the self-improve D1 rule is absolute. A path outside the clone is uncommittable, unpackable and unexportable by construction, and the repo already keeps per-clone state there (sync cursors), so no new convention is introduced. The cost is real and accepted: deleting the repo strands the store, hence the 90-day retention and purge; and diagnostics are per machine, which is correct for evidence that describes this machine's sessions. Counting exempt commands there is safe because the exemption's only purpose (a settleable tree) is untouched.
- 2026-09-15T16:49:25.064Z — chose **1.2 implementation rulings within D2/D3: (1) `ok: true` is written EXPLICITLY on every successful command_run/file_touched, not just `ok: false` on failures — so absence means "an engine or host that predates capture", never "succeeded"; a reader can tell unknown from success. (2) The failed half of the record is a new PostToolUseFailure shim (matcher Edit|Write|MultiEdit|Bash → `sofar event post-tool-failure`), installed by `sofar init` as the sixth shim: Claude Code fires PostToolUse only on exit 0, so until now a failing `npm test` left no event at all. (3) The MCP server writes its own `mcp_call` row per call (success or typed rejection) since the PostToolUse matcher never sees mcp__sofar__* calls. (4) Retention mechanics: 90-day sweep at most once a day per clone (meta.json), 8 MiB per-file cap compacting to half, one stat per write. (5) Row `kind` set: tool_outcome, tool_failure, mcp_call, injection (SessionStart bytes + repo-memory bytes as the size half of a memory-use signal).** over Recording only `ok: false` (absence = success); a consumer inferring failure from output text; putting exit-code parsing of the host's prose `error` line into the event; a sweep on every write. because Verified against Claude Code hook docs (code.claude.com/docs/en/hooks, checked 2026-09-15): PostToolUse fires only on success, PostToolUseFailure carries structured `exit_code` and `stderr` for Bash. In this session's own transcript the Bash tool_response had NO exit_code (stdout, stderr, interrupted, isImage, noOutputExpected), so `exit` on a success event is host-dependent and 1.3 must mark it so. Explicit true costs ~10 bytes per event and buys the unknown/success distinction D2 (4) requires. A per-write sweep would read the whole file inside the 100ms shim budget; a daily sweep costs one stat.
- 2026-09-15T17:18:04.195Z — rule: **`sofar tune` never writes anything and never runs without `--dry-run`; a persisting or applying surface must be a separately named command, and a detector must never print a number for a signal the availability map calls unavailable on the clone.** — chose **`sofar tune` has exactly one mode and it must be spelled: `--dry-run` is REQUIRED, the command refuses without it and writes nothing (no event, no row, no file under .sofar or the store). Detectors are pure functions over (raw events, diagnostics rows, availability map) with no filesystem or clock, so the report is byte-deterministic for the same inputs and replayable against a log prefix; the JSON is version-stamped and carries the corpus (events per initiative, rows, cutoff id). A detector runs only for a signal the map does not call unavailable on this clone; everything else is UNKNOWN with the map's reason and the missing requirement. Evidence is event ids or `row:`+sha256[0:16] of the stored row line. No detector names a cause and none proposes anything.** over A default mode that detects AND persists suggestions (the audit's S5 warning about repeated tune calls re-emitting the same proposal); detectors reading the filesystem directly; a report stamped with the run time; printing 0 for a signal the clone has no shim for. because Audit S1/S3/S5: a count for an unobserved signal is confident nonsense, a re-derivation is not evidence, and a persistence mode hidden behind a default lets a shell history lie about what an invocation did. Purity buys replay (2.2 will replay detectors against held-out logs) and the determinism test buys the byte-identical assertion; the raw log rather than the fold is required because the fold deduplicates exactly the duplicate session starts one detector counts.
- 2026-09-15T18:31:52.049Z — chose **2.2 measurement protocol, pre-registered before any label exists. CORPUS (snapshot outside the repo at ~/IO/bench-cells/tune-eval-2.2, local git 0ae9e72, manifest sha256 2c4bf41d…, rubric sha256 44727e8d…): REGRESSION split = the 9 smoke/calib bench cells with records (visible when 2.1 was designed — rediscovery only, never counted toward trust); VALIDATION split = round-1 rep-1 claude-sofar, codex-sofar, cursor-sofar (S1–S10 complete, unseen); OPERATOR split = this repo's 56 initiatives, events strictly before 01M2K1JCAKZT77FM8HEFP3HB0Z (distinct project, real sessions). LABELS: blind subagent labelers who may not read tune.ts, signals.ts, their tests, SPEC or the self-improve record, nor run sofar; they label semantic failure instances (duplicate_registration, record_correction, stall, formatter_config, mcp_rejection, sofar_cli_failure) from transcripts plus the snapshot, loss/benign/ambiguous, evidence by event id; label files are hashed into the record BEFORE replay. REPLAY: detect() bundled from b219616, rows=[] (no capture-era rows exist in any unit), signals from each unit's own settings. SCORING: finding↔label by shared event id and mapped type; TP = matches a loss; FP = matches only benign or nothing; ambiguous-only excluded and counted; FN = loss with record events in a unit where the detector ran and no finding shares an id; loss with no record event = FN-untraceable, reported apart (two recalls); loss in a unit where the detector is UNKNOWN = unobserved, never FN. COVERAGE VIOLATION = a detector prints a number on a unit whose corpus cannot contain its source while labels show ≥1 loss of its type. GATE: TRUSTED for 2.3 iff on validation ∪ operator TP+FP ≥ 5, precision ≥ 0.80, Wilson 95% lower bound ≥ 0.50, and no coverage violation on any split; otherwise UNTRUSTED naming the failing clause (insufficient evidence is not failure).** over Scoring detectors by rediscovering the smoke preview rows (audit S3: regression, not validation); labeling with the detector's own rule or after seeing its output; committing bench-cell logs and labels into sofar's pushed git; a single pooled score across detectors and splits; a gate on recall. because Audit S3 asks for held-out logs from distinct sessions/projects, negative and unknown-coverage cases, frozen labels, per-detector counts. Labels must be semantic (a correction that was a change of mind is not a loss) or precision is tautological for event-type detectors, and they must come from transcripts or recall can only find what the detector's own rule finds (e.g. calib-1's biome.json edit left 0 file_touched events). Precision gates, not recall, because 2.3 turns findings into suggestions and a false alert costs trust; recall is reported so blind spots stay visible. The corpus stays out of sofar's git because round 1 is live and bench content is unpublished; hashes in the record make any later edit detectable. No corpus has capture-era rows or ok fields, so tool_failure / mcp_rejections / bookkeeping_share / injection_bytes cannot be validated here and stay untrusted by construction.
- 2026-09-15T19:01:51.408Z — rule: **2.3 may turn only a detector TRUSTED by the 2.2 protocol (today: corrections) into suggestions; every other detector's output stays report-only until a re-run of that protocol on a fresh held-out corpus trusts it.** — chose **2.2 VERDICTS under the pre-registered protocol (detect @ b219616, labels frozen at eval git 3abbf41, results 9fed5a9, results.json sha256 167bae8ac4ccf109; held-out = validation ∪ operator):
- corrections — TRUSTED: 28 TP / 1 FP, precision 0.97 [Wilson 0.83, 0.99]; recall 0.53 (25 of 53 record fixes left no correction event — re-logged decisions, re-sent plans); 26 of the 28 TP come from the operator split.
- duplicate_session_starts — UNTRUSTED, insufficient evidence (1 judged finding); recall 3/31. The dominant real pattern is one logical session under a SECOND minted id (start_session without session_id after write-back; a Cursor CLI start beside the hook's start), which an id-keyed count cannot see. The one held-out finding merges 3 real re-registrations with a benign cross-session id collision.
- formatter_friction — UNTRUSTED: 2 TP / 1 FP, and coverage violations on 5 units (it printed 0 where losses exist: Codex never fires the hooks, and calib-1 edited biome.json before any initiative existed).
- stalls — UNTRUSTED, insufficient evidence: no driven run in the held-out splits (regression-only: 1 finding covering both real stalls).
- tool_failure — correctly UNKNOWN on all 13 units; 27 labeled sofar CLI failures unobserved.
- mcp_rejections, bookkeeping_share, injection_bytes — printed a number on 13/13 units although no unit has a single diagnostics row, which violates D5.
Regression rediscovery: 3 of 4 preview rows found (smoke-1 duplicate starts, smoke-2 corrections, smoke-4 codex-drive stall); calib-1's biome edit was missed.** over Reporting the smoke rediscovery as validation; trusting duplicate_session_starts on its 1.00 precision point estimate (n=1); treating formatter_friction's zeros on Codex cells as "no friction". because The gate was fixed before labels existed and is applied as written. Precision gates what reaches a suggestion, but the recall numbers change what the detectors may be used FOR: r1-fixes 1.2 predicts "0 duplicate session_started in round-2 cursor cells", and a detector that sees 10% of re-registrations cannot measure that prediction. The zero-prints are the audit's S1 failure (a count for an unobserved signal) reproduced on real logs. The availability map degrades against the clone's settings and store path only, never against whether the CORPUS contains the signal's source.
- 2026-09-16T06:10:07.120Z — rule: **A suggestion carries the 2.2 trust numbers of the detector that produced it, names no cause and proposes no change; approval binds to the candidate hash and is refused once the evidence set moves.** — chose **2.3 contract, settled before code. (1) A SUGGESTION IS A LOSS ROW, never a fix: {candidate hash, signal, scope = one initiative, evidence ids, count, corpus cutoff, engine version, detector report version, trust}. It names no cause and proposes no change; the fix, its predicted gain, its falsifier and its budget belong to Phase 3. (2) TRUST TRAVELS WITH IT: a candidate can only come from a detector the 2.2 protocol marked TRUSTED, and it carries that verdict's numbers (precision, recall, judged n, protocol and verdict event ids) so a reader sees how often this signal is right without leaving the row. Today that is `corrections` alone. (3) THE HASH IS THE EVIDENCE: sha256 over canonical {version, signal, scope, sorted evidence ids} — not the cutoff, not prose, not a branch. New evidence means a new candidate; a rejected candidate stays suppressed until its evidence set changes, which is exactly when its hash does. (4) APPROVAL BINDS TO THE EXACT CANDIDATE: approve, reject and revert re-derive candidates from the current log and refuse when the hash is no longer derivable, naming the hash that replaced it. Revert is a new event that ends an approval; nothing is erased or rewritten. (5) SURFACES: `sofar suggest [slug|--all] --dry-run [--json]` derives and prints, writing nothing; persistence is always an explicit verb — `suggest record|approve|reject|revert <hash>`; `suggest list` shows every candidate with its full history. `sofar tune` stays detection-only. No MCP tool and no digest section: the operator asks for suggestions, they are never pushed into every session's context. (6) MINIMUM EVIDENCE 3 instances in scope — one correction is ordinary work, a cluster is a pattern — and at most 10 open candidates per initiative. (7) PROTECTED FLOOR: the set of things a suggestion may ever change is today EMPTY (a loss row changes nothing), and log integrity, session routing, standing constraints, evaluator integrity, permissions and release policy are never in it. (8) Offline replay (context size, information preservation) is DEFERRED with a rule, because no trusted detector yields a candidate that changes injected context: no candidate kind that changes what is injected may ship before that check exists.** over Candidates that name a cause or a fix (2.1's rule 3 forbids the detector; inheriting it in the proposal would launder the same guess); one candidate per finding (27 rows in this repo's own record); hashing the cutoff (every new event would churn every hash and defeat rejection suppression); a digest section or an MCP tool for suggestions (r1-fixes 2.1/2.4 are cutting injected bytes and tool schemas); building the replay check now against no candidate that needs it. because The audit's S5 asks for a complete lifecycle with immutable references, approval bound to the exact evaluated candidate, stale applies refused, suppression of rejected equivalents, minimum evidence and a protected floor. A loss row is what the trusted detector can honestly support: `corrections` proves that the record was fixed N times, not why, and 2.2 measured that it misses half of them — a number that belongs on the row the operator reads. Phase 3 consumes approved rows, which is what 3.4 ("first manual cycle on one round-1 loss row") needs from Phase 2.
- 2026-09-16T06:50:09.276Z — rule: **Every execution of candidate code — the agent, the candidate sofar, its build, the agent-written app's build and tests — runs under the evaluator's sandbox profile, and a result is written only by the evaluator process after the pin, the sandbox self-test and both controls hold; never score a candidate with any sofar binary or from any text the agent wrote.** — chose **3.1 contract — the FROZEN EVALUATOR, settled before code. (1) SEPARATE REPO AND PROCESS: the evaluator lives in ~/IO/sofar-evaluator (own local git, bun + TypeScript, zero deps), never in sofar (the candidate's repo) and never in handoff-bench (the live round-1 runner a peer session edits). Results land under ~/IO/bench-cells/evaluator/<run>/, written only by the evaluator process. No sofar binary — candidate or installed — ever interprets a result (audit S4: the candidate may be the supervising binary). (2) THE MEASUREMENT PATH IS PINNED, NOT TRUSTED: PIN.json names the handoff-bench commit plus sha256 of every file the evaluator uses (hidden-tests/** incl. vendor fixtures, CHAIN-A.md, runner/lib/{agents,arms,billing,cell,chain,mcp,paths,proc}.ts, runner package.json + bun.lock), the skeleton commit efc0137 and its tree hash, the chain-A reference commit 65a98ca, the sandbox profiles' hashes, and the tool versions (bun, node, claude, macOS, sandbox-exec present). Every run re-verifies all of it and REFUSES (exit 2, a refusal record, nothing else) on any hash mismatch, a missing or added file, a dirty working copy of a pinned path, or a tool version change — naming the first offender. (3) THE CANDIDATE RUNS INSIDE A KERNEL SANDBOX, AND SO DOES EVERY BYTE OF ITS CODE: macOS sandbox-exec (Seatbelt) profile `candidate.sb`: (allow default), (deny file-write*) with an allow-list {the cell directory, the bench agent profile ~/.bench/claude-config, ~/.local/state/sofar, /private/tmp, /private/var/folders, /dev}, (deny file-read*) on {hidden-tests, ~/IO/bench-sealed, the chain-A reference, the evaluator results, the evaluator repo}, and (deny process-exec /bin/launchctl). It applies to the agent (`claude` reached through a PATH shim that exec's sandbox-exec), to the candidate `sofar` (its bin entry IS such a shim), to `sofar init` during cell setup, and to the candidate's own build during `pack`. The agent-written app's `bun install`/`build` and the hidden-test run execute under a second profile `score.sb` (writes: the cell repo, tmp, the junit outfile only) because an agent-authored build script is candidate code too. (4) ISOLATION IS MEASURED AT EVERY RUN: before any launch the evaluator runs a self-test inside candidate.sb — write hidden-tests, write results, read harness.ts, read bench-sealed, exec launchctl must ALL fail with EPERM and a write into the cell must succeed — and refuses to launch otherwise. Verified 2026-09-16 on macOS 27.0: write allow-list, rm/mv/chmod, symlink escape, sealed read and nested profiles all enforce. (5) CANDIDATE IDENTITY IS THE ARTIFACT, NOT THE BRANCH: `pack` exports one git commit with `git archive`, builds and `npm pack`s sofar.sh inside the sandbox, and names the candidate `cand-<sha256(tarball)[0:12]>`; the result carries the commit, the tarball hash and the package version; the install lives under bench-cells/evaluator/candidates (write-denied to the candidate). (6) CONTROLS PROVE THE EVALUATOR CAN SEE PASS AND FAIL: per pin, the requested sessions' hidden tests are run against the chain-A reference (every test must pass) and the skeleton (each session must fail at least one test); a candidate score is refused until both controls hold for the current pin. (7) SCORES ARE READ FROM THE TRUSTED PROCESS, CROSS-CHECKED: the junit file is written to a random per-run name, parsed by the evaluator, and must agree with the `bun test` exit code and stdout summary; a filter that ran zero tests is `no_tests_ran`, never a pass; disagreement is `score_inconsistent` and refused. The agent's own text is never a score. (8) EVERY LAUNCH IS RECORDED BEFORE IT HAPPENS: attempts.jsonl gets a row before and after each agent launch with usage and reported cost, the seed 3.2's durable ledger sums; a billing/limit signal (bench-refresh D14 patterns) stops the run with exit 3. (9) THE HOLDOUT IS UNREACHABLE BY CONSTRUCTION: the evaluator re-execs ITSELF under `evaluator.sb`, which denies reading ~/IO/bench-sealed, so the development loop cannot touch chain B even by bug. (10) STATED LIMITS: everything runs as one uid, so the boundary covers the process tree the evaluator launches and nothing the operator starts outside it; sandbox-exec is deprecated by Apple though enforced today; the candidate can read most of the machine (only sealed paths are read-denied) and reach the network; a candidate could still plant persistence through IPC the profile does not name. The one handoff-bench change is `BENCH_SOFAR_PREFIX` as an env override of the pinned prefix in runner/lib/paths.ts, default unchanged, so the live round is untouched (bench-refresh D5).** over A checksum plus a final diff of hidden-tests/*.ts (the audit's S4 straw man); chmod a-w on the evaluator tree (same uid can chmod back); a second unix account (real isolation, but needs sudo and an interactive setup the operator has not granted — kept as a later hardening); putting the evaluator inside the sofar repo or its packages (the candidate's write surface); reusing the round-1 runner's run.ts/ledger.ts/modes.ts (dirty in a peer's tree, and its caps are 3.2's job); swapping the contents of ~/.bench/sofar-0.32.0 to point at a candidate (audit S6: never swap the installed sofar under a running benchmark); trusting the junit file alone (the app under test runs inside the same score sandbox and could write it). because Audit S4 asks for a frozen supervisor and evaluator outside the candidate's write/control surface with the whole measurement path pinned, known-good/known-bad controls, and tamper fixtures — and says to pick a mechanism that actually enforces the boundary and to state remaining limits honestly. sandbox-exec is the only enforcement available to this uid without sudo, and its behaviour was tested rather than assumed. Pinning by content hash rather than by commit alone catches uncommitted edits in the live runner tree; pinning only the files the evaluator uses lets it run while a peer's round-orchestration edits are dirty. Sandboxing every execution of candidate code, not just the agent, closes the `sofar init`, pack-time build and agent-authored build-script paths that a checksum never sees. The self-test at every run turns the isolation claim into a per-run measurement, in the same spirit as session-driver D8's settings read-back.
- 2026-09-16T07:05:33.586Z — rule: **Never apply a second, different Seatbelt profile inside a sandboxed process, and never put a symlinked path form in a profile — realpath every entry — and treat a `sandbox_apply` failure as "no isolation", never as a deny.** — chose **D9 §9 REVISED (the evaluator's own kernel sandbox is dropped) and two measured Seatbelt facts fixed into the evaluator. Measured 2026-09-16 on macOS 27.0 with /usr/bin/sandbox-exec: (a) a profile can be applied inside an existing sandbox ONLY when it is byte-identical to the one in force — outer `(allow default)` with any different inner profile, or any two different profiles in either order, fail with `sandbox_apply: Operation not permitted` (exit 71); identical text nests fine. (b) Profile paths match the RESOLVED vnode path only: an entry naming `/var/folders/…` never matches a write that reaches `/private/var/folders/…`, while an entry naming `/private/var/folders` matches writes spelled either way. Consequences built: the evaluator process is not re-exec'd under a holdout-denying profile (it would break every candidate, pack and score sandbox inside it, which is exactly how the first controls run recorded `build_failed` for both reference and skeleton); the holdout stays unreachable by the candidate through its kernel profile, and by the evaluator through test/holdout.test.ts, which pins that every mention of the sealed path in evaluator source is a deny-list entry, a probe target or the definition. Every path is realpath'd before it enters a profile. The self-test judges a probe from exit code AND what it left behind, and a `sandbox_apply` failure is reported as "could not be applied" for every probe rather than counted as a successful deny — the first self-test passed ten deny probes vacuously for that reason and was caught only by the one allow probe. The scorer throws (never records a score) when its sandbox could not be applied. The self-test fixture lives on a real path under the evaluator store, not under tmp, which every profile allows.** over Keeping the evaluator's own profile and applying candidate profiles identical to it (the evaluator must write results the candidate must not); enforcing the holdout ban with chmod (same uid); trusting a deny probe's non-zero exit without checking why it failed. because D9 §4 says isolation is measured, not assumed, and the measurement found the mechanism's limits before any launch: nested-apply refusal and symlink-form mismatch. Both are properties of the OS, not of this code, so they belong in the record for every later session that touches a profile. A self-test that can pass while the sandbox was never applied is worse than none, so the allow probe and the apply-failure classification are now load-bearing and unit-tested.
- 2026-09-16T07:13:07.177Z — chose **D9 §2 REVISED: the evaluator pins a COMMIT and runs from its own snapshot of it, never from the handoff-bench working tree. `pin write [--commit]` resolves the commit, `git archive`s exactly the pinned paths (hidden-tests, CHAIN-A.md, the eight runner library modules plus the runner's package.json and bun.lock) into ~/IO/bench-cells/evaluator/pinned/<commit>/, installs the hidden tests' locked deps there, and records the sha256 of every extracted file; verification re-hashes the snapshot (enumerated, so an added file shows), checks the commit still exists in handoff-bench, and no longer looks at working-tree dirtiness at all. The runner library is imported from the snapshot and the hidden tests run from it; the snapshot root joins the candidate's read-deny set alongside the live scenario dir. Pin hash for c628b55 is unchanged (3b02d408…), so the S1 controls recorded under it stand.** over Refusing on a dirty pinned path in the live tree (the first design, D9 §2): correct but it made the evaluator hostage to a peer session's uncommitted edits — the first launchd smoke run was refused because a parallel bench-refresh session had agents.ts and billing.ts modified in its worktree; re-pinning against dirty content (still not reproducible from any commit); asking the peer to commit (their timing, not the loop's). because "Frozen" has to mean frozen from a commit, not from whoever's working copy the evaluator happens to import. A snapshot is reproducible by anyone from the commit id, is hashed file by file, and is one more directory the kernel keeps the candidate out of. The live tree is only ever consulted to find the repo and the commit to pin.
- 2026-09-16T07:50:35.130Z — rule: **Every launch writes a ledger row before it starts and one after it ends; an attempt with no reported cost is charged at the next-session estimate, never zero; no launch proceeds past a per-run or rolling cap without a recorded pause.** — chose **3.2 contract — the DURABLE SPEND LEDGER, settled before code. (1) ONE append-only JSONL across every run: ~/IO/bench-cells/evaluator/ledger.jsonl, written only by the evaluator process (inside the candidate's write-denied store). Kinds: pack, controls, score (time only, no model), launch_intent (BEFORE a launch), launch_outcome (after: ok, timed out, exit, ms, turns, tokens, reported cost), launch_lost (an intent the process never closed, written on resume or reconcile), refusal, stop. The run-local results/attempts.jsonl stays as a filtered copy. (2) EVERY ATTEMPT COSTS SOMETHING, never zero: a launch is REPORTED (claude -p's total_cost_usd), UNKNOWN (an outcome with no usage) or OPEN (an intent with no outcome — the evaluator died, the machine slept, the operator killed it). Unknown and open attempts are charged at the next-session estimate (`--next-session-usd`, default $8, the same headroom figure the round-1 runner uses) in every cap check and reported apart from reported spend, with their counts. Controls, packs and scoring count as attempts with time but no model cost — the whole improvement cost (self-improve D1 addition C) includes them. (3) CAPS, checked before EVERY launch and stated on the run's stream: per-run `--max-usd` (default $20) on the run's cumulative spend across restarts; the bench-refresh D17 rolling caps — 7-day $450 and 5-hour $150 — over Claude spend computed the runner's way (claude-code arms, reported cost, ended inside the window) over the round-1 ledgers PLUS this ledger, so the evaluator's spend counts against the operator's half-limit; `--max-launches` (default sessions × reps, lost attempts included) bounds no-progress retries. A cap hit is a PAUSE (exit 4, a `stop` row with the reason, resumable), a billing/limit signal is a STOP (exit 3, D14), and neither is ever silent. (4) RESTART-SAFE: `run --run <id> --resume` re-verifies pin, controls and the sandbox self-test, skips every (rep, session) that already has a result, closes open intents with launch_lost, resets the cell repo to its last bench commit (git reset --hard + clean, as the round-1 runner does) before relaunching, and charges the lost attempt. (5) LIMITS STATED: the round-1 runner does not read this ledger, so ITS caps exclude evaluator spend while the evaluator's include both; costs are what the agent reports on a subscription (API-equivalent, not billed); only claude-code is metered here.** over Reusing the runner's per-round ledger.json (a mutable snapshot the runner owns; not append-only; not readable as a cross-run stream); a per-run ledger only (a restart or a second run loses the cross-run view D17 needs); counting an unattributed attempt as $0 (audit S6: include unattributed failures and unavailable-cost cases); a hard stop on a rolling cap (the runner pauses, and pacing is not a billing event — D17). because Audit S6 asks for a finite experiment budget across every launch, retry, resume and evaluator run, including unattributed failures and unavailable-cost cases, with enforceable bounds and stated limits; D1's addition C says improvement claims are net of the WHOLE cost, which is only computable from a ledger that saw every attempt. Charging unknown and open attempts at the estimate errs toward under-spending the operator's limit, the direction bench-refresh D17 chose. Aligning the rolling arithmetic with budget.ts means one number means the same thing in both tools.
- 2026-09-16T08:32:00.566Z — rule: **Every fix session of an improvement cycle — loop or baseline — launches through `evaluator fix` on an APPROVED loss row from a refless export of one commit; the baseline's budget is the loop's full cost, no rep is selected by hidden-test score, and a comparison with any unequal parameter yields no verdict.** — chose **3.3 contract — the EQUAL-BUDGET DIRECT-FIX BASELINE, settled before code. (1) ONE RUN MODE FOR BOTH ARMS: `evaluator fix` in ~/IO/sofar-evaluator is how any fix session in a cycle is launched — the baseline arm `direct-fix` and the loop's arm `loop-fix` alike. Same agent binary through the same sandbox shim, same model/effort/timeout, same D12 caps and ledger, same cell construction, same result capture. The arms differ in the PROMPT ONLY: direct-fix gets the loss row and the fix instruction; loop-fix gets exactly that plus the loop's brief (`--brief <file>`, sha256 recorded). run.json records every parameter and `evaluator compare` refuses a verdict when base commit, loss hash, model, effort, timeout, reps, pin, evaluator commit or the row text differ between the two arms. (2) THE LOSS IS THE APPROVED ROW, VERBATIM: `--loss <candidate-hash> --repo <sofar clone>`. The evaluator reads the scope initiative's events.jsonl itself (JSON, no sofar binary — D9 §1 is about results, this is input), requires a `suggestion_proposed` for the hash whose LAST transition is `suggestion_approved` (a rejected, reverted or merely recorded row is refused), and renders the row text from the proposed payload (signal, scope, count, 2.2 trust numbers) plus every cited evidence event (id, ts, type, payload text). Both arms receive byte-identical row text; its sha256 is in every result. (3) THE COMPARATOR IS HIDDEN BY CONSTRUCTION, AND THE FIX IS MADE FROM A COMMIT: the cell is a `git archive` of `--base <commit>` into a fresh `git init` repo with one commit — no refs, reflog or objects beyond that tree, so the human r1-fixes change is unreachable through git. The kernel profile denies reads of the source repo and every worktree of it (`git worktree list`) plus any `--hide <path>`, alongside the D9 sealed set; a read probe against each hidden path joins the self-test. After the session the transcript is scanned for `--hidden-term`s (default: `r1-fixes` and every hidden path); a hit VOIDS the attempt — recorded, not packed. Stated limit (D9 §10): the network is open and origin carries the branch, so the scan catches the name, not content fetched under another name. (4) SAME FEEDBACK, NAMED: what either arm may use during its session is the candidate's own suite inside the cell (`npm test`, `npm run typecheck`, the build) and nothing from the evaluator — no hidden tests, no scores, no controls; the kernel denies both arms the same sealed set. Deviations from this feedback set are a new Decision. (5) NO SELECTION WITH HIDDEN-ANSWER ACCESS: one session per rep; the candidate is the cell's HEAD after `git add -A && commit` at session end, packed by `pack` from the cell (candidate identity stays the artifact, D9 §5); EVERY rep is packed and scored; the arm score is the mean over reps; nobody — agent, evaluator, operator — picks a rep by its hidden-test score. (6) EQUAL SPEND MEANS THE LOOP'S FULL SPEND: the loop arm runs FIRST; its full fix-stage cost = its fix launches charged per D12 + its cost outside the evaluator (`tune`, `suggest`, replay, the operator's own sessions), which `compare` takes as `--loop-extra-usd <n>` — required, 0 allowed, never assumed. The baseline's `--max-usd` must be ≥ that full cost, its `--max-launches` = the same reps; compare checks the inequality and WITHHOLDS the verdict when it fails. Stated limit: caps bound launches, not what one `claude -p` session spends inside itself, so actual charged spend of both arms is printed beside the caps and an overspending baseline never helps the loop (a baseline that wins at any budget defeats the loop; a loop that spent past the baseline's cap gets no verdict). (7) VERDICT: KEEP only if the loop arm's mean hidden-test pass on the evaluator's workload is STRICTLY greater than the baseline's at a baseline budget ≥ the loop's full cost; a tie or a loss is DISCARD; a missing eval run, a voided rep or an unequal parameter is WITHHELD with the reason. compare also prints any `--also <candidate>` (the human r1-fixes candidate for 3.4) on the same table, and the full-cost delta in dollars. Results: runs/<run>/results/<arm>-r<n>-fix.json (loss hash, row sha, prompt sha, brief sha, base commit, cell HEAD, diffstat, agent outcome, spend, candidate id or void reason), ledger rows with arm `direct-fix`/`loop-fix`.** over A baseline run outside the evaluator (an operator session by hand — unequal launch path, unrecorded spend, no sandbox); handing the baseline the loss as prose the operator writes (not the same row, not verbatim); a cell cloned from the live repo with its refs (the comparator branch reachable by `git log --all`); a best-of-N pick among baseline reps by hidden-test score (test-set selection, the thing 4.1 exists to forbid); charging the baseline only the loop's fix-session cost (the loop's detector and replay machinery would be free, the audit's finding 7); a score-per-dollar exchange rate to fold cost into the verdict (an invented constant — equal budget puts cost on the input side instead); letting the arms iterate on evaluator scores mid-session (hidden-answer feedback). because D1 keeps a change only if it beats the equal-budget direct-fix baseline net of the full improvement cost, and the audit's finding 7 asks for a matched-budget SIMPLER baseline. "Matched" is only checkable when both arms run through one launch path with every parameter recorded and one ledger charging every attempt (D12), and "full cost" is only honest when the loop's out-of-evaluator spend is stated on the baseline's input side rather than estimated on the output side. Hiding the comparator by exporting a commit into a refless repo costs nothing and closes the git route; the kernel closes the filesystem route (D9 §3); the transcript scan is the measured limit of what remains. Reading the approved row from the record makes approval (2.3) the only door into a fix cycle for the loop and the baseline alike.
- 2026-09-16T08:48:14.293Z — chose **3.3 implementation rulings within D13, from building and dry-running it: (1) The transcript scan VOIDS a rep only on a hidden branch-tip sha (every `refs/heads` tip of the source repo other than the base commit) or an explicit `--hidden-term`; a hidden PATH in the transcript is a warning, never a void. (2) The comparator's RECORD is hidden separately: `--hide-record <slug,…>` removes `.sofar/initiatives/<slug>` from the cell before its one commit; nothing is hidden by default, the operator names it (for 3.4: `--hide-record r1-fixes`). (3) A fix that fails to build or pack is the arm's own failure and scores 0 in the mean; only voids, missing candidates without a build failure, missing eval runs, unequal parameters and an unstated loop cost withhold. (4) The fix cell keeps the repo's committed hooks and `.mcp.json`; they reach the runner's pinned sofar install, sandboxed by inheritance, so both arms work in the same developer environment; no candidate shim is written in fix mode.** over Voiding on the branch NAME (`r1-fixes` appears legitimately in the cell's own record — self-improve notes cite it — and in absolute paths under file_touched events, so a name scan voids honest reps); hiding records by default (the evaluator would carry a list of initiative names it has no business knowing); withholding on an unbuildable fix (it would let a broken arm escape a loss). because The dry run on the fixture clone showed the record inside the cell already names the comparator's initiative and the source repo's paths, so only content the agent could not have seen without reaching the hidden history — a tip sha — is evidence of a leak. D13 §7 says a voided rep withholds; a fix that does not build is not a leak, it is a result the evaluator can see (build failed), and D9 §6's whole point is that the evaluator can see fail.
- 2026-09-21T11:51:11.361Z — rule: **Never build a fix cell from a clone or checkout with later history; export the base into a one-commit repo and assert in the cell that the human fix commit is unreachable.** — chose **D13 AMENDED for 3.4 (operator ruling, 2026-09-21). (1) `evaluator fix --loss` also accepts a round-1 loss-study L-row from handoff-bench analysis/ROUND-1-INTERIM.md, class S only. The row the agent sees is the observable columns (id, class, loss, scope) verbatim; the "fixed in RC?" column is dropped because it can name the fix. The source file's sha256 is recorded, and the run records the loss as an L-row, not an approved suggestion. (2) The cell stays a `git archive` of the base into a fresh one-commit repo. Base = the human fix's parent. `--hide-record r1-fixes` holds; the fix commit's sha is a voiding term; a probe asserts that `git log --all` in the cell reaches only the base and that the fix commit is absent. (3) A fix cell also read-denies the evaluator candidate store and every install built from a commit that contains the fix (today ~/.bench/sofar-0.33.0-rc.2). The human candidate is packed only after both arms finish. (4) There is no wait for round 1: fix cells run the pinned ~/.bench/sofar-0.32.0, and eval cells run the candidate install, never the global sofar.** over Approving a corrections row in this repo and dropping the human r1-fixes comparator; waiting for round 1 to finish. because 3.4's point is loop vs direct-fix vs the human-built change on the same loss. The round-1 losses r1-fixes fixed exist only as loss-study L-rows, not suggestion rows. r1-fixes merged into main at 25cef58, so the human fix is reachable from this repo's history, and only a refless one-commit export plus hidden records, installs and store keeps it away from the agent.
- 2026-09-21T11:52:54.730Z — rule: **Every L-row fix cycle must name a loss-specific metric (a deterministic replay of the recorded loss where possible) as its primary score; hidden-test pass is only a non-regression guard for it.** — chose **D13 AMENDED again (operator ruling, 2026-09-21): an L-row cycle is scored on the LOSS'S OWN SIGNATURE, not on hidden tests. For 3.4 the row is L11, with base 48f63b2^ and human comparator 48f63b2. PRIMARY: replay round 1's recorded failing phase-update calls (claude-sofar/r3 S7 and S8 invalid_input; claude-sofar/r1 S9's full-replace plan update that wiped phases 6–8) against each candidate build (loop, direct, human). Each replay starts from the cell's record truncated to just before the call. It is deterministic and costs $0, with no model. SECONDARY, as a non-regression guard only: hidden tests on S1–S9 at haiku, 1 rep per candidate. The evaluator adopts the bench-refresh weekly-reset cap (--week-reset, bench-refresh 01M31Q1F) before any launch.** over Scoring an L-row cycle by hidden-test pass alone (D1's default metric), which cannot see a bookkeeping loss, so every verdict would be noise. because The hidden tests score the app the agent builds. A bookkeeping loss (a refused phase update, a wiped plan, a split session) barely moves that, and most of r1-fixes' L-rows are bookkeeping losses. A replay of the recorded failing calls says directly whether a build removes the loss. The hidden-test run stays only to catch a fix that breaks the agent's work.
- 2026-09-22T09:11:31.587Z — rule: **Never launch an evaluator agent or score under a profile that lets it signal processes outside its own sandbox; selfTest must prove an outside process survives a sandboxed pkill -9 -f first.** — chose **Evaluator sandbox profiles (candidate and score) deny every signal outside their own sandbox: (deny signal)(allow signal (target same-sandbox)), the round-2 runner's SIGNAL_PROFILE rules. Every selfTest proves, before any launch, that a sandboxed `pkill -9 -f` and `kill -9` cannot reach an outside process (judged by the process still being alive), and that the sandbox can still kill its own children and a nested same-profile sandbox's (the sofar shim). Evaluator 25f6889; 58 tests pass. Cause: at 09:03:29Z on 2026-09-22 an eval agent in eval-L11-cand-0e1ec9a9dec5 (job com.sofar.evaluator.cycle-L11) ran `pkill -9 -f node; pkill -9 -f next; pkill -9 -f bun`. That SIGKILLed the round-1 Codex runner mid codex-drive/r1 and every live sofar MCP server (bench-refresh note 01M345TR). The operator paused the evaluator until this held (relayed by sofar-c3).** over File-only Seatbelt rules (the D9 profile as built). They denied reads and writes but let any agent signal any same-uid process on the host, which is the L02 cross-cell loss round 1 had already recorded. because D9 put every execution of candidate code under the evaluator's sandbox, but the profile covered files and launchctl, not signals. BSD pkill spares only the caller's ancestors, so one agent's cleanup reached every node and bun process on the machine. Signal isolation is what makes running the evaluator beside other cells safe. The probe is judged by liveness because pkill's exit code does not say whether the kill landed.
- 2026-09-22T14:54:57.993Z — chose **3.4 RE-RUN, operator ruling 2026-09-22: re-run all three arms from scratch at ONE fixed evaluator commit rather than resume. The arms are loop-fix and direct-fix (each a fix session, then a replay and a guard eval of its candidate) and the human guard (replay and guard eval of cand-0e1ec9a9dec5, which is 48f63b2 as packed on 2026-09-22 and is reused because pack is keyed by commit). New run ids use L11b. The first cycle's runs stay on disk as provenance and are never compared. (1) Fix the commitSession call first. The evaluator passes the cell's paths object to the pinned runner and checks after every session that the cell's HEAD reads "bench: after Sn" and that the evaluator's HEAD has not moved. (2) D13's evaluator.commit check is NOT relaxed, it is extended: compare also withholds when the guard eval or the replay it scores was run at an evaluator commit other than the fix runs'. That covers the loop, the baseline and every --also comparator. (3) The launch parameters are the same as the first cycle's: haiku, high effort, 20 min, 1 rep, fix --max-usd 12, loop-extra $8 and guard-tests 3. The only exception is the guard evals, which get --max-launches 10 so each can retry one lost session. The operator sees the total cost and the caps before launch.** over (a) Resuming the paused human guard (--max-launches 10) and the direct arm (--max-usd 20) and comparing against the existing loop arm. The loop arm's fix run is at an older evaluator commit and its guard ran without per-session cell commits, so compare withholds correctly, and forcing a verdict would mean relaxing D13. (b) Relaxing D13's evaluator.commit equality to "same evaluator code modulo empty commits". (c) Rewriting sofar-evaluator history to drop the 20 empty 'bench: after Sn' commits. They are empty, and old results cite their shas. because A verdict is only meaningful when every arm ran through the same evaluator code. The first cycle's arms span 25f6889 and 883961e, and every eval cell ran without the runner's end-of-session commits, so a resumed session reset to the agent's own commit and lost work (human guard S6). Re-running costs about $16 reported at haiku and about 2.5 h. Round 1 has ended and round 2 has not started, so no runner competes for ports. Scoring guard and replay runs from a different commit than the fix runs would reproduce exactly the inequality D13 exists to catch, one level down.
- 2026-09-22T16:54:50.763Z — chose **Evaluator port isolation per cell, at the user's direction relayed by sofar-c3 on 2026-09-22. (1) Each evaluator cell, eval or fix, gets its own 10-port block, PORT = 10000 + (sha256("<run>/<arm>/r<rep>") mod 1000) × 10. It is exported to the agent the way round 2 exports its block (runner/lib/sandbox.ts cellPort), and the range 10000–19990 is disjoint from round 2's 20000–59990. (2) The candidate profile kernel-denies port 3000, the skeleton's hard-coded `next dev --port 3000`, for both binding and connecting: (deny network-bind (local ip "*:3000")) and (deny network-outbound (remote ip "localhost:3000")). An evaluator cell can then neither take a bench cell's default dev port nor talk to a bench cell's app there. Port-0 binds, other local ports and the internet stay open. (3) selfTest proves this before every launch: binding the cell port works, binding or connecting to :3000 is denied, and binding port 0 works. (4) The cycle wrapper drops its :3000 wait. It keeps the round-2 marker gate, because the user ruled that eval cells never run beside round 2's own runs; they share the $450 D17 week cap.** over (a) Only exporting PORT, as round 2 does. The app's dev script ignores PORT, so an agent's `bun run dev` would still bind 3000 beside a bench cell. (b) An allowlist of the cell's 10 ports for all binds. It works, but it also denies port-0 binds, and `*:0` is invalid SBPL, so an agent's own tests that listen on a free port would fail. (c) Rewriting the cell's dev script to use $PORT, which changes the measured workload. (d) Denying round 2's whole block range, which SBPL cannot express as a range. because A tested Seatbelt profile (allow default plus the two :3000 denies) gives EPERM on bind and connect at :3000 over 127.0.0.1, ::, 0.0.0.0, ::1 and localhost, while port-0 binds, :3100 and 1.1.1.1:443 still work. The one collision the skeleton makes certain, two cells both on the hard-coded 3000, becomes impossible from the evaluator's side. An agent that picks another port explicitly is still advisory-only, as it is in round 2; that is the stated limit. The same profile applies to all three arms, so the comparison stays equal.
- 2026-09-22T19:44:58.382Z — rule: **Never treat the evaluator's caps as covering spend outside a cells root — work that runs outside one carries its own stated accounting, and an unreported cost is recorded in the units it was observed in, never as an invented total.** — chose **The caps enforce only what they can SEE, and that is a standing property of the D12/D17 machinery, not a one-off (sofar-c3, 2026-09-22). Two proofs from tonight. (1) BLIND BY PATH: roundClaudeSpend read only ~IO/bench-cells/boopada, so round 2's /Users/Shared smoke ledgers were invisible until c562789. The since-reset figure moved $124.69 → $160.61 the moment they became visible; that jump was accounting catching up, never new spending. (2) BLIND BY LOCATION: drive-visibility 5.1 ran in a session scratchpad outside every cells root, so no evaluator cap can ever see it. It is real usage in the same 5-hour window and week. Therefore: any future work that spends outside a cells root states its own accounting in the record, and a cost the adapter never reported is written in the units actually observed ("6 short haiku sessions"), with the estimate labelled as scale and never folded into a total. The one place the true figure exists is the provider's usage view, which no session here can read; asking the operator is the only way to obtain it. A follow-up is queued, not done: put this property in a comment at roundClaudeSpend in the evaluator's lib/ledger.ts. It cannot be done while cycle-L11b runs — D18 makes a moved evaluator HEAD throw mid-run and a dirty tree refuse the next launch — so it waits for the cycle to end.** over (a) Inventing a dollar figure for 5.1 from comparable haiku sessions and folding it into the budget picture. A made-up number cannot be told from a measured one later and cannot be removed. (b) Leaving the property in a passing note, where the next session reading the cap code would not meet it. (c) Widening the caps to scan the whole filesystem for ledgers, which would make the arithmetic unpredictable and still miss work that keeps no ledger, such as 5.1. because A cap that silently omits a spend source reads as authoritative while being wrong, which is worse than one whose blind spots are named. Naming both blind spots — by path and by location — tells a future session which question to ask before trusting a number, and keeps the honest gap visible until the operator can close it from the usage view.
- 2026-09-22T22:45:35.704Z — rule: **Never compare guard numbers across an evaluator pin move — cycles on either side are different regimes — and state the pinned handoff-bench commit in every cycle's evidence.** — chose **THE PIN IS A REGIME BOUNDARY, written down while the pin is stable (sofar-c3, 2026-09-23). Three clauses. (1) Pinning eight runner modules and the hidden tests is NOT a defect to be fixed: it is what makes cycle N comparable to cycle N+1, and reproducibility across cycles is the whole point of a frozen evaluator. Today's regime is handoff-bench c628b557fd8f43ba6ec73f6ceffb04cb2588400c, pin hash 3b02d4088a6b. (2) Its cost is the ten round-1 loss rows the evaluator inherits live (note 01M35MJ0B7), and the response is NOT to move the pin: it is to build the orchestration equivalents in the evaluator's own code, which is not pinned — tasks 3.5 and 3.6. (3) WHEN THE PIN EVENTUALLY MOVES, every cycle before it belongs to a different regime: guard numbers must not be compared across that boundary, and each cycle's evidence names the pin it ran under so the boundary is visible after the fact rather than reconstructed. The same holds for the pinned agent binary, which drifts under the evaluator (the live Claude Code install reached 2.1.280 tonight while the cycle ran the frozen 2.1.273).** over (a) Treating the inherited rows as a reason to re-pin to round 2's runner, which would buy the fixes and silently destroy comparability with every cycle already run. (b) Leaving the boundary as an understanding held by whoever was present, to be reconstructed later from commit dates. (c) Recording it only in task 3.6, where it would be read once and closed with the task. because The sentence is worth nothing today and a great deal the first time someone compares a pre-move cycle with a post-move one and finds a difference that is entirely harness. A pin move is invisible in the numbers it changes, so the boundary has to be written before it moves, by whoever knows the pin is stable — after the move nobody remembers to.
- 2026-09-23T15:48:54.075Z — rule: **A session the host slept through or that was stopped from outside is never scored; the evaluator records each session's regime and never gates on the prompt hash.** — chose **3.6 applies round 2's rules in the evaluator's own code, and the pin stays where it is. L22 (the host slept) or L24 (the agent was stopped from outside) during an agent session makes it a HARNESS FAILURE: the session's work is discarded, a harness-*.json is written, the run stops with exit 5, and --resume re-runs the session. A score the host slept through is taken once more; if the host sleeps through the second pass too, the result carries score_invalid and compare skips it. The regime (Claude Code version, normalised prompt hash, auto_mode steer flags, foreign instruction files) is RECORDED in every run and fix result and never enforced. L25 seals sibling runs with a require-not rule, which also covers runs created later. L21 uses a per-cell ZDOTDIR that puts the pinned runner's SOFAR_BIN_DIR and the claude shim first.** over (a) Scoring a slept-through or externally stopped session as an agent failure, which is what round 1 did and what L22/L24 record as a loss. (b) Enforcing the prompt hash or the steer. The hash moves within one version for reasons outside the experimenter's control (note 01M35N82X0), so gating on it turns the gate into noise; round 2 enforces the steer only against a per-arm reference, which the evaluator has no source for yet. (c) Listing the existing sibling runs in the profile, which misses a run started after the profile was written. (d) Importing round 2's shellpin/hostpower/validity from handoff-bench, which would move the pin. because Each rule is round 2's measured fix, carried over as the evaluator's own code, so the guard column no longer inherits those round-1 losses while the pin (handoff-bench c628b55, the eight modules) and its comparability stay intact. A stop-and-resume costs one re-launch, while a mis-scored session silently corrupts a verdict.
- 2026-09-23T15:52:48.073Z — rule: **A chain registered as held out in HELDOUT.json never reaches a fix: its roots are kernel-denied and refused as inputs, its id is refused for tuning, and a fix naming it is refused until the chain is retired by a committed registry edit.** — chose **4.1 is enforced by the evaluator through a committed registry, sofar-evaluator HELDOUT.json, which names held-out chains by IDENTITY only: chain id, root paths, commit shas. Chain L is registered with ~/IO/bench-sealed, ~/IO/tallybox-ledger and ~/IO/tallybox-reference-l, and no shas yet. Enforcement: (1) every root joins the candidate profile's unreadable set and gets a self-test deny probe; (2) a root is refused as an evaluator input (--repo, --brief, --loss-study, a candidate's repo); (3) a held-out chain id is refused where a tuning chain is chosen; (4) a loss row or brief that names a held-out chain ("chain L"/"CHAIN-L"), root or sha is refused; (5) a held-out sha in a fix transcript voids the fix, while names and paths only warn. Retiring a chain is a committed edit that moves it from `heldout` to `retired` with a reason. A retired chain may be used for tuning and is refused as a release proof (releaseChainRefusal, for 4.2). Every run.json records the registry's sha256.** over (a) Separation by convention, trusting fix-builders not to look: the operator asked for an exclusion the evaluator enforces. (b) Voiding a fix transcript on the chain's name or path: sofar's own record names chain L legitimately (memory-lead 5.2, chain-l-author), so a fix agent reading its record would void every fix. (c) Automatic retirement when a held-out mention is seen: retirement changes what counts as a release proof, so it is a reviewable commit, never a side effect. (d) Reading the chain to derive its shas: the fix-builder stays blind; the author or operator adds them. because A held-out chain's value is that no fix saw it. Only the evaluator is present at every point where tuning could touch the release chain: the kernel profile, the inputs, the chain choice and the transcript. So the rule is enforced there, and a malformed registry refuses rather than permits.
- 2026-09-23T15:55:33.538Z — rule: **A release passes the self-improve gate only when, on the held-out chain with ≥ 3 reps per arm, no claim's lead margin over the best current competitor shrinks and the candidate beats the equal-budget direct-fix baseline on some claim while trailing it on none.** — chose **4.2's release gate is `evaluator gate --evidence <scores.json>`, a pure decision over per-claim, per-rep held-out scores, so the evaluator never reads the chain. It REFUSES evidence with fewer than 3 reps on any arm and claim, from a chain that is not held out or was retired, whose direct-fix baseline spent less than the whole improvement cost, or whose competitors lack a version and a pinned_at. It PASSES only when (a) on every claim, the candidate's lead over the best current competitor (per claim and direction) is at least the previous release's lead, and (b) the candidate is at or above the equal-budget direct-fix baseline on every claim and strictly above it on at least one, since a tie loses. Means are compared strictly; each mean is reported with its min–max. Exit codes: 0 pass, 6 fail. The verdict is written to runs/gates/<ts>.json with the evidence sha256, the registry identity and the evaluator commit.** over (a) An aggregate score across claims: D19 says per claim, and averaging lets one claim's gain hide another's lost margin. (b) A noise tolerance on margins: any tolerance is a way to ship a shrink, and choosing one needs its own measurement and Decision. (c) Having the evaluator run or read the held-out chain for its scores, which D9 §9 and 4.1 (D23) forbid. (d) Measuring "net of cost" as dollars per point: D1 defines it as beating a direct-fix baseline given at least the same budget, which is what the gate checks. because The gate is where D1 and bench-refresh D19 become mechanical. It has to refuse evidence too thin to judge rather than pass it, and it has to stay blind to the chain it gates on.
