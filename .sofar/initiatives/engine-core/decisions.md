<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: engine-core

- 2026-09-14T17:00:30.387Z — chose **engine-core starts now, in parallel with sofar-cloud desktop-v2 phase 1 (sign-in), so the Swift app folds through UniFFI from its first record window** over Running the port behind the apps, with the Swift app on a sofar-CLI bridge until the core lands because Founder (Sep 14): the app is greenfield and iPhone is imminent, so there is no reason to build on a bridge first. The bridge stays as a fallback behind desktop-v2 RecordSource protocol if parity is not green in time; the parity harness (Phase 1) is the gate either way. desktop-v2 D2 records the same call from the app side and rejects GPUI.
