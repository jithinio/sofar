<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: agents-parity

Goal: Make sofar fully compatible with Codex, the way Phase 6 of r1-fixes did for Cursor: capture Codex's hook, MCP, AGENTS.md and exec contracts from the installed binaries and docs, then native hooks, MCP registration, the write-back gate, protocol text and a revised drive adapter, proven live with the operator's consent. Carries r1-fixes tasks 7.2–7.7 (r1-fixes D35). Built on branch agents-parity and merged into r1-fixes only after tag v0.33.0-rc.2 (bench-refresh 01M2Q79R).

Progress: 6/9 tasks done (66%)

## Phase 1 — Codex contract [done] — 1/1 done

- [x] 1.1 (r1-fixes 7.2) Codex contract capture, the 6.1 of Codex. Hook config (.codex/hooks.json and config.toml [hooks]), event names, stdin fields and honoured outputs, the review/trust gate, and the minimum Codex version with hooks. Project MCP registration (.codex/config.toml [mcp_servers]). AGENTS.md loading. Changes to `codex exec --json` since the 0.136.0 adapter (src/driver/codex.ts). Read from the installed binaries without inference (D3): ~/.local/bin/codex 0.136.0 and ~/.bench/codex-0.154.0/node_modules/.bin/codex 0.154.0 (--help, `codex help <cmd>`, bundle strings); docs saved at /Users/jins/IO/handoff-bench/scenario5-launch-bench/analysis/memory-gap-2026-09-16/research/codex-hooks.html and codex-hooks2.html. Record them as a note, SPEC text (a §Codex host section beside §Cursor host) and payload fixtures the tests read. Mark every claim read-from-binary, read-from-docs or unverified. Enabler for 2.1–3.1: later tasks test against these fixtures, never guessed shapes.

## Phase 2 — Codex integration [done] — 3/3 done

- [x] 2.1 (r1-fixes 7.3) Codex hooks: init writes Codex's hook config pointing at the shims, cli/host.ts adapts Codex payloads and outputs, and sessions are recorded as tool codex. Decide where Codex's shims live and log it (r1-fixes D36 gave Codex Claude Code's home until this task). Codex takes its hook files in the 7.1 agent map (agents.ts AGENT_FILES, wiredAgents, doctor, uninit). Note the hook contract change for sofar-rust-core (memory-lead 1.4 mirrors hooks). PREDICT: Codex sessions that open by running `sofar status` themselves 10/10 → ≤1/10; hook-captured file_touched and command_run in Codex cells 0 → every edit and command.
- [x] 2.2 (r1-fixes 7.4) Codex MCP: init registers the sofar server where Codex reads project MCP config, or states the one user-level step when it cannot; uninit and doctor mirror it, and the agent map counts it. PREDICT: sofar MCP calls in Codex sofar cells 0 → ≥1 per session; invalid `--payload` JSON errors → 0.
- [x] 2.3 (r1-fixes 7.5) Codex write-back gate and protocol text: the Stop hook holds a Codex session that owes a write-back, loop-capped; the AGENTS.md block tells an agent that has sofar hooks and MCP tools to follow the MCP loop, consistent with r1-fixes 6.7's reconciliation of the CLAUDE.md and AGENTS.md blocks. New block text moves the old one into the shipped ledger (init.ts). PREDICT: Codex sessions ending with record debt and no session_ended → 0.

## Phase 3 — Codex drive and proof [active] — 2/5 done

- [x] 3.1 (r1-fixes 7.6) Codex drive adapter: revise the session-driver D9 capability declarations for what hooks now give a driven Codex session (session identity from the hook, nudge channel), keeping every inert option stated; prove it with the fake-transport tests, not live runs (D3). PREDICT: codex-drive stalls from unknown session identity → 0.
- [ ] 3.2 (r1-fixes 7.7) Proof: a live Codex end-to-end on a scratch repo (init with the picker → oriented session → MCP writes → write-back gate → drive run), plus SPEC updates (§Host tiers moves Codex out of Tier 3, §Hooks, §CLI, §Driver) and acceptance criteria. NEEDS THE OPERATOR'S CONSENT for the Codex usage it spends (D3): a driven session writes the SPEC and acceptance text it can, then marks this task blocked with the exact live steps as the ask, and never runs live Codex inference itself. (blocked)
- [x] 3.3 Codex write-backs attribute to the Codex session outside a driven run. Live 3.2 showed an interactive session (held by the Stop gate) and an exec session write back with `sofar event append … --source codex` and no --session, so session_started/session_ended landed under session `cli` and the hook-registered session never got its write-back. A driven session was fine, because the driver's prompt carries the id. Make an unprompted Codex session's CLI append adopt its live session (the r1-fixes L09 pointer, which it evidently does not reach for Codex), and make the Codex-facing text name the id: the digest's Session line (it says 'adopted on Claude Code'), the Stop hold message, and the AGENTS.md block. Test with the live fixtures (D12). PREDICT: in a re-run of S1 step 3 and S2, session_ended carries the Codex thread id, and Codex write-backs under `cli` = 0.
- [ ] 3.4 sofar MCP tools under `codex exec`. Live 3.2 S2: sofar_start_session was refused with 'MCP tool call requires approval, but approval policy is never', so every exec and driven Codex session falls back to the CLI. Read from the 0.154.0 binary and docs whether a project MCP server's tools can be pre-approved (a per-server or per-tool approval key in config.toml), with no inference (D3). If one exists, init writes it and uninit/doctor mirror it; if not, SPEC §Codex host, the AGENTS.md block and the codex adapter's D9 declaration state the limit. PREDICT: sofar MCP calls succeed in an exec session, or the limit is stated wherever a Codex agent or operator reads it.
- [ ] 3.5 A session whose first recorded event is a CLI write-back never gets a session_started (evidence: live thread 01a0d6ae-03f7-7a13-b935-5031047312c2)

Active phase: Phase 3 — Codex drive and proof
Next action: 3.4: find in the 0.154.0 binary and docs whether a project MCP server can be pre-approved under approval_policy=never (no inference).
Blocked on: task 3.2: Live proof run (note LIVE CODEX PROOF): S1 steps 2 and 4, S2 identity and S3 pass. S1 step 3 write-back and S2 MCP fail, so 3.3 and 3.4 are filed. Re-prove S1 step 3 and S2 after they land; that needs the operator's consent again (D3), and the Codex account is at its usage limit until 2026-09-21 22:25 (gpt-reserve works). Then rewrite SPEC §Codex host and §Driver unverified marks and the §Host tiers sentence per the note's SETTLED list.
