<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: agents-parity

- 2026-09-17T13:48:05.855Z — rule: **Build Codex parity only on branch agents-parity; never commit it to r1-fixes before tag v0.33.0-rc.2.** — chose **Codex parity (r1-fixes 7.2–7.7) as its own initiative, agents-parity, bound to branch agents-parity in worktree ~/IO/sofar-agents-parity, branched from r1-fixes fea975e. r1-fixes changes are merged in as rc.2 work lands; the branch merges back into r1-fixes only after tag v0.33.0-rc.2.** over (a) building Phase 7 on r1-fixes, which the rc.2 cut rule forbids until the tag; (b) driving the r1-fixes record from this branch, where `sofar drive` would start on r1-fixes' own rc.2 tasks (it takes the active phase first) and a second copy of that record would take plan replaces from two writers. because The operator said go with Codex under sofar drive (2026-09-17). The run owner's rc.2 cut rule (bench-refresh 01M2Q79R, r1-fixes note 01M2Q7AX) puts Codex Phase 7 on agents-parity until the tag. The driver has no task filter, so a record holding only these tasks is the only way to scope the run.
- 2026-09-17T13:48:05.919Z — rule: **sofar must be fully compatible with Claude Code, Cursor and Codex: ship every host integration (init, hooks, MCP, protocol blocks, drive) for each, and install only the agents the operator selects.** — chose **Carry r1-fixes D35 as a standing rule here: Codex gets every host integration Claude Code and Cursor have, installed only when the operator picks it. Codex takes over its own files in the 7.1 agent map as they land: cli/agents.ts AGENT_FILES, init's wiredAgents, and doctor (r1-fixes D36).** over Leaving the rule only in r1-fixes, where a driven session on this record would never see it in its digest. because Driven sessions orient from this record's digest alone. D35 is the operator's ruling that defines done for every task here, and D36 says Codex owns only AGENTS.md until 7.3/7.4.
- 2026-09-17T13:48:05.984Z — rule: **Never run Codex inference (codex exec, an interactive codex, cloud tasks) in a session on this record; the live proof waits for the operator's consent.** — chose **Capture Codex's contracts without spending inference: read `--help` and `help` output, the installed bundles and their strings, and the saved docs. No session runs `codex exec`, an interactive codex, or any other Codex model call. The live end-to-end (task 3.2) is marked blocked with the ask until the operator consents.** over A live capture of Codex hook payloads inside a driven session, which spends the operator's Codex usage unattended. because The r1-fixes 6.1 precedent: the zero-cost sandboxed capture was not permitted, and 6.9's live run waited for the operator. A driven session cannot ask for consent, so the blocked status is how it asks.
