<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: push-ping-reach

Goal: Field fixes from splen 2026-09-14, where a push carried two peers' commits and nobody was told (stale-session-signals note 01M2GFKGJ0BB9CJWFE73F69ZDJ). (1) The push ping names live sessions that already wrote back, not just open ones. (2) doctor names a core.hooksPath that does not exist, because git then runs no hooks at all.

Progress: 3/3 tasks done (100%)

## Phase 1 — Reach and diagnosis [done] — 3/3 done

- [x] 1.1 The push ping (othersLanded) addresses every LIVE session a carried record knows, open or written back, still gated on the registry resolving it. Tier 0 exposes the finished sessions it already remembers. Tests: a written-back live peer is named; a written-back dead one stays silent.
- [x] 1.2 doctor: when core.hooksPath is configured but the directory does not exist, name that path and say git runs no hooks, with the unset or repoint fix. Test pins it.
- [x] 1.3 SPEC §Hooks and §Acceptance criteria updated for both; typecheck and the full suite green.

Next action: User runs npm publish -w sofar.sh; then verify 0.31.1 on npm.
