<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Decisions: branch-visibility

- 2026-09-21T10:22:20.145Z — rule: **Never write to, or rewrite, another checkout's copy of a record; reading across copies is read-side only and adds no event type.** — chose **`sofar list` and `sofar status` fold the UNION of every copy of a log by default. The copies are this checkout's file, every other worktree's working file (uncommitted appends included, read as files with no subprocess), and every local branch NOT merged into HEAD and not checked out anywhere, read at its tip through one `git cat-file --batch`. Duplicate ids are dropped and this checkout's lines stay first, so their line numbers and warnings are unchanged. Output names where the extra events live and what this checkout alone holds. It renders nothing extra, byte-identical to before, when no other copy adds an event. `--here` restores the single-copy view. Remote-tracking refs are opt-in with `--remotes` (operator said "do it" without ruling on remotes, so the conservative default stands).** over (a) Remote-tracking refs on by default. (b) Reading every branch, merged ones included. (c) Moving the record off-branch, or writing events through to main's copy (rejected in note 01M31PRW). (d) Leaving the default local and adding the union as an opt-in flag. because Measured 2026-09-21: memory-lead's copies hold 11, 143, 132 and 149 of 158 events, so any single copy misreports. The fold is ulid-ordered and convergent (SPEC §Cursor primitive (sync-ready contract)), so the union is exactly what merging every branch would give. (b) costs nothing in correctness to skip: logs are append-only and merge=union, so a branch merged into HEAD has its whole committed log already in HEAD's. That is 4 unmerged refs here, not 13. (a) pulls in abandoned remote branches, and a remote branch that matters usually also exists locally. (d) keeps the misreport as the default, which is the reported bug.
- 2026-09-21T11:39:59.948Z — chose **3.1: `sofar next` and get_state view:"initiatives" read the same union as `sofar list`, through one helper, listAcrossCopies. `next` takes --here and --remotes. The MCP view folds worktrees and unmerged local branches, never remotes, and has no single-copy switch. A budgeted line that carries an across-branches part gets that part's length on top of the 220-char clip, capped at 100 more (PROVENANCE_LINE_BUDGET). Worst case is 20 × 320 chars.** over (a) Keeping the MCP view single-copy, the one surface a session reads. (b) Union on the MCP view within the flat 220 budget. (c) Adding here/remotes args to get_state, which would change the schema. because Measured on this repo (5 worktrees, 62 initiatives): the union adds about 80 ms to a CLI listing (0.18 to 0.26 s) and about 130 ms in-process. The view is called on demand, never at session start (speed-2 T5a), so that is within budget. (a) would leave agents reading the stale view D1 exists to fix. (b) puts the across-branches part (about 65 to 100 chars) before `next:`, so the clip removes the next action from exactly the entries whose work is split across branches. (c) is not needed: `sofar list --here` already gives the single-copy view, and remotes stay off by D1's default.
- 2026-09-21T16:38:50.753Z — chose **3.2: `status --watch` folds the union and rescans on change. Its targets come from copyWatch: the common git dir plus each other checkout's .sofar/initiatives, filtered to this slug's log, the HEADs, packed-refs, refs/heads (refs/remotes with --remotes) and worktree entries. Bursts are debounced to one rescan per 150 ms. The pulse no longer re-folds: it re-renders the cached view, backed by one stat of the local log.** over (a) Rescanning on every 600 ms pulse, which spawns git up to 100 times a minute. (b) Per-pulse fingerprinting of refs, which still needs a subprocess, or re-implementing packed-refs parsing. (c) Watching the whole common git dir unfiltered, which walks objects and fires on every index refresh. (d) Keeping the per-pulse local re-fold, now a union fold of up to about 1k extra events every 600 ms. because A copy set changes only when a log, a HEAD, a ref or the worktree list changes, and each of those is a file write git makes under paths we can name. chokidar 5 silently drops a watched path that does not exist yet (probed 2026-09-21), so the targets are existing parent dirs with an `ignored` filter. A missed watcher event on this checkout's log is covered by the stat backstop, which the old per-pulse fold used to cover by accident.
- 2026-09-21T16:44:41.126Z — chose **3.3: the SessionStart hint covers WORKTREES only and reads files only. worktreeLeads stats each other checkout's copy of the bound record. A copy no longer than this log whose last 4,096 bytes equal this log's at the same offset is an older prefix and is skipped unread. Only a diverged copy is read in full, against this log's ids, read once. The notice sits in the volatile tail after recent-work-elsewhere, names two worktrees at most, and is capped at 360 chars. The quick lane gets none.** over (a) Covering branch tips too, which needs git for-each-ref and cat-file, a subprocess on the hot path. (b) Reading every copy in full: 10 ms for r1-fixes' four 1.7 MB copies, most of which are prefixes. (c) A stat-only line (mtime newer means "changed on X"), which cannot give N and misses a copy that appended before this one did. (d) A persisted cache keyed by size and mtime, which misses on nearly every session because the hook's own appends move this log. because Measured on this repo: 0.2 to 0.3 ms when every copy is a prefix (r1-fixes), 3.3 ms when one diverged 1.8 MB copy is read (rust-core, +1074 events), and shim-latency passes 3/3. A diverged copy cannot match a whole 4 KB window at the same offset, because every line carries a ulid and a timestamp, so the probe's only failure mode is one missing hint. Branches with no checkout stay with `sofar status`, which the notice names.
