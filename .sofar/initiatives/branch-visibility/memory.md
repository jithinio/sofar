<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: branch-visibility

Cite these in .sofar/repo.md by qualified handle — `branch-visibility M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-09-21T16:44:41.114Z) — Two tests fail intermittently under a loaded full `npm test` but pass alone. Seen 2026-09-21 with several peer sessions running. (1) test/reach-index.test.ts "3.5 lexical seeds … prints the matched words" (`logged alpha D1` missing): 2 of 4 full runs failed; 5 of 5 passed alone. (2) test/shim-latency.test.ts: SessionStart best 116 ms against the 100 ms budget; the next run passed. Diagnose by rerunning the file alone (`npx vitest run <file>`, from packages/engine for unit tests). A pass alone means load, not your change.
