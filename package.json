{
  "name": "sofar-monorepo",
  "version": "0.1.0",
  "description": "Sofar v1 — event-log initiative memory for coding agents (npm workspaces monorepo)",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @alignlabs/sofar",
    "test": "vitest run",
    "typecheck": "npm run typecheck -w @sofar/schema && npm run typecheck -w @alignlabs/sofar"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
