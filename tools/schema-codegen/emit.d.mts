/** Path of the committed JSON Schema (crates/sofar-schema/schema/events.schema.json). */
export const OUT: string
/** The JSON Schema text for `KnownEventPayloads` and everything it reaches, as emit.mjs writes it. */
export function emit(): string
