# conformance golden: repo.branch-elsewhere

## step 1: bound record outpaced by others: the recent-work line
argv: ["event","session-start"]
stdin: {"session_id":"conf-session","transcript_path":"<ROOT>/transcript.jsonl","cwd":"<ROOT>","hook_event_name":"SessionStart","source":"startup"}
exit: 0
--- stdout
# Sofar status: session-driver

Goal: Automate the session handoff sofar already makes content-complete: a stateless driver (sofar drive <initiative>) that runs an initiative task-by-task through fresh agent sessions, records every handoff as events in the record, and reaches any headless agent through a three-call adapter (launch, usage, wait) — Claude Code first, a second adapter to prove the contract. The record is the queue; the …

Next action: Run the close review: `sofar review session-driver --final`, work the packet (note it warns its commit walk hit the 200-commit ceiling, so review in smaller ranges with a watermark part-way rather than trusting the listed range to be complete), record the verdict, then `sofar close session-driver`. Weigh two open items in it: the record-index reach flake, which needs its own initiative, and D14's deferred budgets-in-the-record.

Phases:
- done: Phase 1, Phase 2, Phase 3, Phase 4 (11/11 tasks)
Progress: 11/11 tasks done (100%) across 4 phase(s)

Memory (4; full text in memory.md):
- [M3] Claude Code 2.1.251's `--permission-mode` advertises acceptEdits | auto | bypassPermissions | manual | dontAsk | plan, and still accepts the unadvertised `default` (verified by running it, not by reading --help). Commander in the claude binary DOES enforce the choices list, so a…
- [M1] Driver end-to-end without an API call: put a stub `claude` on PATH that prints a `{"type":"system","subtype":"init","session_id":...}` line, then registers itself in the record with `sofar event append <slug> --root <repo> --session <id> --type session_started|task_status_change…
- [M4] After a publish, `npm install -g --prefix ~/.local sofar.sh@latest` can silentl…
- [M2] Capturing a headless agent's real stream shapes costs nothing: point it at an u…

Repo memory (.sofar/repo.md):
# Repo memory — sofar

- Product renamed harness → sofar on 2026-07-07 (BD49-revised, BD50):
  CLI/bin `sofar`, packages sofar-monorepo + @sofar/schema + sofar, MCP
  server "sofar" with sofar_* tools, record dir `.sofar/`. No back-compat
  aliases (pre-publish). Historical event/projection text and the archived
  docs still say harness — that is history, never rewritten.
- Contracts: docs/SPEC.md is authoritative for envelope, tools, layout,
  acceptance criteria. If code and SPEC disagree, SPEC wins; log a decision
…truncated — read .sofar/repo.md for the rest

Recent decisions (last 5 of 14; full text in decisions.md):
- [D13] 2026-08-30 (rule below) The permission vocabulary is the AGENT's, and `--agent-arg`… — over Keeping sofar's own curated subset of modes
- [D14] 2026-08-30 (rule below) `--cost-cap` and `--max-sessions` stay per-DRIVER, and the … — over Adding `cost_usd` to the handoff payload and a recorded fact for a la…
Earlier rejected approaches — do NOT re-propose (9 older):
- …and 9 more (see decisions.md)

Next ids: D15 (decision), M5 (memory)

Adjacent records — 255 decisions across 38 other initiatives that have worked this one's files, densest first:
- repo-memory-capture — 8 shared files, 3 decisions
- drift-certification — 7 shared files, 33 decisions
- commit-attribution — 6 shared files, 22 decisions
…and 35 more. Adjacency, not aboutness — offered as worth reading, never as a rule.

Session: conf-session — adopted on Claude Code; else pass to sofar_start_session.
Git: main @ c0ffee0 — in sync with origin/main

⚠ More recent work is in ANOTHER record: rust-core (last event <AGO> ago) vs session-driver (<AGO> ago), which this branch is bound to and which the block below describes. If rust-core is the work you were asked to continue, re-home now — call sofar_start_session with initiative "rust-core". If it is a parallel session's work, ignore this and stay put.

Standing constraints — obey verbatim (10):
- [D14] `--cost-cap` and `--max-sessions` bound ONE driver, not one run: a resumed run restarts the cap at zero and counts its session budget from recorded handoffs alone. Never describe either as a run-wide guarantee, and keep the resume warnings that say so.
- [D9] An adapter declares what it CANNOT do — usage, nudge, permission_rules, cost — and the driver states the consequence on the run's progress stream before the work starts; never let an inert flag (a cost cap that cannot fire, an allow-list the agent cannot express) pass silently into an unattended run.
- [D8] A driven session's permission surface is recorded in `run_started.surface` and its settings file is written, read back and compared at EVERY launch — never assert a surface the record does not carry, and never claim the driver bounds what a session can do, since its file only adds to the operator's own settings sources.
- [D11] State the driver's token result as a measurement with its scope — 2.44M vs 5.80M per task on the 4.1 run, for tasks that fit one cold session — never as a general claim that driving is cheaper. Where this and D1's rationale disagree, the measurement wins.
- [D6] `sofar drive` never creates or destroys a git worktree or switches a branch; it launches into one directory for the whole run and refuses to start unless that directory's `.sofar` log for the initiative is the same file it is driving.
- [D3] An adapter never reports record state: wrote_back and the launched session's identity are derived from the fold, and when several candidate sessions exist the driver records a stall rather than guessing.
- [D13] PERMISSION_MODES must carry every mode the target agent's own `--permission-mode` advertises (check `claude --help`), and every mode in it must map in codex's SANDBOX_BY_MODE — a mode sofar refuses is a mode no operator can reach.
- …and 3 more (see decisions.md)

Read-back: before acting, restate goal, next action, and standing constraints in one sentence each — if your restatement disagrees with this block, trust the block and say so.

(generated by sofar — full detail in plan.md, decisions.md, sessions/)
--- stderr
(empty)

## step 2: statusline
argv: ["statusline","--no-color"]
stdin: {"hook_event_name":"Status","session_id":"conf-session","transcript_path":"<ROOT>/transcript.jsonl","cwd":"<ROOT>","model":{"id":"claude-fable-5-1","display_name":"Fable 5.1 (1M context)"},"workspace":{"current_dir":"<ROOT>","project_dir":"<ROOT>"},"version":"2.1.0","context_window":{"used_percentage":42.4,"current_usage":{"input_tokens":1200,"cache_creation_input_tokens":3000,"cache_read_input_tokens":40000}}}
exit: 0
--- stdout
Fable 5.1 (1M) · root:main · session-driver 11/11 · ctx 42% · cache 90% ✓
--- stderr
(empty)

## step 3: status
argv: ["status"]
exit: 0
--- stdout
# session-driver

Goal: Automate the session handoff sofar already makes content-complete: a stateless driver (sofar drive <initiative>) that runs an initiative task-by-task through fresh agent sessions, records every handoff as events in the record, and reaches any headless agent through a three-call adapter (launch, usage, wait) — Claude Code first, a second adapter to prove the contract. The record is the queue; the driver holds no state of its own.

Standing constraints — obey verbatim (10):
- [D1] sofar may launch the operator's own agent process under the operator's auth, but it never holds API keys, never infers, and never sends user content off the machine; any further guard-rail deviation must be logged as a Decision naming the rail it supersedes before the work proceeds.
- [D2] The driver holds no state outside the record and never implements an agent loop of its own — it launches existing headless agents through the adapter, and every driver-side state change is an event in packages/schema.
- [D3] An adapter never reports record state: wrote_back and the launched session's identity are derived from the fold, and when several candidate sessions exist the driver records a stall rather than guessing.
- [D5] Driver handoff reasons are derived from the fold — needs_user is the named task in status `blocked`, task_done needs a write-back AND a terminal task status; never infer a reason from write-back prose or an exit code.
- [D6] `sofar drive` never creates or destroys a git worktree or switches a branch; it launches into one directory for the whole run and refuses to start unless that directory's `.sofar` log for the initiative is the same file it is driving.
- [D8] A driven session's permission surface is recorded in `run_started.surface` and its settings file is written, read back and compared at EVERY launch — never assert a surface the record does not carry, and never claim the driver bounds what a session can do, since its file only adds to the operator's own settings sources.
- [D9] An adapter declares what it CANNOT do — usage, nudge, permission_rules, cost — and the driver states the consequence on the run's progress stream before the work starts; never let an inert flag (a cost cap that cannot fire, an allow-list the agent cannot express) pass silently into an unattended run.
- [D11] State the driver's token result as a measurement with its scope — 2.44M vs 5.80M per task on the 4.1 run, for tasks that fit one cold session — never as a general claim that driving is cheaper. Where this and D1's rationale disagree, the measurement wins.
- [D13] PERMISSION_MODES must carry every mode the target agent's own `--permission-mode` advertises (check `claude --help`), and every mode in it must map in codex's SANDBOX_BY_MODE — a mode sofar refuses is a mode no operator can reach.
- [D14] `--cost-cap` and `--max-sessions` bound ONE driver, not one run: a resumed run restarts the cap at zero and counts its session budget from recorded handoffs alone. Never describe either as a run-wide guarantee, and keep the resume warnings that say so.
Progress: 11/11 tasks done (100%) across 4 phase(s)

Phases:
- Phase 1 — Contract [done] 3/3
  - [x] 1.1 Amend the launch clause: CLAUDE.md guard-rails and SPEC §Architectural invariants read as D1 (sofar may launch the operator's own agent under the operator's auth; still no API keys, no inference, no user content off the machine)
  - [x] 1.2 Handoff events in packages/schema — run_started, handoff {reason: threshold|task_done|stall|needs_user}, run_stopped — folded and rendered in sessions/*.md and the digest; unknown lines still skipped, never fatal
  - [x] 1.3 Adapter contract as a TS interface in packages/engine: launch(digest, task), usage() → tokens, wait() → {exit, wrote_back}; a fake adapter drives the tests
- Phase 2 — Claude Code adapter + sofar drive [done] 4/4
  - [x] 2.1 Claude Code adapter: claude -p --output-format stream-json, per-message usage (input + cache_read + cache_creation), SessionStart injection unchanged, exit and write-back detected from the record fold
  - [x] 2.2 sofar drive <initiative>: stateless loop — next task from the fold → launch → watch → record handoff; stop rules: initiative closed | next action needs the user | N sessions with no task change | cost cap; each session in its own git worktree
  - [x] 2.3 Context policy: task-scoped by default; optional threshold (config) via a PostToolUse hook that injects "ctx N% — finish the current task, write back, end your turn" as additionalContext
  - [x] 2.4 Permission surface for unattended sessions: pinned settings file (allow-list, permission mode, effort, model) written per session and verified by reading it back — drift-certification D6/D11 discipline
- Phase 3 — Second adapter proves the contract [done] 2/2
  - [x] 3.1 Second adapter (codex exec --json or opencode run, whichever is installed) with the usage fallback for agents that report none — the contract is proven when sofar drive runs unchanged against it
  - [x] 3.2 Per-task routing: model/effort hints on a task, driver picks the adapter per task; sofar itself still never infers
- Phase 4 — Proof + release [done] 2/2
  - [x] 4.1 Drive one real initiative end to end unattended; publish sessions, handoffs by reason, tokens versus the manual baseline, and operator minutes
  - [x] 4.2 Release sofar.sh with sofar drive; SPEC §Acceptance criteria extended for the driver

Next action: Run the close review: `sofar review session-driver --final`, work the packet (note it warns its commit walk hit the 200-commit ceiling, so review in smaller ranges with a watermark part-way rather than trusting the listed range to be complete), record the verdict, then `sofar close session-driver`. Weigh two open items in it: the record-index reach flake, which needs its own initiative, and D14's deferred budgets-in-the-record.

⚠ Staleness:
- last write-back summary exceeds the SessionStart budget (450 chars) and is clipped there — full text in sessions/13b12f8c-1e82-4f05-8bb2-43aaf23aaac2.md

Last session (claude-code, ended 2026-08-30T08:06:05.462Z):
  0.30.0 is published and verified; session-driver is 11/11 with every phase done. The publish looked like it failed and had not: the web auth round-trip staged the version, so the retry returned E409 'Cannot publish over previously staged version' while the FIRST PUT had already succeeded, and `sofar upgrade` said 0.29.0 only because it reads a once-a-day cached answer. Confirmed from the registry instead — dist-tags.latest is 0.30.0 and dist.shasum is 976a2fa198c60838a489c1904e8b57d9384742d5, byte-identical to the local `npm pack --dry-run`. A clean install of sofar.sh@0.30.0 in a scratch dir reports 0.30.0 and carries drive with all 19 flags. The global at ~/.local is upgraded to 0.30.0, so this repo stops dogfooding 0.29.0 through its hooks and .mcp.json — but only on the second attempt: `sofar.sh@latest` exited 0, printed nothing, and left 0.29.0 in place, while the same command pinned to @0.30.0 changed 1 package. npm's own metadata was already fresh, so that is npm's packument cache, and repo memory now says to pin the version and verify with `sofar --version` rather than trust @latest — an unverified upgrade reproduces exactly the stale-engine failure that rule exists to prevent. Phase 4 marked done. Not done and deliberately left: the close, because `sofar review --final` is a real cross-cutting pass over 18 commits with a recorded verdict (D9/D10), and it should weigh two open things rather than rubber-stamp them — the reach-index 3.5 flake this session found, and D14, where the operator chose to state the per-driver cost and session caps rather than record them.

Files touched (39):
- /Users/jins/IO/sofar/CLAUDE.md
- /Users/jins/IO/sofar/docs/SPEC.md
- /Users/jins/IO/sofar/packages/schema/src/events.ts
- /Users/jins/IO/sofar/packages/schema/test/schema.test.ts
- /Users/jins/IO/sofar/packages/engine/src/core/fold.ts
- /Users/jins/IO/sofar/packages/engine/src/projections/templates/shared.ts
- /Users/jins/IO/sofar/packages/engine/src/projections/templates/status.ts
- /Users/jins/IO/sofar/packages/engine/src/projections/templates/session.ts
- /Users/jins/IO/sofar/packages/engine/test/driver-events.test.ts
- /private/tmp/claude-501/-Users-jins-IO-sofar/f9e912cd-9332-43a6-b0f9-bb53d961bf25/scratchpad/commit-1.1.txt
- /private/tmp/claude-501/-Users-jins-IO-sofar/f9e912cd-9332-43a6-b0f9-bb53d961bf25/scratchpad/commit-1.2.txt
- /private/tmp/claude-501/-Users-jins-IO-sofar/8adc3273-68ff-4c0d-936a-9a3e26e5b66c/scratchpad/runtime-comparison.html
- /Users/jins/IO/sofar/packages/engine/src/driver/adapter.ts
- /Users/jins/IO/sofar/packages/engine/test/helpers/fake-adapter.ts
- /Users/jins/IO/sofar/packages/engine/test/adapter-contract.test.ts
- /Users/jins/IO/sofar/docs/ARCHITECTURE.md
- /private/tmp/claude-501/-Users-jins-IO-sofar/f9e912cd-9332-43a6-b0f9-bb53d961bf25/scratchpad/commit-1.3.txt
- /Users/jins/IO/sofar/packages/engine/src/driver/claude-code.ts
- /Users/jins/IO/sofar/packages/engine/test/adapter-claude-code.test.ts
- /private/tmp/claude-501/-Users-jins-IO-sofar/f9e912cd-9332-43a6-b0f9-bb53d961bf25/scratchpad/commit-2.1.txt
- /Users/jins/IO/sofar/packages/engine/src/driver/drive.ts
- /Users/jins/IO/sofar/packages/engine/src/cli/drive.ts
- /Users/jins/IO/sofar/packages/engine/test/drive.test.ts
- /private/tmp/claude-501/-Users-jins-IO-sofar/6622fe8c-316d-409f-b1fb-bcdb8f0be937/scratchpad/msg.txt
- /Users/jins/IO/sofar/packages/engine/src/driver/nudge.ts
- /private/tmp/claude-501/-Users-jins-IO-sofar/6622fe8c-316d-409f-b1fb-bcdb8f0be937/scratchpad/msg2.txt
- /private/tmp/claude-501/-Users-jins-IO-sofar/6622fe8c-316d-409f-b1fb-bcdb8f0be937/scratchpad/msg4.txt
- /Users/jins/IO/sofar/packages/engine/src/driver/routing.ts
- /Users/jins/IO/sofar/packages/engine/test/driver-routing.test.ts
- /private/tmp/claude-501/-Users-jins-IO-sofar/dc97485d-5728-4ac3-bbb2-d15ba80f1632/scratchpad/msg.txt
- /Users/jins/IO/sofar/packages/engine/test/zz-probe.test.ts
- /Users/jins/IO/sofar/packages/engine/src/driver/permissions.ts
- /Users/jins/IO/sofar/packages/engine/src/driver/codex.ts
- /Users/jins/IO/sofar/packages/engine/src/cli/index.ts
- /Users/jins/IO/sofar/packages/engine/test/driver-permissions.test.ts
- /Users/jins/IO/sofar/packages/engine/test/adapter-codex.test.ts
- /Users/jins/IO/sofar/README.md
- /private/tmp/claude-501/-Users-jins-IO-sofar/13b12f8c-1e82-4f05-8bb2-43aaf23aaac2/scratchpad/writeback.json
- /private/tmp/claude-501/-Users-jins-IO-sofar/13b12f8c-1e82-4f05-8bb2-43aaf23aaac2/scratchpad/writeback2.json
--- stderr
(empty)

## step 4: register
argv: ["event","post-tool"]
stdin: {"session_id":"conf-session","transcript_path":"<ROOT>/transcript.jsonl","cwd":"<ROOT>","hook_event_name":"PostToolUse","tool_name":"Edit","tool_input":{"file_path":"<ROOT>/x.ts","old_string":"a","new_string":"b"},"tool_response":{}}
exit: 0
--- stdout
(empty)
--- stderr
(empty)

## step 5: prompt shows the in-sync push state
argv: ["event","user-prompt"]
stdin: {"session_id":"conf-session","transcript_path":"<ROOT>/transcript.jsonl","cwd":"<ROOT>","hook_event_name":"UserPromptSubmit","prompt":"continue"}
exit: 0
--- stdout
sofar: main @ c0ffee0, pushed (in sync with origin/main).
<no trailing newline>
--- stderr
(empty)

## step 6: a session homed elsewhere sees no recent-work line
argv: ["event","session-start"]
stdin: {"session_id":"aefa6315-3725-4e4d-9f9a-224ff6f86ddb","transcript_path":"<ROOT>/transcript.jsonl","cwd":"<ROOT>","hook_event_name":"SessionStart","source":"startup"}
exit: 0
--- stdout
# Sofar status: session-strategy-bench

Goal: Automated controlled benchmark: one long session vs fresh-sessions-per-task (Sofar pattern), measuring cost shape, output quality, and time. Second scenario of handoff-bench; reuses its harness, blind-grading machinery, and evidence format. MEASURES only — read-side; zero engine/injection changes; findings that suggest engine changes are logged as proposals.

Next action: When the driver exits: extract telemetry, run the sidecar, grade (A+B primary, C robustness), write the R02 evidence section.

Last session (claude-code, ended 2026-08-13T14:58:50.576Z):
  Phase-close repair from phase-lifecycle 4.3 (D5). No bench work: read plan.md and the event log, closed Phase 5 (3/3 tasks, freeze confirmed), and LEFT PHASE 6 OPEN — all five of its tasks were marked done in one minute, only four have a confirming note, and a W1-L control re-run landed afterwards whose three-way drift t… (clipped — full text in sessions/01KZXT38PBSSKXMFYGN7K54QVX.md)

⚠ 1 other session(s) did work without writing back: 8d40de69-7589-4f67-8ffc-da71d4e99951

Phases:
- Phase 6 — S2-R02 execution + evidence [pending — all tasks done; mark phase done?] 5/5
- done: Phase 1, Phase 2, Phase 3, Phase 4, Phase 5 (15/15 tasks)
Progress: 20/20 tasks done (100%) across 6 phase(s)

Repo memory (.sofar/repo.md):
# Repo memory — sofar

- Product renamed harness → sofar on 2026-07-07 (BD49-revised, BD50):
  CLI/bin `sofar`, packages sofar-monorepo + @sofar/schema + sofar, MCP
  server "sofar" with sofar_* tools, record dir `.sofar/`. No back-compat
  aliases (pre-publish). Historical event/projection text and the archived
  docs still say harness — that is history, never rewritten.
- Contracts: docs/SPEC.md is authoritative for envelope, tools, layout,
  acceptance criteria. If code and SPEC disagree, SPEC wins; log a decision
…truncated — read .sofar/repo.md for the rest

Recent decisions (last 5 of 12; full text in decisions.md):
- [D8] 2026-07-20 S2-R02 P3' probe ships as a NEW task 7 in both workloads — over Replacing task 6 with the redesigned probe as the brief literally spe…
- [D9] 2026-07-20 R02 arm S runs the PUBLISHED @alignlabs/sofar 0.10.0 in DEFAULT config — over Running arm S against the daemon to show the T3 speedup, or leaving t…
- [D10] 2026-07-20 Three graders, two roles. PRIMARY = Grader A claude-opus-4-8 + Grader B GLM 5.2 via OpenC… — over Folding all three graders into one worse-of-three or majority rule, o…
- [D11] 2026-07-20 P5' fabricated-path sidecar runs POST-HOC by replay, never inside the measured run — over Running the orientation probe live in the arm worktree before each ta…
- [D12] 2026-07-20 R02 replicates R01's arm-setup procedure EXACTLY — over Equalizing the arms in R02 by giving arm L a .claude/ carrying the sa…
Earlier rejected approaches — do NOT re-propose (7 older):
- [D1] Post-hoc analysis of whatever the data shows, or predictions written/…
- [D2] Letting benchmark findings drive engine/injection tweaks in-flight wi…
- [D3] Building a new bench, single-family
- [D4] Running arm L with sofar wired
- [D5] Running bench worktrees inside the live brillo checkout
- …and 2 more (see decisions.md)

Next ids: D13 (decision), M1 (memory)

Adjacent records — 7 decisions across 1 other initiative that have worked this one's files, densest first:
- speed — 4 shared files, 7 decisions
Adjacency, not aboutness — offered as worth reading, never as a rule.

Session: aefa6315-3725-4e4d-9f9a-224ff6f86ddb — adopted on Claude Code; else pass to sofar_start_session.
Git: main @ c0ffee0 — in sync with origin/main

Read-back: before acting, restate goal, next action, and standing constraints in one sentence each — if your restatement disagrees with this block, trust the block and say so.

(generated by sofar — full detail in plan.md, decisions.md, sessions/)
--- stderr
(empty)

## record delta
=== .sofar/initiatives/session-driver/events.jsonl (appended 451 bytes after 632606 unchanged)
{"v":1,"id":"<ULID>","ts":"<TS>","initiative":"session-driver","session":"conf-session","source":"hook","actor":"agent","user":"conformance@example.invalid","type":"session_started","payload":{"tool":"claude-code"}}
{"v":1,"id":"<ULID>","ts":"<TS>","initiative":"session-driver","session":"conf-session","source":"hook","actor":"agent","user":"conformance@example.invalid","type":"file_touched","payload":{"ok":true,"op":"edit","path":"<ROOT>/x.ts"}}
=== .sofar/initiatives/session-driver/sessions/conf-session.md (added, 425 bytes)
<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Session conf-session

- Initiative: session-driver
- Tool: claude-code
- Started: <TS>
- Ended: (in progress)

## Summary

(none recorded — no write-back yet; derived resume point below)

## Next action

(none recorded)

## Activity (derived from mechanical events)

- Derived: 1 file (<ROOT>/x.ts)
- Files:
  - <ROOT>/x.ts
- Commands run: 0
