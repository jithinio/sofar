<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: peer-messaging

Cite these in .sofar/repo.md by qualified handle — `peer-messaging M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-08-08T12:42:33.878Z) — grep SILENTLY MISSES packages/engine/src/core/fold.ts — the most-searched file in the repo. `file` reports it as "data" rather than text, so plain grep returns no output and exit 1 for symbols that demonstrably exist there (FileConflict:763, openSessionFileConflicts:823 as of 2026-08-08). It is NOT corrupt: valid UTF-8, no NUL bytes, no C0/C1 control characters, longest line 119 chars — cause undetermined, likely a `file`(1) heuristic on its non-ASCII density. Symptom: you grep for a type, get nothing, and conclude it lives elsewhere or was renamed; every importer shows up and the definition never does. Always search this repo with `grep -a` (Grep tool users: fall back to `grep -a` via Bash when a symbol's importers appear but its definition does not). Only fold.ts is affected; schema/src/tool-inputs.ts and cli/adopt.ts read as unusual-but-text and grep fine.
