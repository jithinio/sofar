<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: hookspath-attribution

Cite these in .sofar/repo.md by qualified handle — `hookspath-attribution M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-08-13T14:41:54.691Z) — VERIFYING A PACKED BUILD: put the tarball's bin FIRST on PATH, or you are testing the installed engine and not the artifact. The prepare-commit-msg shim resolves `sofar` from PATH by design (D5: no absolute path, so it survives any install layout), so `npm install -g --prefix <tmp> sofar.sh-<v>.tgz` and then invoking the CLI by its full path proves nothing about attribution — the hook still shells out to whatever `sofar` PATH finds, which is the OLD global install. Symptom: the artifact check "fails" while the code is fine, or passes while the fix is absent. Reported by the phase-lifecycle session on 2026-08-13 after hitting exactly this while verifying 0.27.0, and it matches how the 0.26.0 smoke test in this repo behaved before the same correction. Fix: `export PATH="<prefix>/bin:$PATH"` for the whole check, and confirm with `sofar --version` inside the scratch repo. Companion fact from the same report: `sofar init` takes no initiative argument.
