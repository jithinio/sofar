<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: drift-signal

Goal: Make drift mean unrecorded work: scope the write-back nudge and Stop gate to the session that owes the write-back, and stop counting command_run as drift. Supersedes speed D-T1's premise that pure reads emit no events.

Progress: 7/7 tasks done (100%)

## Phase 1 — Fold [done] — 3/3 done

- [x] 1.1 SessionState.unwritten + FreshnessState.unattributed_mutations, kept in the same recordFreshness pass so the two scopes cannot disagree
- [x] 1.2 sessionDebt(state, session) = own unwritten + unattributed — the one definition the Stop gate and the nudge share
- [x] 1.3 Drop commands from freshnessTotal and from describeFreshness so the headline and its breakdown agree

## Phase 2 — Hooks [done] — 2/2 done

- [x] 2.1 Stop gate reads sessionDebt, not initiative drift OR'd with session.activity; fail-closed seam kept
- [x] 2.2 UserPromptSubmit nudge states THIS session's debt

## Phase 3 — Contract [done] — 2/2 done

- [x] 3.1 SPEC: freshness/unwritten/sessionDebt derivation, nudge + Stop wording, new acceptance block
- [x] 3.2 Tests: four new behaviors pinned; fixtures that built drift from Bash calls reseeded with mutations
