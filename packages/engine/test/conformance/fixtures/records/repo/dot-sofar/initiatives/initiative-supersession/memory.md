<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: initiative-supersession

Cite these in .sofar/repo.md by qualified handle — `initiative-supersession M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-09-11T11:46:55.833Z) — npm publish can land in the registry's STAGE QUEUE instead of going live (npm staged publishing, GA 2026-08): the run prints the tarball and "Publishing to https://registry.npmjs.org/", then the version is NOT in `npm view <pkg> versions`, and a retry fails E409 "Cannot publish over previously staged version X". Diagnose with `npm view sofar.sh versions --json --prefer-online` (staged versions are absent) and the debug log in ~/.npm/_logs (PUT 401 → web auth → PUT 409). Resolve by APPROVING the staged version, never by re-publishing or bumping: on npmjs.com, account → "Staged Packages" tab → Approve (2FA), or `npm stage list` / `npm stage approve <id>` on npm ≥ 11.15.0 (Node ≥ 22.14). npm 11.12.1 has no `stage` command. Hit 2026-09-11 on sofar.sh 0.31.0.
- **M2** (2026-09-11T11:48:05.368Z) — CORRECTION to the staged-publish note above (same day, 2026-09-11): the E409 "previously staged version" after a publish that printed success can be npm's AUTOMATED REVIEW, not a human-approval stage. Diagnose on the npmjs.com package page → Version History: the version shows Status "Validating — Automated review hasn't finished. The version will remain unavailable until review completes.", while the account's Staged Packages tab is EMPTY (nothing to approve). Nothing to do but wait: do not re-publish, do not bump. Verify with `npm view sofar.sh version` until it prints the new version; an upgrade check inside sofar (`sofar upgrade --check`) reads the same dist-tag, so it lags identically. If the version instead appears under Staged Packages, that is the human gate — approve there (2FA) or `npm stage approve` on npm ≥ 11.15.
