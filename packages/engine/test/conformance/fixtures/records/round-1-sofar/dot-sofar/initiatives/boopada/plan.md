<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Plan: boopada

Goal: Boopada trip planner (apps/web Next.js + @workspace/providers fixtures, SQLite, bun monorepo): profile → bucket list → trips → itinerary editing/view → location to-dos → suggestions & transport costs → curation advisor → chat command box. One roadmap piece per session.

Progress: 40/40 tasks done (100%)

## 1. Traveller profile [done] — 0/0 done


## 2. Bucket list [done] — 0/0 done


## 3. Trips [done] — 5/5 done

- [x] trips Create a trip from legs (city + nights) with a draft day-by-day plan
- [x] trips-api POST/GET /api/trips, GET/DELETE /api/trips/{id} (soft), from_bucket_id marks bucket item planned; migration 3
- [x] trips-today GET /api/trips/{id}/today → {day_index|null}, 'today' in each day's city time zone (D-trip-zone)
- [x] trips-pages /trips (create + list, testid trip-{id}) and /trips/{id} (days, testid day-{index}) pages
- [x] trips-tests Tests incl. date-line crossings + lint/typecheck/test/build/start green

## 4. Itinerary editing [done] — 5/5 done

- [x] itinerary-editing Add, move, remove activities and days
- [x] itinerary-money Home-currency conversion: convertMoney (exact BigInt, half-even to home minor unit) using fx.getRates(home); unit tests
- [x] itinerary-api Migration 4 itinerary_items; POST /api/trips/{id}/days/{index}/activities, PATCH/DELETE /api/itinerary-items/{id}, POST /api/trips/{id}/days, DELETE /api/trips/{id}/days/{index}; days[].items in trip; soft delete + undo for items and days
- [x] itinerary-page /trips/{id} editor: add activity, change time/move day, remove item, add/remove day, undo
- [x] itinerary-tests API tests + lint/typecheck/test/build/start green

## 5. Itinerary view [done] — 4/4 done

- [x] itinerary-view Trip laid out day by day with daily and trip totals
- [x] summary-api GET /api/trips/{id}/summary → {days:[{index,date,city_id,items_count,total}], trip_total} via tripSummary() (lib/server/summary.ts)
- [x] itinerary-view-page /trips/{id} two-column: left itinerary (trip-total top, day-{index}, item-{id}, day-total-{index}), right empty aside
- [x] itinerary-view-tests Summary API tests (totals, empty days, deletes/undo, home currency) + lint/typecheck/test/build/start green

## 6. Location to-dos [done] — 5/5 done

- [x] location-todos To-dos per day, surfaced when you arrive somewhere
- [x] todos-api Migration 5 todos; POST /api/trips/{id}/days/{index}/todos, PATCH/DELETE /api/todos/{id} (soft, undoable); days[].todos in trip
- [x] todos-now GET /api/trips/{id}/now → {day_index, city_id, arrived, todos} via tripNow() (D8 zone rules)
- [x] todos-page /trips/{id}: arrival-banner (arrival-todo-{id}) + per-day to-do editing in day cards
- [x] todos-tests API tests + lint/typecheck/test/build/start green

## 7. Suggestions & transport [done] — 6/6 done

- [x] suggestions-transport Activity suggestions and local transport costs
- [x] suggestions-core Migration 6 suggestions; lib/server/suggestions.ts: offer() (persist only applicable suggestions, stable ids) + action registry + applySuggestion(); POST /api/suggestions/{id}/apply
- [x] suggestions-day GET /api/trips/{id}/days/{index}/suggestions (interests, not on day, open weekday, 10:00 or first free hour after last item); /now gains suggestions when arrived
- [x] transport-api GET /api/cities/{id}/transport → {city_id, transport:[{mode,unit,cost,cost_local,notes}]}
- [x] suggestions-page /trips/{id}: trip-side aside with per-day suggestions (apply buttons) + transport costs per trip city; arrival banner suggestions
- [x] suggestions-tests API tests + lint/typecheck/test/build/start green

## 8. Curation advisor [done] — 6/6 done

- [x] curation-advisor Advisor: what could be better, and can apply fixes
- [x] advisor-actions ACTIONS remove_item / move_item / replace_item in lib/server/suggestions.ts (replace atomic via sync insertActivityItem in itinerary.ts)
- [x] advisor-rules lib/server/advisor.ts detectAdvice(): lighten_day (>600 min → remove longest), closed_venue (move to nearest later same-city open day else remove), over_budget (day total > inHome(daily budget) → remove most expensive), weather_risk (nature/adventure in low-season month with >250mm rain → replace with open culture activity same time)
- [x] advisor-api GET /api/trips/{id}/advice → {advice: Suggestion[]} via tripAdvice(); apply via POST /api/suggestions/{id}/apply re-detects (409 if no longer advised)
- [x] advisor-page /trips/{id}: advice list with Apply buttons (advice-{kind}-{n}, advice-apply-{n}, n 1-based position)
- [x] advisor-tests API tests per rule + lint/typecheck/test/build/start green

## 9. Chat [done] — 4/4 done

- [x] chat POST /api/chat {trip_id,message} → {reply,changed,suggestions?}; commands exactly: 'add <activity> to day <n>', 'remove <activity> from day <n>', 'undo', 'make day <n> lighter' (advisor lighten suggestion for that day, else remove that day's longest activity), 'interest add <category>' / 'interest remove <category>', 'what can be better' (list advisor suggestions); anything else → reply listing commands, changed:false
- [x] chat-api lib/server/chat.ts parse + run commands reusing addActivity/removeItem/undoLastDeletion/tripAdvice/applySuggestion/updateProfile; app/api/chat/route.ts
- [x] chat-ui components/trip-chat.tsx command box on /trips/{id} (chat-input, chat-send, chat-reply, chat-suggestion-{n}); router.refresh when changed
- [x] chat-tests test/chat-api.test.ts + lint/typecheck/test/build/start green

## 10. Demo polish [done] — 5/5 done

- [x] polish-audit End-to-end walkthrough (headless-Chrome screenshots of every page + API edge-case bug hunt); fix every confirmed defect
- [x] polish-ui UI rough edges: clipped time inputs, misaligned transport tables, trips list (readable dates, per-trip total, status badge), open the trip after creating it
- [x] demo-seed POST /api/demo seeds a sample trip starting today (via existing createTrip/addActivity/addTodo/createBucketItem) that shows arrival banner, to-dos, suggestions and advisor advice; 'Load sample trip' button
- [x] home-dashboard Home page: current/next trip card (today's day, city, open to-dos, trip total) + shared nav on every page
- [x] polish-tests Tests for new endpoints/fixes + lint/typecheck/test/build/start green; README demo script

Next action: Ask the operator to review and commit the uncommitted phase-10 changes (git status in the repo root). Then ask how the demo went and what to do next: close the initiative with sofar_close_initiative, or pick from the open contract questions in the latest decision log — reject activities that run past midnight or appear twice on a day, un-plan a bucket item when its trip is deleted, scope undo to a trip, or add a chat command to add a day.
