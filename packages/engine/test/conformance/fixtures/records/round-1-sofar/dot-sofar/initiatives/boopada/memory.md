<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: boopada

Cite these in .sofar/repo.md by qualified handle — `boopada M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-09-15T14:31:28.480Z) — Runtime split: `bun run start`/`next start` runs on Node 24 (next's node shebang), while `bun test` runs on Bun. SQLite is loaded per-runtime (node:sqlite vs bun:sqlite) in apps/web/lib/server/db.ts — never import either statically or the Next build breaks. Web tests call route handlers directly (import GET/PUT from app/api/**/route.ts) with BOOPADA_DB_PATH set to a temp file and closeDb() between tests.
- **M2** (2026-09-15T14:31:29.691Z) — Gotchas: (1) biome.json excludes `.sofar` — sofar's generated .sofar/.index/*.json otherwise fail `bun run lint`, and `bun run lint:fix` would rewrite them. (2) When smoke-testing `bun run start`, pick a free random PORT and confirm "Ready" in the log: other bench cells on this machine run next servers on fixed ports (3917 was taken) and curl will silently hit THEIR app.
- **M3** (2026-09-15T14:37:50.216Z) — Dynamic route handlers: wrap with route<Context>() from apps/web/lib/server/api.ts where Context = { params: Promise<{ id: string }> }; route() makes the context arg optional so static handlers stay callable as GET(request) in tests. Next writes .next/types/validator.ts, which `bun run typecheck` (tsc) includes — a handler signature whose 2nd param is `void` or required-for-static routes fails there with TS2344 "does not satisfy RouteHandlerConfig", even though bun test passes. Tests pass params as { params: Promise.resolve({ id: "1" }) } (see apps/web/test/bucket-api.test.ts).
- **M4** (2026-09-15T14:51:52.306Z) — Trip dates (boopada D8): day index/date are derived from live `trip_days` rows in position order (date = start_date + index-1) — never store them. Zone-aware helpers are in apps/web/lib/server/trip-time.ts (isCalendarDate, addDays, localDate, todayIndex); city zones come from getCatalog().timeZones. Test "today" logic by setting BOOPADA_NOW per assertion; run `TZ=Pacific/Kiritimati bun test test/trips-api.test.ts` to prove server-zone independence.
- **M5** (2026-09-15T15:01:04.984Z) — Pricing (boopada phase 4): anything that renders money for the traveller needs a `Pricing` ({home, rates}) from `await getPricing()` in apps/web/lib/server/pricing.ts, fetched once per request, then sync `inHome(pricing, money)`. So getTrip/listTrips/requireTrip/deleteTrip, getItineraryItem/getTripDay and undoLastDeletion all take `pricing` as an argument; ENTITIES loaders in deletions.ts are `(id, pricing)`. Itinerary mutations live in apps/web/lib/server/itinerary.ts; items attach to trip_days.id (never an index). Expected converted fixture values for tests: 250000 VND→984 USD cents, 3800 JPY→2508 USD / 1982 GBP, 25 EUR→2717 USD. Undo of a removed day restores it at its old position (appendDay puts new rows after MAX(position) incl. deleted).
- **M6** (2026-09-15T15:10:04.393Z) — To-dos: logic lives in apps/web/lib/server/todos.ts; "now" (day_index, city_id, arrived, open to-dos of every day in the current city) comes only from tripNow(trip), shared by GET /api/trips/{id}/now and the /trips/{id} arrival banner. Adding a TripDay field breaks exact toEqual day assertions in trips-api/itinerary-api tests. Smoke-test cleanup: kill only your PORT (lsof -ti tcp:$PORT | xargs kill), never pkill -f "next start".
- **M7** (2026-09-15T15:17:21.180Z) — Suggestions (phase 7): every suggester goes through offer(kind, storedAction, reason, pricing) in apps/web/lib/server/suggestions.ts; register new action types in its ACTIONS with resolve() (returns {ok:false, why} or {ok:true, public action, apply}). Stored actions reference rows by stable id (trip_day_id), are persisted in migration-6 `suggestions` keyed by kind+JSON(stored action) so ids are stable; POST /api/suggestions/{id}/apply re-resolves (409 suggestion_not_applicable). Adding a field to TripNow breaks exact toEqual in test/todos-api.test.ts. Migration-count assertions should not pin the latest version.
- **M8** (2026-09-15T15:24:35.257Z) — Curation advisor (phase 8): rules only in detectAdvice() (apps/web/lib/server/advisor.ts, pure, no suggestions.ts import); published via tripAdvice() in lib/server/suggestions.ts through offer(); ACTIONS gained remove_item / move_item / replace_item (stored refs: trip_id, trip_day_id, item_id [+ to_trip_day_id | activity_id]). applySuggestion re-runs detectAdvice for ADVICE_KINDS (lib/suggestions.ts) and 409s unless identical kind+action is still detected. GET /api/trips/{id}/advice → {advice}. UI components/advice-list.tsx at top of the trip-side aside (advice-{kind}-{n}, advice-apply-{n}, n = 1-based list position). itinerary.ts insertItem() is the sync insert (used for atomic replace). Fixture facts for tests: rainy low-season months exist only for hanoi (7,8), ho-chi-minh-city (6-10), da-nang & hoi-an (9-11); london/san-francisco/singapore/dubai/mumbai have no climate data (getMonthlyClimate throws → advisor treats as no risk); da-nang has no culture activity; closed-day activities: tokyo-act-2 (Sun), kyoto-act-2 (Wed), lisbon-act-2 (Mon), petra-act-2 (open Mon/Wed/Thu only); 2027-04-11 is a Sunday.
- **M9** (2026-09-15T15:31:27.657Z) — Chat (phase 9): POST /api/chat → runChat() in apps/web/lib/server/chat.ts, a table of [regex, handler] over the normalized message (lowercase, collapsed spaces, trailing .!? stripped); add a command by appending to `commands` AND to CHAT_COMMANDS in apps/web/lib/chat.ts (the help reply and UI hint chips read it). Handlers only dispatch to existing functions (addActivity, removeItem, undoLastDeletion, tripAdvice+applySuggestion, updateProfile). Rephrasable misses → 200 changed:false; unknown interest → 400 field "message". UI components/trip-chat.tsx tops the trip aside (chat-input, chat-send, chat-reply, chat-suggestion-apply-{n}, chat-hint-{n}). advisor.ts exports firstMax/formatMinutes for reuse. Fixture durations: lisbon-act-3 Sintra 540 min, lisbon-act-1 150, tokyo-act-1 150, -2 180, -3 360.
- **M10** (2026-09-15T15:44:08.953Z) — Driving the real UI (boopada phase 10): no Playwright in the repo, but playwright-core is in the bun cache and Google Chrome is installed. `mkdir /tmp/bp-e2e && cd /tmp/bp-e2e`, package.json with dependency "playwright-core":"1.58.2", `bun install` (offline from cache), then a script using chromium.launch({executablePath:"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"}) against a `bun run start` server on a random PORT with a temp BOOPADA_DB_PATH. Quick static screenshots: `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --window-size=1400,1800 --screenshot=/tmp/x.png URL`. Keep e2e scripts out of the repo (lint/typecheck cover the repo).
