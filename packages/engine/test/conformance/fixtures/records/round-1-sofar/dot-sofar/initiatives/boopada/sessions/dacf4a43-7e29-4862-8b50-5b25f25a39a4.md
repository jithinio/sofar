<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Session dacf4a43-7e29-4862-8b50-5b25f25a39a4

- Initiative: boopada
- Tool: claude-code
- Started: 2026-09-15T14:52:41.431Z
- Ended: 2026-09-15T15:01:35.545Z

## Summary

Shipped roadmap piece 4, Itinerary editing, per operator spec. Operator keeper decision logged as a standing rule: traveller-facing money is in the home currency, converted via fx.getRates(home) with exact BigInt math, half-even to the home minor unit (convertMoney in lib/money.ts; getPricing/inHome in lib/server/pricing.ts; converted at read time). Migration 4: itinerary_items (trip_day_id, snapshot of name/duration/quoted price, deleted_at). API: POST /api/trips/{id}/days/{index}/activities → 201 item {id,activity_id,name,start_time,duration_minutes,cost,cost_local}; GET/PATCH/DELETE /api/itinerary-items/{id} (PATCH {day_index?,start_time?}, other-city day → 400); POST /api/trips/{id}/days {city_id} → 201 trip; DELETE /api/trips/{id}/days/{index} → {deleted:{type:"trip_day",item},trip} (409 last_day); trip.days[].items ordered by start_time in list and single. Item/day removal soft + undoable (trip_day, itinerary_item registered in ENTITIES). Extra: GET /api/activities?city_id=. /trips/{id} is now an editor (components/itinerary-editor.tsx). Shape decision + additions note (with testids) logged; M5 + repo.md updated. Tests: web 101 pass (also under TZ=Pacific/Kiritimati); lint/typecheck/test/build green; bun run start smoke-tested on Node. Nothing committed to git.

## Next action

Start phase 5 "Itinerary view": ask the operator for the spec, then split `itinerary-view` into subtasks (API, page, tests). Daily and trip totals must sum item `cost` (home currency, already converted per item via inHome — decide with the operator whether totals sum rounded item costs or convert summed local amounts) and exclude deleted items/days (D6); reuse getPricing() per request (M5).

## Activity (derived from mechanical events)

- Derived: 6 commands, task changes: itinerary-money → done, itinerary-api → done, itinerary-page → done, itinerary-tests → done, itinerary-editing → done
- Commands run: 6
- Task changes:
  - itinerary-money → done
  - itinerary-api → done
  - itinerary-page → done
  - itinerary-tests → done
  - itinerary-editing → done
