<!-- generated by sofar — do not hand-edit; truth lives in events.jsonl -->

# Promoted to repo memory: boopada

Cite these in .sofar/repo.md by qualified handle — `boopada M<n>` —
which is how `sofar doctor` sees that a promoted fact reached repo memory.

- **M1** (2026-09-15T13:09:19.474Z) — `bun run lint` (biome check .) covers repo-root JSON too: the sofar-generated .mcp.json fails formatting until `bunx biome format --write .mcp.json`. Pre-existing a11y warnings in packages/ui/breadcrumb.tsx are warnings only (exit 0). Fixture data has no "nightlife" activities even though it is a valid category.
- **M2** (2026-09-15T13:11:08.827Z) — Smoke-testing the prod app: this machine runs other bench servers, so fixed ports (e.g. 3917) may already be taken and silently answer with someone else's app. Pick a free port (bun -e 'const s=Bun.listen({hostname:"127.0.0.1",port:0,socket:{data(){}}});console.log(s.port);s.stop()'), run `BOOPADA_DB_PATH=<tmp>/db.sqlite PORT=<port> bun run start` in apps/web, and stop it by `lsof -iTCP:<port> -sTCP:LISTEN -t | xargs kill` — never `pkill -f next`. Tests: `bun test` in apps/web (route handlers are imported and called directly with Request objects; set BOOPADA_DB_PATH per test + closeDb()).
- **M3** (2026-09-15T13:17:07.709Z) — Next.js 16 route-type validation (`.next/types/validator.ts`, checked by `bun run typecheck` after a build and during `next build`) requires route exports to accept `(request, context: { params: Promise<...> })`. Wrap handlers with `handle<C>()` from apps/web/lib/server/api.ts (context is optional in the returned type so tests can call `GET(new Request(url))`); for `[id]` routes use `handle<IdContext>` and tests pass `{ params: Promise.resolve({ id }) }`. Typing the context as `void` breaks the build typecheck.
