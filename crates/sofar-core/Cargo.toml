[package]
name = "sofar-core"
description = "sofar's native hot path: hook handlers, fold and digest behind the same CLI contract as the TypeScript engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "sofar-core"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
ulid = { workspace = true }
lexopt = { workspace = true }
sofar-schema = { workspace = true }

[lints]
workspace = true
